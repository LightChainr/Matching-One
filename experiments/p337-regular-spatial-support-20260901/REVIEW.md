# 独立复核范围

固定输入为 `2ba8863f75e0ced211b7b5442e8cddbe2fbd3deb` 的 canonical Kreg。

- **核计算：**第二代理直接从原有理核展开 equality diagrams，逐项保留系数、颜色圈数和 Q^(-b) 归一化的导数。未导入主程序、使用颜色 coarsening 或有限Q插值。独立算法完成后才比对主表，4140项全部相等。单点15项及两端C4/交换对称性均通过；回执在 [review](review/COMPARISON.json)。
- **解析桥：**另一代理独立用唯一共享颜色上的 S_Q 传递作用证明精确因式分解，再证明占据总和恒等式和同一个原U的 W[a_xy] 接口。确认需要每个单插入在Q1为零；正则性和颜色对称性本身不够。证明正文保留了这一限制。
- **实际见证：**主程序直接检查预先列出的两个占据带；独立静态检查确认它们无跨带NN边、无周期占据边，八端口确为00110011。异色时每核 `(Q-2)/(4Q)`，故 a_xy=1/16。仅检查这个构型，没有遍历16×16总体。
- **归一化纠正：**最后文稿审查要求把 `2W/N²` 明确写成首阶Q激活的二阶epsilon导数；裸Q1 epsilon导数恒零。RESULT与NEXT已修正，数值核无需改变。

核查没有计算占据平均距离曲线、双点原U值或连续极限；上界没有被误记成实际几何最大值。原局部pair单点W和四路径13/8属于输入分支已完成结果，不在这里重复计为新发现。
