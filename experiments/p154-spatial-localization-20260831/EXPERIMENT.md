# Existing-data angular bridge

Frozen input commit: `764595ea5c838c110e416382a3a90e2ecf7297bb`. The preceding conditional-line and fixed-K analyses are complete and are not repeated.

Use `h_g=Re[((a-ib)^4/N²) O4]` and `W±=R(1±h_g)/2`. The weights are nonnegative and sum to R=1−E, but do not define two discrete winding classes. With the original source-subset pooled matching root and D=mean_g ∂p〈q〉, compute

```
U± = -N^(13/8) P4[∂p〈W±〉] / (2D).
v± = -A [P4[J_W±,p]/D + pdot P4[W±,pp]/D
         - P4[W±,p] (mean J_q,p + pdot mean q_pp)/D²],
A = N^(13/8)/2, J_X=Cov(X,s), s=CB+CW bulk.
```

Every same-batch omission reuses the previously stored matching root and pdot, then updates all displayed jets and ratios. U++U− must reproduce old U; v++v− must reproduce its old bulk-source derivative, in all central and aligned omitted states. Preserve the four-cyclic shared group plus the two endpoint seed groups; current endpoints use their1000+9000 nested batch unions. No root finding, configuration replay, fitting, or new random counter.

Deliver the six-size allocations, complete covariance, each source-term allocation, and fixed q2/Jordan chain contrasts. Components are dependent and may have large opposite means or correlated uncertainty. The original high-precision production baseline is not replaced by this source-subset allocation. The calculation answers whether the already observed conditional angular coupling survives in the original global observer or cancels on integration/projection; it does not assign an energy/CFT field.

Before reading bridge outcomes, also decompose the source by the declared rank-one, fixed-K conditional projection `s_perp=R[s−E(s|K,R=1)]`. At each K this has zero mean, q*s_perp=E*s_perp=0 and therefore zero q/E mixed means and every thermal derivative. Consequently it has zero root motion, slope-source change and global U derivative. Its W± mixed means are `±(R h s − (R h)(R s)/(R))/2`, using same-archive plug-in means and zero contribution on empty/singleton empirical layers. Their soft U derivatives may be nonzero but cancel exactly. Report those two allocations and the remaining-source allocations with the same refitted-per-omission covariance. This is an explicit conditional decomposition of the measured source, not an independently acquired physical intervention, and the exact global cancellation is structural rather than a significance test.
