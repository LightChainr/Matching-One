# Roadmap: Default Attention, Not Permission

This roadmap ranks expected mechanism reduction per unit effort. It is not a permission, lock, veto, acceptance or merge system. Exact work, reanalysis, targeted pilots, production and independent theory may proceed in parallel, and a lower-ranked line may rise whenever it gains a sharper discriminator.

The evidence constraints remain narrow: preserve frozen chronology; compare claim-bearing rows only when observable semantics match or an exact map is registered; and never add correlated views of one random block as independent evidence.

For scientific coordinates use `docs/RESEARCH-MAP.md`; for claim and branch status use `docs/STATUS.md`; for execution contracts use `docs/NEXT-TARGETS.md`; for bold mechanisms and sharp falsifiers use `docs/HYPOTHESIS-BOARD.md`.

## Current inventory and default attention

The integrated control-plane snapshot is `main@4d70c1787ff97dbb98cb5e96022f947bb8fad97e` through PR #375. It inventories 59 score paths: 28 direct typed entrypoints, 27 wrapper-covered frozen kernels, 3 migration-required paths and 1 non-applicable utility. The synthetic threshold-profile shape/symmetry certificates and exact production-pair control/control motif covariance add no production threshold result or empirical invariant and do not change the scorer inventory. The motif certificate supplies neither target/control covariance nor demonstrated variance reduction. These are semantic-adoption counts, not evidence grades or task gates.

| Attention | Live object | Immediate discriminator |
|---:|---|---|
| 1 | P250 augmented annihilation+extension gate | the radius-four joint null keeps R3 while the independent radius-five direction gate keeps R2; build one old-plus-degree-five operator instead of selecting from disjoint marginal survivor sets |
| 2 | P337 birth/completion vector reuse | N680 validates the overshoot/return shape while identity dressing, same-base Jordan, free recurrence and free-single remain aliased; fit birth and line-weighted completion jointly and treat line-free `DIRECT_RANK2` as an external bypass control |
| 3 | P321 physical closure extension | exact work requires crossed plus trivial closure modules and shows regular Q differentiation is semisimple; add one generic-Q detach/loop-weight generator and test whether the affine Hom space collapses to one line |
| 4 | P334 corrected-reservoir theorem | mark-only/base-only injections are exactly refuted; prove arbitrary-HNF orbit-Hall saturation for one base exchange plus one transverse output mark, while proving BA separately |
| 5 | P333 physical Jordan control | transport the exact signed connected-history nilpotent into a positive or physical Q-dependent transfer expansion, with Gram compatibility and dimension velocity tested separately |
| parallel method | proof-carrying elimination | certify small, bounded, gauge-complete model classes when exact or interval reconstruction is available; an inconclusive relaxation never serializes or demotes exploratory work |
| dashboard | six-coordinate Operator Passport | expose orthogonal missing information across representation, finite algebra, propagation, sphere fusion, torus/modulus and microscopic control; it is not an admission checklist |

### 1. Resolve P250's empty cross-round survivor intersection

The old axis rank-three and fitted Weyl/free-commuting results are no longer the frontier. The covariance-aware bivariate Hankel score now localizes the conflict without assuming exponential roots and while allowing defective/Jordan commuting transfer. Each plus/minus two-charge hand is compatible with a five-dimensional truncated quotient (`p=.0543/.0655` at rank at most five), whereas the all-channel identity-shared block rejects rank at most five (`p=2.84e-5`). Charge two supplies the extra within-hand directions: both charge-two rows require rank at least five at alpha `.01`, while charge one requires only at least three at current resolution.

The shared result rejects one raw identity-shared degree-two column chart; it is not a physical six-state lower bound. Each hand has one compatible degree-two null line. The old direction scorer kept all Alexander rotations and identity because it discarded residual magnitude. An independent preregistered 1.2M radius-five shell at `11130ae` has now moved the live mechanism: both hand-specific annihilators extend, identity-plus-conjugation is rejected (`p=.000728`), and `Alexander_R2_conjugation` is the only frozen map above `.01` (`p=.013379`, versus R3 `.009686`). This is a thin-boundary held-out morphism on the observed moment domain, not a closed five-state algebra or field identity. Its archive names historical `Huawei-CodeBuddy-XPk2PZ`; this roadmap uses only the committed artifact and does not contact that environment.

The missing joint score is now complete at `99d23a7`. Identity-linear exactly replays the raw shared-rank statistic, including its pivot, residual, 38 modes and p-value. The scientific result rejects identity and R2 (`p=.005824`) and retains primary-family R3 (`p=.077486`). The radius-five direction gate at `11130ae` keeps only R2 (`p=.013379`, R3 `.009686`). Their frozen survivor sets are disjoint. The two gates are not independent because the published radius-five map score reuses the old 80k moments.

**Immediate decision output:** build one augmented candidate-constrained operator over the radius-four Hankel rows and already acquired degree-five shifts, keeping old and fresh jackknife influence covariance separate. Ask whether any fixed map satisfies annihilation and extension in the same model. Until then, preserve the empty intersection as a tension; do not select R2/R3 by best p-value or repeat the shell. Add degree six or ordered `TxTy/TyTx` only if the combined gate leaves a higher-degree/path ambiguity.

### 2. Use the four-generation P337 block to separate components

The R-odd-source result still identifies `[H,A,D]` as one A4 triplet and the response as A net current rather than common activity. N170 and N340 resolved the exact H4 sign sequence and rejected scale neutrality while retaining null projective scalars. The independently preregistered N680 child now lands closest to the two-mode recurrence: its source-aware residual is `-.295` predictive SE to recurrence, `+.747` to free-single, `-2.024` to fixed single and `+7.159` to neutral. Its realized variance was 1.99 times projection, so recurrence versus free-single remains unresolved rather than repairable by post-reveal sample extension.

The four-generation covariance fit with fixed `lambda0=2^-13/8` gives `lambda1=.27068+/-.12801` and passes (`.077/1`, `p=.781`), but free-single also passes (`1.979/2`, `p=.372`). The `branch_only` zero-production theory-fixed adversary at `efd6d31` sharpens the ambiguity: fixed identity dressing with eigenvalues `2^-13/8` and `2^-21/8` passes (`.803/2`, `p=.669`) and is the descriptive-AIC leader; a same-base rank-three Jordan polynomial also passes (`.084/1`, `p=.772`). These mechanisms are forecast-near-degenerate at N1360 under source covariance.

**Immediate decision output:** decompose the committed N85/N170/N340/N680 archives into a `birth x line-weighted completion` vector recurrence with full within-block covariance; use N130/N170 only as a cross-lineage control. `DIRECT_RANK2` has no projective line and must not be treated as a third additive A/H4 term. Freeze its bypass rate as an external modulation coordinate and test whether it explains the apparent identity dressing. If the same mechanisms remain aliased, add a geometry/modulus covector on which identity-family dressing, same-base Jordan and free-single transform differently. Another same-lineage size is not a universal discriminator. Missing completion winding/lift, basis, transporter and ambiguity remain explicitly `not_scoreable`.

### 3. Select the physical P321 closure extension

P321's ordinary three-scale shape score remains compatible with the frozen E4 curve, but exact descendants now type the missing object. `c2a5e2d` shows that the subleading identity-dressed correction retains the homology-resolved thermal one-point `F_t(tau)`. `9dda24e` identifies the finite contrast as `Tr(TA)-QTr(A)`, a difference of crossed and trivial closure traces rather than one central or transparent defect. `d0fca79` realizes the two closures as an exact graded direct sum, but the regular Q derivative is diagonal/semisimple. Singular Jordan confluences require an extra intertwiner and remain nonunique: the affine spaces at widths 2, 3 and 4 have dimensions 2, 3 and 9.

**Immediate decision output:** add the smallest generic-Q detach/loop-weight generator to the doubled closure module and recompute the affine Hom space with exact arithmetic. If it collapses to one normalized line, test its first-jet Gram action and overlap with the P333 `K=(D-J)^2` radical. If it remains multidimensional, retain two scalar closure amplitudes and stop describing ordinary Q differentiation as a Jordan mechanism. This is a zero-production exact gate and outranks another ordinary P2-P0 rectangle.

### 4. Prove the corrected P334 reservoir or find its next obstruction

P334 now separates scalar balance timing from character-Gram geometry and has collapsed the Hall family. Translation regularity gives

```text
g(U)=|U|/N*g(V)+(D_cut(U)+S_cut(U))/2,
```

so the all-site aggregate TM inequality implies every proper Hall cut. The 2,470,440 bounded cuts and 984 carrier-layer rows are exact regression checks, not additional hypotheses or reasons to enlarge the atlas.

For a fixed line and lower layer, choose a state and an ordered pair of distinct absent sites. Its two-site face has four motifs: coexit `D`, mixed `M`, synergy `Y` and flat `F`, with `T=D+M+Y+F`. The latest exact reduction is

```text
M^2 + 4Y(T-D) - 4DF = 4(m-1) TM_margin,
```

so aggregate TM is exactly the curvature-corrected Rayleigh inequality

```text
4DF <= M^2 + 4Y(T-D).
```

The complete oriented rank alphabet is `D=1222`, `M_left=1212`, `M_right=1122`, `Y=1112`, `F=1111`. The only negative product is `D x F`; the only positive reservoirs are `M x M` and four replicas of `Y x nonD`. A finite lexicographic rewrite closes all 984 audited rows, but exact configuration work at `c2d8170 -> 6a7b042` shows why the naive injection fails. Mark-only and every union-preserving crossover fail at N6; after enforcing fixed-line output bases, base-only reaches `588/1152` and transverse-only `768/1152`. One carrier-base exchange plus one independently released transverse output mark reaches `1152/1152` on every minimal N6 row and `5760/5760` on the first Smith-(2,4) N8 gate. Translation acts freely, so full Hall and factor-N compressed orbit Hall are equivalent for arbitrary HNF.

**TM target:** prove that the corrected base-exchange plus transverse-mark graph has orbit-Hall saturation for every HNF, or locate its next minimal obstruction. Do not revisit mark-only, base-only or union-preserving rules. The unresolved capacity is `K=max(0,4DF-M^2)-4Y(T-D)`.

**BA target remains independent:** construct a weight-preserving injection from discordant birth/exit marked state pairs to concordant pairs. Alexander complement preserves the BA determinant but does not provide the within-layer switch. No further local motif, Fourier, displacement, quotient-order or Hall-cut scan addresses either missing general injection.

### 5. Move P333's signed connected-history control into the physical cone

Raw Q tangents remain lift-dependent, and the universal first-jet Gram radical remains nonselective. The post-267 exact positive control is sharper:

```text
K=D+J-DJ-JD=(D-J)^2,
rank K=1, K^2=0, HK=K^T H.
```

On three marks it supplies an exact isotropic-bottom Jordan chain compatible with the first-jet Gram form. Join-only dynamics and bare deterministic detach/join words do not. The signs make `K` non-stochastic, so this is not yet a physical transfer or LCFT field identification. **Immediate decision output:** derive the connected-history subtraction inside a positive or physical Q-dependent transfer expansion, or prove that the physical cone cannot contain it. Gram compatibility, positivity and agreement with the known dimension velocity are separate gates.

This is the live **connectivity/defect radical** theory line. Adding more equivalent semisimple scalar Q-source derivatives has lower mechanism gain than transporting this explicit radical action into the physical cone.

### 6. Run proof-carrying elimination as a parallel method lane

For fixed state dimension, real and imaginary matrix entries and their word predictions form a polynomial model. The best certificate targets are small declared classes already made live by data: scalar versus ordinary rank two, shared-eigenpair commuting rank four/five on P250, and one shared square-zero nilpotent with the P333 Gram condition.

A claim-bearing SOS/moment certificate requires bounded or Archimedean parameter support, complete similarity-gauge coverage, explicit singular-covariance handling, a frozen outer confidence set and exact or interval reconstruction. A numerical relaxation value is diagnostic, not proof, and finite-order success is not guaranteed. P321's closure and extension obstructions already show that exact semantic/module witnesses can eliminate mechanisms before SOS. Proof-carrying elimination is therefore high value when cheap and decisive, but it does not sit in front of invariant identities, model-free structure discovery, production, exact work or speculative mathematics.

### 7. Use the Operator Passport as a six-coordinate dashboard

Track representation/projector zeros, finite TL/partition-algebra action, propagation words and rank, normalization-free sphere fusion, torus/modulus response and a cross-microscopic positive control. Each coordinate may redirect attention independently; “passport complete” is not a lifecycle state or a prerequisite.

The literature boundary is exact. Loop-model three-point work motivates normalization-free OPE ratios, and torus one-point work motivates modulus ratios, but both are **candidate discriminators** for Matching One rather than established fingerprints for its current lattice row or arbitrary Jordan/defect fields. Finite-lattice singular-vector relations are an exact **ADE RSOS/root-of-unity TL template**; transport to percolation/Q4 is a new theorem problem, and even the ADE paper leaves convergence to Virasoro singular-vector relations open. For Jordan structure, the supported finite-width dashboard is the eigenvalue gap, positive-metric eigenvector overlap and Gram-Schmidt generalized-vector coupling, not the presence of a literal finite-size Jordan block.

P321's fresh scale/E4 pass remains ordinary scalar compatibility, not transparency or field identity. Its exact closure-module descendants now make the next continuum-facing gate concrete: add the smallest generic-Q detach/loop-weight action and test whether one canonical extension survives. The exact Q=4 colour ratios and the single-orbit spatial alias gate remain distinct coordinates and must not be pooled.

### 8. Preserve the remaining orthogonal lanes

The completed Gaussian-annulus rectangle does not reject one effective generator on its local pivotal rows, but it still lacks path order. A maximin `AU` versus `UA` cell or intermediate state is more informative than another replica. The external Euler N325/N425 determinant likewise leaves the current source/observer basis projectively rank one; the next source must be physically new, such as cross-boundary connectivity, winding/seam character or a K1/K2-conditioned Euler hazard.

The triangular energy/log-partner result is a positive microscopic control, while its raw amplitudes and `kappa_proxy` remain gauge-dependent. A normalization-free top-field statistic and a typed square row at two moduli are still useful dashboard coordinates. An exactly critical oblique family whose microscopic H4 proxy crosses zero remains an independent opportunity.

## Parallel exact-topology promotion

Three branch-only theorem claims change the role of brute-force frontier growth. `23fd682` proves that the unique honest-torus homological balance root converges to square-site `p_c` using strict finite-volume monotonicity, subcritical exponential decay on the square and matching lattices, and the exact complement identity. It gives consistency, not a root-shift rate; the next theorem is a two-sided near-critical homology bound at `p_c+/-lambda L^-3/4`.

`73d4960` proposes unrestricted rational Digital Alexander duality for every nonsingular integer period matrix via the canonical four-sheeted `2L -> L` cover. Child `c1a72e5` proposes the integral refinement: honest carrier images on every `qL` cover plus a prime coprime to any hypothetical Smith defect force saturation and `iota=1`. Main-integrated PR #330 now places the finite regression through index 13—140 HNFs and 101,140,028,118 weighted paths with zero failures. The highest-value exact action is line-by-line review of carrier embedding, matching-diagonal component preservation and `H_up=H intersect qL`. If those hold, higher-index growth has regression value rather than inferential weight for saturation; if one fails, the index-13 backend is the counterexample locator.

The separate P334 line has already converted its ULC observation into a conditional theorem. `a9da7cd` proves BA plus aggregate TM on both complementary carriers imply ULC, `c8405b5` makes every proper Hall cut an exact corollary of the all-site cut, and `7d16c88` identifies aggregate TM with `4DF<=M^2+4Y(T-D)`. Exact children `c2d8170 -> 6a7b042` refute mark/base-only switching and identify the first successful resource as base exchange plus a transverse output mark, with factor-N orbit-Hall compression. Promotion now requires uniform arbitrary-HNF saturation of that corrected graph plus the separate BA switch; more bounded Hall scans have regression value only.

Main-integrated PR #319 separately freezes a bounded PSLQ search contract. It contains no integer-relation result, exclusion, preferred decimal or closed-form claim. It is a low-cost side sprint after the higher-value stored-data and topology work, not threshold evidence.

## Parallel threshold-origin program

Operator identification does not determine the microscopic numerical value of square-site `p_c`. Main now records a bounded mechanism map: known exact site routes rely on self-matching closure, an independence-preserving line-graph map or separated self-dual three-terminal cells; standard square-site has a different matching lattice, overlapping site-to-bond correlations and a four-terminal periodic cell. This explains why the catalogued moves fail without claiming that every future exact representation is impossible. A serious new construction must preserve the full law and predict a basis-independent identity.

The block-event line has separately moved from feasibility to an auditable execution contract: exact tiny-event semantics, an O(`s^2`) evaluator with 520/520 differential agreement, and a content-addressed familywise plan with 400 trials per attempt and acceptance cutoff 373 are on `main`. The missing empirical object is fresh IID acquisition.

Main-integrated PR #331 adds a fail-closed exact-rational comparison schema, but only for two hard-coded synthetic artifacts. It is not a bound, a real gadget certificate or evidence about `p_c`, and its rational arithmetic is unrelated to rational Digital Alexander homology. The next claim-bearing step is deliberately small: reproduce one published baseline end to end, bind the actual artifact bytes, independently derive its certified probability interval and compare one canonical real candidate before broad enumeration.

Main-integrated PR #340 separately freezes the Issue-27 rational-width prereveal boundary through width 21 plus the existing width-22..24 forecast. No primary 22..24 artifact or score exists. Treat arrival of that source as an external trigger: bind it and score the frozen metrics once before any refit. Its “Stage B” is unrelated to P321 modulus enrichment.

The shape-contract line has now acquired branch-only retrospective production evidence. Exact finite-size collapse is strongly rejected, but after median/IQR removal the norm-2, norm-5 and independent N185-to-N265 residuals lie on an almost common upper-tail direction, with cosines `.9915` to `.9996`. Freeze that vector and score only its amplitude on a fresh lineage; connect the amplitude to the thermal jet and activation-shape fiber. Do not fit a universal exponent from the current `.632/.429` effective powers or count two norm-5 views as independent votes. Correlated-hyperedge/self-dual closures and symbolic obstructions may run beside all eight priorities.

The exact anisotropy-stencil gate supplies a second microscopic design filter. A nonempty nonnegative axis-only fourth-moment proxy cannot cross zero; in the declared axis-plus-integer-diagonal normalization cancellation requires `4:1`, and a more general positive-weight search needs a negative-phase oblique orbit. This prevents a blind axis-only scan while leaving exactly-critical oblique constructions and the renormalized H4 coupling fully open.

## Existing infrastructure to reuse

Reuse the information-per-CPU optimizer, sequential-stopping calibration, threshold-rank histograms, aligned batches, full curves, joint moments, Krawtchouk modes, topology/deck labels and transported frames. Add main's exact fixed-rank motif contrasts to same-N paired streams when their dependency boundary is retained. A new coordinate on an old archive may be decisive while remaining dependent on that archive.

Issue #40's exact production-pair certificate now supplies the full control/control Gram for the four declared fixed-K motif contrasts on five Gaussian pairs. All conditional means are exactly zero; the Gram is full rank for every `4<=K<=N-3`, and the N130/N170 `nn_edge` versus `diagonal_pair` entry is exactly zero for every K. This removes the need to estimate the control Gram from production, but it measures no target/control covariance, fitted coefficient, variance reduction or wall time and does not satisfy the `>=2x` promotion gate. **Minimum pilot:** retain paired batches, estimate only target/control cross-covariance, fit without leakage, and report held-out variance reduction. Motif control remains a shared method lane, not a scientific mainline.

Main also has a coherent 200-descriptor/952-map registry. At `main@4d70c1787ff97dbb98cb5e96022f947bb8fad97e`, through PR #375, the live corpus still contains 59 paths: 28 direct typed entrypoints, 27 wrapper-covered frozen kernels, 3 migrations and 1 utility. PRs #373/#375's synthetic exact threshold-profile shape/symmetry, the earlier threshold Krawtchouk replays and PR #374's exact production-pair control/control covariance do not modify frozen kernels or production results and do not constitute an empirical threshold invariant. The covariance certificate establishes neither target/control covariance nor held-out variance reduction. This is a semantic transport layer, not a new evidence grade; topology typing does not yet type every derived response. `Migration_required` does not invalidate a result.

PR #339 provides a reusable exact odd-jet normalization under linear thermal-coordinate rescaling, and PR #349 supplies exact order-five synthetic nonlinear bare-coordinate elimination. Do not schedule either as a separate production line. The remaining methods gap is a typed real-observable adapter with common normalization, delete-one recomputation, root uncertainty and full joint nonlinear covariance on an existing shared-observable block.

Merged PR #341 types the C4 holdout, PR #344 types both Issue43 full-curve kernels, and later wrappers through PR #372 include prequential, P231, v14 scalar-root, Issue43-secondary and threshold Krawtchouk replay; none changes production data, score or scientific priority. PRs #373-#375 add bounded exact profile/motif certificates rather than scorer paths or physical evidence. Excluded states remain excluded, reused stages do not become independent and implicit roots remain response coordinates.

PR #342 records a CPU-to-GPU resource gate, not a scientific lane. CPU remains the measured default; GPU attention rises only after the identical end-to-end workload exceeds 5x while preserving exact output and determinism. The current absence of such a measurement is not a prohibition.

Current utility should reward

```text
mechanism classes removed
x new observer/source/geometry axis
x independence from reused random blocks
x nuisance cancellation and archive reuse
/ expected compute under a valid stopping rule.
```

## Completed results that change repetition value

- **#50 N145->290 full curve — complete:** the resolved shape direction already rejects one scalar multiplier. Its archive remains reusable for new typed coordinates, while another endpoint-only repeat has lower information gain.
- **Digital Alexander frontier:** main-integrated PR #330 is exact through index 13 with 140 quotients and 101,140,028,118 weighted paths. Branch-only `73d4960` supplies the rational-cover theorem and child `c1a72e5` claims integral saturation. Review the child; if it holds, use the finite frontier as regression rather than evidence for saturation.
- **P205 ordinary prism:** H4 beats the frozen H8/H12 lines. A `rho`/C3 run gains more information from a distinct typed observer than from repeating this selector.
- **PR #267 activation reuse:** all ten archived pairs are scored, the full curves turn three negative root points into stable local node crossings, and the P205 subset resolves H4/H4. The new transport/shape reuse shows that the K2 pointwise sign split is positive translation plus negative zero-area deformation; `W/ell/landing` attribution is now more valuable than another root split.
- **Marked-birth and external-observer chain:** q-only contact closure, bare mean-line q2 and the two-radius local rejection retire three narrow carriers. The completed `26cecb4` two-observer result does not reject the current rank-one lane (`4.279/4`, `p=.370`). The next discriminator is a physically new source direction, not another observer design or repeat.
- **PR #288 mixing audit:** parity gives the four powers but the amplitude map is rank four. Two external Taylor ratios, not another parity argument, are the live closure variables.
- **Q=4 seam plus main-integrated PR #291 alias gate:** colour characters and normalization are exact, and a single C4 orbit aliases scalar/spin 4. The missing spatial kernel needs calibrated phase diversity or typed internal data; six raw seam values alone certify colour sector, not pure H4.
- **Same-semantics context rectangle:** one shared generator is not rejected (`p=.4002`), but lambda and path order remain unidentified. The next row must separate models or measure `AU/UA`.
- **Topology-typed C3:** N112 is genuinely multi-character; the E4-squared completion phase remains viable but unresolved, and N60-only replication has a proved power ceiling. Strengthen N112 or change geometry.
- **Threshold shape flow:** exact collapse fails, while an almost rank-one upper-tail direction replicates across cover and independent-size blocks. Freeze its direction before a fresh-lineage amplitude test.
- **P321 closure extension:** fresh three-scale data do not reject conditional E4, but exact descendants require crossed plus trivial modules and show regular Q differentiation is semisimple. Test the generic-Q detach/loop-weight Hom dimension rather than replicate ordinary shape.
- **Projective birth/twist flux:** P334 isolates a corrected base-exchange plus transverse-mark reservoir and keeps BA separate. P337's N680 child validates the overshoot/return shape, while aggregate fits leave fixed identity dressing, same-base Jordan, free recurrence and free-single aliased. Fit birth and line-weighted completion jointly, using line-free direct-rank-two births only as a bypass control; a geometry/modulus covector outranks another size if aliases remain.
- **P250 augmented joint gate:** hand-specific rank-five quotients survive, but the radius-four joint gate keeps R3 and the radius-five direction gate keeps R2. Their empty survivor intersection is the result. Combine old and degree-five rows in one operator before assigning a fixed bridge.
- **Q-lift and connected-history transport:** P333 proves raw Q jets are lift-dependent and the raw dual-number Gram radical is universal/nonselective, then supplies the exact signed nilpotent `K=(D-J)^2` as a Gram-compatible Jordan positive control. Physical Q-dependent realization, positivity and dimension velocity now outrank another untransported scalar derivative.
- **Gaussian norm-10 power:** source-only planning projects 27.21B/55.18B samples per lineage and negligible ideal-CRN gain. Defer production; only a target variance/runtime pilot can reopen it.
- **Issue #40 motif covariance:** exact geometry fixes the control/control Gram, including full central-K rank and the N130/N170 `nn_edge`--`diagonal_pair` zero. The remaining pilot is target/control cross-covariance plus held-out variance reduction; no production target or `>=2x` result exists.
- **Semantic control plane:** `main@4d70c1787ff97dbb98cb5e96022f947bb8fad97e`, through PR #375, inventories 59 paths: 28 direct, 27 covered, 3 migrations and 1 utility. The synthetic exact threshold-profile shape/symmetry certificates and exact production-pair control/control motif covariance add neither a production threshold result nor an empirical invariant. The motif certificate does not supply target/control covariance or demonstrated variance reduction. Synthetic exact primitives, regressions, resource gates and semantic replays are not new scientific evidence. Migration changes future composability, not historical evidence role; the next layer must record units, model/harmonic, size map, chronology, response semantics and dependencies.
- **Norm-4:** analytic q2 and the frozen common generator fail; the frozen generation-four target keeps the small-state family live but does not separate pure Jordan, an analytic even mode or persistent curvature, nor establish a nonzero extra mode.
- **PR #247 / annulus:** the ordinary plus-shell control passes, the matching-odd minus-shell scalar law fails and matched-delta equality is marginally tense inside one correlated block; a distinct two-state recurrence survives held-out N365. The sector score rejects rank one specifically for `A_minus` but leaves several rank-two dynamics aliased. A discriminating context is higher value than more same-row replicas.
- **Triangular log pair:** the cross-cutoff shear is measured; repeat production without gauge closure is lower value.
- **P263 stopped estimator:** unbiasedness and the mechanism are exact; variance advantage remains unmeasured until the stopped/global comparison is paired.

These are information judgments, not prohibitions. New semantics, a new observer or an independent geometry can immediately change the ordering.

## Secondary opportunities and repetition-value signals

- **Same-N coalescence:** the exact amplitude-free interpolation remains ready when a new Smith class can fill the Gaussian/annulus context rectangle or distinguish an eigenvalue alias.
- **Local pivotal N130/N170:** the current two response rows are nearly rank one. The historical signal to “stop adding replicas” means that replicas of the same readout have low present information gain, not that the task is forbidden; a thermal-null, representation-typed or line-versus-landing row can immediately raise its priority.
- **Third primitive norm-2 generation:** it remains an independent opportunity when its new phase, character or Smith geometry separates mechanisms rather than extending the same marginal fit.
- **Ordinary H4/H12 opposite-alias result:** branch `86e77db` completes 600M/design. H4-only and zero both survive and A12 is unresolved. The third alias stays available, but an independently stronger source has higher current information gain.
- **Gaussian character commuting square:** main PR #328 freezes exact root factors, but branch P158's source-only gate projects 27.21B/55.18B samples per lineage, expected 500M z only `.470/.337` and no useful covering-CRN gain. It has no target roots or empirical commutation result. Use only a target variance/runtime pilot before reconsidering production; keep it distinct from ordered Gaussian/annulus `AU-UA` dynamics.
- **Scalar width and free exponent repeats:** the declared scalar-width shortcuts have failed and another free exponent alone is weak discrimination; a new observer, source or exact typed map remains welcome.
- **Microscopic H4 design:** an exactly-critical negative-phase oblique orbit can immediately raise this lane; axis-only nonnegative proxy scans cannot realize the declared cancellation and therefore add little mechanism information.
- **Threshold confidence acquisition:** the frozen 400/373 plan is ready for fresh IID trials. New closures or symbolic arguments remain parallel alternatives rather than prerequisites or vetoes.

## Lifecycle maintenance and decision rule

Frozen predictions and preregistrations remain immutable. Every unmerged pointer must carry branch, commit, path and `open_pr` or `branch_only`; every shared-line claim must be reproducible from `main_integrated` artifacts. Retrospective reuse must keep dependency groups and full covariance visible.

Keep four statuses separate when a lane advances: `integration_status`, readiness flags (`exact_gate`, `protocol_frozen`, `implementation_ready`), acquisition stage (`no_data`, `smoke_only`, `production_acquired`) and result stage (`no_score`, `engineering_score`, `claim_score`). Main integration is not mechanism acceptance, and an exact mapping gate plus smoke is acquisition readiness rather than an empirical coupling result.

Prefer observations that force different zeros, signs, parity channels, representations, ranks, complex characters, composition laws or function-valued shapes. Slightly different fitted exponents are lower information when a typed algebraic discriminator is available.
