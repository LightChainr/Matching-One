# Next Targets: Mechanism-Changing Analyses

**Updated:** 2026-08-31, after the [actual full-chain source response](../results/norm4-source-thermal/REPORT.md) and [P40 original-U response](../results/p40-source-thermal/REPORT.md), continuing from the completed N900 and even-given-odd results. Main incorporated through `e300609`; these new calculations remain in Draft #267.

The immediate priority is **physical mechanism discrimination**, not another signal-existence test or synthetic preparation round. In particular, `E_top=P0+P2` is a topological coordinate, not an identified energy operator. Canonical Phase D is done; the original norm-4 second-direction/field question is still open.

This is the single current attention board. [Repository Context](REPOSITORY-CONTEXT.md) explains what the full review recovered; the [complete Issue/PR index](ISSUE-PR-INDEX.md) preserves all tasks; [the earlier detailed board](NEXT-TARGETS-history-20260831.md) is historical. Priorities do not lock, close, reject or require approval for any parallel line.

## Default attention

| Attention | Question and discussion home | Completed input — do not repeat as a first step | Next scientific output |
|---:|---|---|---|
| 1 | **Does a common microscopic source change the norm-4 generator?** [#275](https://github.com/LightChainr/Matching-One/issues/275), [#154](https://github.com/LightChainr/Matching-One/issues/154) | [Original U source response and both full chains are computed](../results/norm4-source-thermal/REPORT.md), with root motion and slope response. Six-size `dp0/dlog Q≈.029`; source-rigid q2/Jordan and the common-generator drift remain unresolved on100k old permutations per N. The [topology/within-sector decomposition](../notes/p40-thermal-clock-source-quotient.md) uses the same source marks. Earlier C and the independent million-sample P40 U response are also complete. | Use the already precise full-production q/E curves to reduce source-response noise, then evaluate the explicitly stated source-rigidity versus generator-drift predictions. Track three-state reweighting and within-sector thermal information separately. Preserve the common microscopic source `s=CB+CW`: its derivative is `v_N=N*Udot_density`. First U derivatives, first full-chain source residuals and first jet splits are no longer preparation tasks. |
| 2 | **Which physical change distinguishes the named propagation mechanisms?** [#398](https://github.com/LightChainr/Matching-One/issues/398), [#333](https://github.com/LightChainr/Matching-One/issues/333) | Current deletion `520a9d21` leaves the fast/slow inversion. New `33c6028f` retains the entire instantaneous Je direction, yet the two-observer projection still misses99.7429% of plus t=4 correlation; omitted initial curvature is exactly the reversible hidden-force norm squared, \|QSe\|². | Compare a named hidden reversible geometry/transport prediction with a microscopic or matching overlap. First current deletion and first current-optimal observer are complete. Capturing instantaneous current does not recover propagation, and a projected observer pair is not a new Markov chain or an identified square-site field. |
| 3 | **What controls the intrinsic rank-clock width across scale?** [#205](https://github.com/LightChainr/Matching-One/issues/205), [PR #484](https://github.com/LightChainr/Matching-One/pull/484), [#485](https://github.com/LightChainr/Matching-One/pull/485) | [N90032M/800 is completed](../notes/frontier-increment-20260831f.md), `5f30397c`, after N1002M/N4008M. Vz2.33946±.12039; the two frozen conditional width predictions remain compatible (p=.13497/.08418), sharing target/anchor covariance. Common-symmetric-two-lobe obstruction is a separate completed result. | Ask competing physical mechanisms for additional joint area/center/de-width shape or source responses using the existing histograms/moments. First consume already delivered shape results, then score a genuinely different prediction. Do not repeat first N900 production, force a winner from two correlated p-values, or call finite effective width.30688±.03861 an asymptotic exponent. |
| 4 | **Which full continuation structure carries the population direction response?** [#334](https://github.com/LightChainr/Matching-One/issues/334), [#429](https://github.com/LightChainr/Matching-One/issues/429), [#487](https://github.com/LightChainr/Matching-One/issues/487) | All147 complete clocks, actual finite-mixture noise budgets and12-prefix canonical crossings are done. New `d53db2f` proves every uniform blockade mean is determined by the full clock; conditional final-site collision determines spatial response variance, which separates the constructed isoclock example. | Use spatially marked conditional response/variance, then paired stratum-weighted population loading with cut covariance. Another mean blockade-dose curve cannot separate equal-clock geometries. The constructed example is not an observed isoclock pair among147, and that one-direction fixed stratum is not global H4 attribution. |
| 5 | **Does the resolved source separation reach matching?** [#337](https://github.com/LightChainr/Matching-One/issues/337), [#439](https://github.com/LightChainr/Matching-One/issues/439) | F5 even rows resolve distinct W_line/JS responses at both N325/N425; PR #451 and the direct/plateau split leave canonical M loading unresolved. | Transport the already defined F5 readouts to a genuinely discriminating geometry/common stream and estimate their relation to M. Preserve the distinction between positive source/readout rank and unmeasured matching coupling; another K_A-only size does not answer it. |
| 6 | **How do two measured C3 observer directions transform?** [#156](https://github.com/LightChainr/Matching-One/issues/156), [#226](https://github.com/LightChainr/Matching-One/issues/226) | N112 primitive-line and E_top responses are measured; `2402a33` rejects their common child ray. N60-only power limitation is known. | A joint complex character prediction for these two fixed observers on a nondegenerate geometry, retaining real/imaginary covariance. Distinguish finite square-bond topology from square-site matching and continuum field assignments. |
| 7 | **Does P250's state increment survive a complete spectral adversary?** [#250](https://github.com/LightChainr/Matching-One/issues/250), [#418](https://github.com/LightChainr/Matching-One/issues/418) | 30.97% held-out gain beyond the declared residue baseline; raw positive cones and radius-resolved CRT comparisons are done; residual k=36 remains. | A same-stream full-terminal-periodogram comparison with the pre-outcome state and common-k36 readout. The required endpoint fields are absent from the existing state archive; this is a targeted acquisition option, not a repeat cone fit. |
| 8 | **What survives a physical change in context order?** [#253](https://github.com/LightChainr/Matching-One/issues/253), [#255](https://github.com/LightChainr/Matching-One/issues/255) | Static Gaussian/annulus rectangle is done. N650 ordered-filtration proxy is also done, with exact covariance rank three. | Explicit annular boundary connectivity and a Gaussian lift/pushforward that make AU and UA comparable, followed by their physical ordered response. The existing filtration proxy is not the missing physical commutator. |
| 9 | **What comparison follows from the explicit W5 periodic pair?** [#14](https://github.com/LightChainr/Matching-One/issues/14), [PR #438](https://github.com/LightChainr/Matching-One/pull/438) | Joint relative-dual state and periodic primal/dual edge bijection are complete. The two graphs are not self-dual. | A concrete stochastic comparison or exact local transformation between those graphs, or the connectivity event obstructing it. Use ordered port states: seven D4 orbit labels do not form the serial quotient. |

Attention 1 now starts after an actual complete-chain source calculation, attention 2 after complete instantaneous-current observation, attention 3 after completed N900, and attention 4 after uniform-blockade mean closure. These are scientific handoffs, not reservations or a new approval sequence.

## Completed source experiments and the new generator question

The original-U bridge is now measured, including both norm-4 lineages. The [full-chain report](../results/norm4-source-thermal/REPORT.md) contains source-rigidity and single-generator-drift residuals, not another planned interface. It also explains the finite subset's endpoint noise. A source `a+bK` common to both directions has exactly zero U response after root relocation; this is the thermal-clock quotient. The same raw source is decomposed into a three-state source and within-sector cluster/occupancy coupling, with full covariance rather than independent component votes. NZ N85's positive2.37-SE W hint has no same-direction support in the independent P40 million stream, so it does not select a mechanism. The next [two-phase calculation](../notes/norm4-source-two-phase-estimator.md) uses the full-production complement as an independent baseline and cross-fitted source marks; it needs no new configuration replay and has not yet been computed.

The earlier C experiment remains useful context:

For the same source `S=(c_blackNN+c_whiteMatching)/N`, let `E=E_top=q²`.
The now-completed readout is

```text
C_even_given_odd = Cov(E,S) − Cov(E,q) Cov(q,S) / Var(q).
```

In the positive two-source measure `exp[lambda S + h(lambda) q]`, choose
`h'(0)=−Cov(q,S)/Var(q)` to keep the matching mean fixed to first order.
Then C is the E response, and **−C is the rank-1 probability response**.
The measured positive C rules out a pure relative-q fugacity tangent for
the first-order finite topological marginal at this p. It does not identify
an energy operator, finite-lambda equivalence or a second continuum field.

Use the common raw S as the primary physical comparison. A separately
named compensated `S_res` can use the same formula, with per-geometry
clock/Euler coefficients explicitly retained; it is not one universal
source across geometries. Subsequent matching compensation need not keep
the earlier Z controls fixed simultaneously.

Each direction's C, exact `Delta cos(4theta)` contrast,49×49 covariance
and100 aligned delete-one vectors are saved. Missing `sum(E*S)` and four
`sum(E*Z_i)` products were supplied by one old-counter reobservation.
There is no first even-sector replay left to schedule.
The engine includes N in its random key, so N65/N85
are separate PRNG domains under the usual independence assumption, not
shared streams merely because their seed/counter labels agree. All views
of each archive retain their original dependence.

The [original norm-4 object](../notes/p40-even-response-norm4-interface.md)
is `U=N^(13/8) P4[partial_p<E>]/(2 Mbar')` at each N's pooled matching
root. Its q2/Jordan residuals use65→130→260 and85→170→340. The now-measured
source observable is `L4[partial_lambda U]`, **not `L4[C]`**: it includes
root/slope motion and `kappa(E,S,K)`, `kappa(q,S,K)` at the proper thermal
coordinate. A model must predict its source response rather than inherit
the old U scaling law automatically. The existing20k N65/N130 source CSVs
can already give supplementary fixed-p C without replay, but not this
three-size root-normalized residual.

## The E_top survivor table

These rows answer different questions and do not combine into one independent evidence total.

| Declared question | Current answer | Meaning for the next step |
|---|---|---|
| Is canonical E_top zero on the ten-size / six-size norm-4 archives? | Rejected by completed full-covariance analyses in Draft #267. | Do not repeat the first detection. |
| Does one A/E ray describe everything? | Specific nested lineage comparison rejects sharing; a broader Gaussian-Bonferroni confidence region still admits `r=-2/3`. | Different confidence constructions are not contradictory model votes. |
| Has #370 touched real production? | Yes: `f5779b9` uses eight 100M/500M rows and rational certificates for E=0 and E=±A. | Generic physical M1/M2d/M2j compilation and exact finite-sample coverage remain separate, not prerequisites for using existing scores. |
| Does the N50 factorial only change amplitude? | Four-coordinate amplitude-only model fails; A/E alone does not. | The correlated response/clock relation changes. Do not claim E alone rotates or that two fields are identified. |
| Is a second physical energy/singlet field selected? | No. The finite even tangent, original-U source response, complete-chain residuals and within-sector split have now all been computed; a shared root response is resolved, while source-rigidity/drift remain unresolved. | Improve the actual source estimate using high-precision existing baselines and compare the explicit source-extension predictions. A finite source tangent is not a continuum-field count. This is attention1. |
| Does high Boolean degree identify the field? | No. P437 measures localized high-order topology; 99.8485% of that fixed-support energy lies above degree five. | A real positive result, but a different measure/readout and not a CFT dimension. |

## Parallel opportunities, including genuine unfinished items

- **#418 / P406 shared spectrum:** the [normalized same-archive analysis](../results/p418-normalized-archive/REPORT.md) is now complete. All four common raw/masked spectra are compatible under the inherited convention, and their saved positive solutions attain the resolved linear least-squares floor. The old large common-mask rejection no longer supports radius flow. Radius5-only fits and derived sharing penalties are numerically unreliable and not mechanism scores; independent P250 state/rank and exact CRT results remain. Next use the normalized common family for genuine separating prediction envelopes/readouts, not another first normalization run or CRT/anchor preparation.

- **#119 / PR #230 multi-u:** the dedicated N145→290 joint score has no completed artifact in the reviewed snapshot. Its template postdates the already public N290 full curve. Existing-data completion would be retrospective unless an earlier public freeze is supplied; #236's quantile-center score is not a substitute. This is a concrete analysis option, not another template task.
- **P437:** keep the measured fixed-support lower-bound contribution and coherent/incoherent split. An outside-support noise kernel could separate degree distribution, but the saved hashes/class counts do not retain the outside configurations needed for it. No such kernel was run during this review.
- **Cross-microscopic controls:** triangular log-pair cross-cutoff production is positive. A normalized top-plus-two-spin statistic and a matched two-modulus H4/E6 invariant address gauge questions that another bare amplitude ratio cannot.
- **P334 Hall / direct birth:** arbitrary-HNF two-mark injection and BA remain theory questions; finite N9 repair and typed theta/figure-eight census are complete. Uniform landing/type frequency and near-critical activation scaling are distinct missing bridges.
- **Motif controls:** real N65/N85 target covariance and cross-fitting give 1.363×/1.331× variance reduction. Use those results opportunistically; first-pilot readiness is no longer a task.
- **Boundary/Q tangent:** production and stopped-transcript pilots already exist. Tiny VJS `1055e221` also already closes the full measure + finite-projector + explicit-field derivative with all three terms nonzero. The next question is fixed-source/field overlap or scale/modulus dependence with bottom-field treatment, not another first finite bookkeeping oracle.
- **Threshold origin:** typed site law and ordered terminal algebra are available; degree-1/2 relation exclusions and the bounded three-terminal screen are complete. New law-preserving maps, genuine event probabilities and near-critical bounds remain open.

## Team handoff and resources

For a work block, aim at one measured response with uncertainty, one exact scientific object, or one discriminating model prediction. Attach the last input commit/path, observer, dependency block and remaining question. This can be a short result card; it need not become another schema or framework project.

Use existing discussion homes and branches: #154/#275 for physical norm-4 identity, #484 for scale production, #398 for propagation, and #334/#429/#487 for continuation. #370's framework and #13's finite serial algebra remain reusable parallel support. Before calling something “next,” consult its latest result card; do not infer lack of production from an old title or open checkbox. This is coordination advice, not a reservation or lock on any route.

Use `main_integrated`, `open_pr`, `branch_only` and `hypothesis` independently of scientific maturity. A branch-only measurement can be complete; a merged tool can still have no physical application. An inconclusive measurement is still a completed measurement. Retrospective views share their original covariance and do not become independent replications.

The20k absolute-source result used old configurations; P40's q/source calculation read saved Gram statistics in about.11 seconds, and its new E_top supplement reobserved each old million-counter block once in18.29/23.44 CPU seconds. No new Monte Carlo samples, test suite or server job were launched by this task. **No GPU is needed for these readouts.** N900 is a separately completed team production, not a new launch here. Only `DevEnvC_TV2N0X` / `4a8d1d443419434889e49148ed0a7ba6` is within the user's server scope; other environments and tunnels remain untouched. Future acquisition is a separate execution choice, not implied by a row in this board.

Detailed semantics and data paths live in [Status](STATUS.md), [Research Map](RESEARCH-MAP.md) and [research_ledger.yaml](../analysis/research_ledger.yaml). No Issue is locked, closed, demoted or renamed, and no PR is merged by this prioritization.
