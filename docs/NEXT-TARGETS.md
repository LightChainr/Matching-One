# Next Targets: Mechanism-Changing Decisions

**Updated:** 2026-08-30

This is the fast decision board. It asks:

> Which next output would most change the scientific model space, given the state, source, observer, geometry and acquisition already available?

It is not a queue or permission system. The listed lanes may run in parallel; priority changes attention, not authorization. Lower-ranked or unlisted work is not rejected, locked or required to merge.

## Read lifecycle before priority

One line may have a main theorem, a branch-only consequence, an open-PR result and a proposed production. Use these labels:

```text
main_integrated  canonical shared result
open_pr         inspectable but not merged
branch_only     inspectable remote frontier
hypothesis      proposed mechanism/discriminator
```

An old issue title does not override a completed result. A post-reveal fit is not a new independent block. Several derived coordinates from one histogram remain one dependency group.

Integration, readiness, acquisition and result status are separate. In particular, `branch_only + exact_gate + protocol_frozen + implementation_ready + smoke_only` means acquisition-ready; it is not a production result or an accepted mechanism.

## Current decision targets

| Rank | Decision target | Five-axis vector | Actual maturity | Exact next output | Acquisition |
|---:|---|---|---|---|---|
| 1 | **Where does the production E_top rejection live inside the two activations?** | finite topology / Bernoulli direction tilt / `K1` age plus line-weighted completion plus line-free collision / N85->N680 with N130 control / existing-data reuse | The complete ten-size covariance excludes `E_top=0` (`445.618/10`), either activation's directional response being zero, one common `E=lambda A` line and one uncorrected `N^-13/8` amplitude. `fee3328` proves finite-volume birth-age memory and identifies `DIRECT_RANK2` as the `K1=K2` diagonal mass | Fit K1-conditioned exit hazards, the two-time rank source and collision modulation in one covariance bundle; ask whether even response is age memory, completion geometry, collision, or their interaction | reuse only; production result already in hand |
| 2 | **Is P250 a flat rank-eight state or ordered/path memory?** | topology-typed charged module / endpoint-Hankel rank plus ordered context / locked old4+radius5+radius6 block / branch-only Level-S certificate | The augmented gate rejects all 16 maps. Branch `33c557b` then rejects rank<=5,6,7 in both hands; rank<=8 is first compatible (`p=.1978/.1214`). This is a statistical lower bound of eight, not flatness or a field count | Test rank-eight at the next moment/border-basis order and one ordered/path row; add a candidate-independent cross-hand projector only after single-hand flatness. Do not revive R2/R3 or five-state voting | existing locked block first; acquire only missing order information |
| 3 | **Does the P321 crossed/trivial closure pair select one physical Jordan extension?** | continuum/finite module / generic-Q detach-loop source / affine intertwiner plus first-jet Gram / widths 2-4 / exact construction | `9dda24e` types the response as `Tr(TA)-QTr(A)` and `d0fca79` gives an exact graded direct sum. Main now supplies noncrossing detach/join primitives; missing physics is Q weighting, closure/sector transport and Gram selection, not state operations | Add generic-Q weights and closure traces, recompute affine Hom, then test Gram/radical/velocity overlap for any surviving normalized line | low exact; no production |
| 4 | **Does P334's corrected reservoir saturate orbit Hall for arbitrary HNF?** | finite topology / Alexander carrier transport / base exchange plus transverse output mark / exact HNF orbit graphs / exact theory | Phase-twin compression shows no universal pure carrier channel: N6 needs YN while Smith-(2,4) N8 needs MM. Full Hall is factor-N orbit Hall, but arbitrary-HNF saturation remains open | Prove a genuinely mixed uniform channel or expose its minimum-cut witness and derive the carrier bound; keep BA and birth-age memory separate | exact work; parallel |
| 5 | **Can the exact signed P333 radical generator be transported into a physical transfer?** | confluent module / Q-dependent transfer and complete lift / Gram radical plus weighted history / small width sequence / exact then paired pilot | P333 already has a nonzero rank-one, square-zero, Gram-self-adjoint connected-history generator `K=(D1-J01)^2`. It is signed, not a positive stochastic transfer, and is not an LCFT identification | Build a positive or physical Q-dependent transfer carrying the same radical action; record the full Q lift, projector and field descriptor, then connect its selected direction to the known dimension velocity | low exact; pilot only after a physical realization exists |
| 6 | **Which surviving bounded class merits a stronger certificate?** | bounded algebraic class / frozen typed statistic / full covariance then outward interval/SOS / existing blocks / certificate reuse | Production scoring has already excluded five E_top parameterizations and the declared P250 bridge family. The 99% no-pooling E_top outer audit is intentionally weaker and leaves a common ratio | Reconstruct a stronger interval/algebraic certificate only where it changes the surviving model set; use SOS after exact zeros/minors/traces and production distance, not before analysis | low exact; never a gate |
| 7 | **Which genuinely new source can escape the completed rank-one two-observer null?** | finite topology / connectivity, birth-history or winding source / external Euler plus typed defect observer / torus or annulus / existing-data then exact pilot | N325/N425 2M determinant gives `4.279/4`, `p=.370`. The exact occupation clock explains 53%/56% of raw far-D magnitude but leaves 47%/44% after fixed-occupation and JS projection, so a density transform is no longer a candidate new source | First score K1 age, K2 completion and `DIRECT_RANK2` if retained fields permit; then exact-gate one `n12` or winding/seam column outside `sigma(n_occ)+span(J_S)` | reuse first; low exact then small pilot |
| 8 | **One dilation generator or context-dependent ordered action?** | low-dimensional dynamics / common source / shared readout / Gaussian×annulus mixed order / targeted pilot | The completed rectangle does not reject a shared generator (`p=.4002`) but all lambdas remain weakly separated; P200 lacks chronology | A maximin covector with actual `AU-UA` order or intermediate state | reuse rectangle; acquire one separating row |
| 9 | **Does the log-pair statistic survive gauge and microscopic change?** | continuum Jordan control / cutoff shear / normalized top+H4/E6 / triangular+typed square / targeted production | Triangular cross-cutoff result is positive; exact three-point and torus one-point work motivate orthogonal ratios, but the square lattice dictionary is not supplied | Gauge-invariant `CL`, one fusion ratio and one modulus ratio after the lattice map is explicit | moderate shared control |

## Team execution advice

These are coordination defaults, not task locks. A team may depart from them whenever it introduces a sharper observer, source, geometry or exact map.

**Production-first default:** once a mature map and covariance bundle exist, apply them to one archived dependency block before adding another synthetic fixture. Use exact relations first, the complete production covariance second, outward interval/algebraic reconstruction third, and SOS or new acquisition only for classes that still survive. A protocol-only artifact does not count as frontier progress, and a failed or inconclusive certificate never suspends exploration.

1. **Run a live lifecycle drift pass at the start and end of each work block.** Compare open PR head/state with the registry and compare latest `origin/main` with the navigation base. Main is pinned here at `e7d63b4`, with integrations through PR #386; P321/P334/P337/P250/P333/two-observer/P158 and open PR #385 remain separately classified frontier objects. PR #384 supplies the first exact four-terminal star reliability baseline, while #386 compresses the one-internal-vertex structural search to 4/27 connected internal-degree-at-least-three three-/four-terminal orbits. Neither is production universality, a certified candidate or a new percolation bound. PR #385 proves useful conditional Jordan-nonseparation and C4-character statements, but its R2-gauge continuation predates the augmented P250 elimination and is not the current application target. Treat drift as routine state change rather than wait for another cleanup.
2. **Publish the lifecycle tuple with every result:** `input_status`, `output_status`, immutable `snapshot_commit`, `base_or_parent`, readiness flags, acquisition stage and result stage. This keeps branch-only theory, smoke-only implementation, stacked post-reveal work and main-integrated facts distinguishable.
3. **Give each raw dependency block one covariance bundle and one statistical coordinator.** Root split, Bernstein curves, areas and node spectra from the same histograms are coordinates of one block. Teams may analyze them in parallel, but downstream synthesis should consume the shared covariance rather than add the views as independent evidence.
4. **Treat semantic adoption as a composability layer, not a scientific grade.** Main inventories 59 paths: 28 direct, 27 covered, 3 migrations and 1 utility. Draft PR #267 adds five channel-bearing scorers, so its working inventory is 64 = 28 + 27 + 8 + 1. The new migration labels request future typed adapters; they do not demote the production eliminations. Prequential history stays history, P231 and Issue43 reused stages stay shared evidence, and the v14 implicit root stays a derived response. Typed replay should preserve old numbers and never require new production.
5. **Do not revive resolved q-only, bare-line or local-shell parameterizations.** The escape is an observer outside sigma(q), such as the branch-only Euler residue, or a genuinely separated/charged/modular covector. Preserve the failed rows as controls.
6. **Attach exact-zero motif contrasts opportunistically to new same-N paired streams.** Their fixed-K mean is known, but weights must be pilot-frozen or cross-fitted and useful directional variance reduction must be demonstrated on held-out batches.
7. **Share schemas across contexts, C3 rows and tangents.** Actual `AU/UA` cells need the completed rectangle's source/readout schema; C3 work must retain complex characters and zero order; Q tangents need P333's full lift/sector-weight/normalization/Q-v-path/insertion/projector/field descriptor plus Pascal subtraction and bottom-field projection.
8. **Keep resource gates separate from science priority.** PR #342 records a fivefold end-to-end GPU threshold with output equality and deterministic regression, but no GPU measurement. CPU remains the measured default. P337's HZsCM6, P321's TgFr7R and P250 radius-five's XPk2PZ names are historical provenance only: never connect to, inspect, stop, resume or retrieve from them. The committed archives are the evidence boundary. The only permitted future Huawei environment remains `DevEnvC_TV2N0X`, and none of the current zero-sample/exact targets requires it.
9. **Use proof-carrying exclusions as portable evidence, never as exploration gates.** Freeze the bounded model class, prediction map, typed confidence set and exact/interval arithmetic before interpreting emptiness. A numerical SDP, failed solver or inconclusive relaxation is a diagnostic only; it neither proves exclusion nor blocks a new mechanism.
10. **Attach an Operator Passport by coordinates, not by a completion badge.** Record representation, finite algebra, propagation, sphere fusion, torus/modulus and microscopic control independently. Missing coordinates identify useful collaborations; they do not lower, lock or deny the result.

## Minimum sufficient parallel acquisition matrix

This is the current smallest high-information starting design, not a requirement to serialize work. All modules can run in parallel, and a cheaper design may replace one when it preserves the same decision.

| Module | Minimum starting cells | Same-stream readouts | Primary decision |
|---|---|---|---|
| projective activation/twist flux | identical `tau1,tau2,kind,ell_x,ell_y,count` archives at N85/N170/N340/N680, plus recovered N130 control | fit K1-conditioned exit hazards and the two-time rank source together with birth/line-weighted completion; retain `DIRECT_RANK2=1[K1=K2]` as correlated collision mass | localize the E_top/overshoot mechanism into observed age memory, completion geometry or collision before any fifth-generation acquisition; enriched completion path remains `not_scoreable` |
| activation/external bridge | existing ten K1/K2 archives, source-orthogonal Euler result, completed occupation-clock residual and rank-one-null matrix | K1-age/K2-completion/collision conditioning plus one exact-gated cross-boundary or winding source outside `sigma(n_occ)+span(J_S)` | localize the 44%--47% survivor and add a physically new source-rank direction |
| fixed-rank motif control | main exact N65/85/130/145/170 multiplicity gates plus PR #374 exact control/control Gram | one N130 target/control cross-covariance block with frozen or cross-fitted weights and held-out scoring | useful variance reduction versus a local-motif projection of K2 deformation; do not re-estimate the exact control Gram |
| tail-flow reuse | norm-2/norm-5 and independent P43 curves | one frozen tail direction and fresh-lineage amplitude with thermal-jet cross-moments | one shared shape fiber versus lineage-specific deformation |
| Q=4 typed seam | exact 2x2 transfer plus main-integrated PR #291 alias gate | calibrated multi-orbit constituents or typed-edge law, then `Z_id,Z_trans,N_[4],N_[2,2]` | necessary spatial angular rank reported separately from singlet 1 versus charged 0 colour character |
| P321 closure extension | shape result `ca72994`, two-closure trace `9dda24e`, graded extension `d0fca79` | smallest generic-Q detach/loop-weight action and exact affine-Hom/Gram calculation | canonical physical confluence versus two independent scalar closure amplitudes |
| Gaussian-annulus mixed order | completed same-semantics rectangle | one maximin row with `AU`, `UA`; add intermediate ranks only for chronology | shared transport versus ordered context action, distinct from static interaction |
| charged complex geometry | augmented all-map rejection plus branch-only radius-six Level-S lower bound eight in both hands | rank-eight next-order flatness/border basis plus one ordered/path-enriched covector; retain old4/radius5/radius6 covariance as one block | finite flat rank-eight state versus context memory, then cross-hand projector |
| proof-carrying small class | production-eliminated E_top/P250 classes plus any bounded survivor | exact relations, complete-covariance distance, then outward interval/SOS reconstruction only when it changes the survivor set | a portable exclusion or surviving witness; inconclusive numerical relaxations remain neutral |

## Production-first elimination lane and Operator Passport

The production-first lane asks whether a **bounded declared class** intersects a frozen typed confidence set, but does not wait for the strongest possible certificate before using archived data. Start with exact semantic zeros, Hankel minors, trace identities and small module relations; then use the complete dependency covariance. Escalate only surviving classes to outward interval arithmetic or SOS. The E_top full-covariance result and conservative no-pooling outer audit make certificate strength visible; P250 supplies a second production-facing elimination; the Euler occupancy-clock decomposition supplies a third and leaves a sharply typed residual. Freeze gauge coverage, support bounds and singular-covariance handling before stronger reconstruction. A solver failure or numerical SDP relaxation is `inconclusive`, not evidence against the class.

Use the six **Operator Passport coordinates** as a separable research map:

1. `representation` -- state space, sectors and gauge quotient;
2. `finite_algebra` -- generators, relations, radical and exact identities;
3. `propagation` -- transfer/action, positivity or signed status and dependency semantics;
4. `sphere_fusion` -- local fields, junctions and fusion/readout map;
5. `torus_modulus` -- deck/seam characters, geometry and modular response;
6. `microscopic_control` -- exact construction, finite-size controls and acquisition provenance.

No coordinate is a permission gate, and “passport complete” is not a lifecycle status. Teams may fill different coordinates in parallel and should expose tensions rather than collapse them into one operator label.

## Target 1 contract: activation shape and contact escape

The activation state is now exact and the first two proposed field carriers have completed their decision tests:

- `main_integrated` PRs #282/#283 identify `K1/K2` with the two essential-H1 births and reconstruct `P0/P1/P2` plus the rank-one lifetime;
- `open_pr` #267 gives ten-size root, full-curve, node and translation/zero-area-deformation views, and its P205 reuse selects `H4/H4` for both activations;
- exact q-only contact closure and the P275 Cartan decomposition show that more nonlinear rank projectors cannot create a field matrix element;
- `branch_only` `analysis/marked-birth-stream-20260829@936cff2` rejects the preregistered primitive-line mean-`J_D4` q2 phase on both one-step edges;
- `branch_only` `research/p275-q2-local-d4-uv-annihilator-20260829@eea85c4` rejects the declared radius-4-minus-radius-2 local landing selector at held-out N130.
- stacked `branch_only` P334 now runs `a9da7cd -> 551f890 -> c8405b5 -> 7d16c88`: BA+TM imply ULC, a one-common-site injection proves the local TM case, every proper Hall cut collapses to aggregate TM, and the remaining margin is the exact four-face inequality `4DF <= M^2+4Y(T-D)`. Only aggregate TM plus BA remain; another proper-cut or generic path enumeration would repeat a resolved reduction.
- branch-only P337 now includes the N170 angle flip `0db21b7`, N340 child `e819f5e`, three-generation forecast `4024a7c`, frozen N680 protocol `ba4ca6f`, preregistered heldout reveal `02080a4`, four-generation covariance fit `7263fbc` and theory-fixed adversary `efd6d31`. N680 gives `A_H=-.00216756+/-.00055693`: recurrence is closest, scale-neutral is closed, fixed-single is tense and free-single remains unresolved. Fixed identity dressing and same-base rank-three Jordan also pass, so no second eigenvalue or unique correction mechanism is identified. N85/N170/N340/N680 retain the same component schema; the recovered N130 archive `634040d` is an independent cross-lineage control. Enriched completion/path remains `not_scoreable`.

The endpoint identities remain

```text
K1 = K_minus,
K2 = K_plus,
F1(p) = E[H_K1(p)],
F2(p) = E[H_K2(p)],
M(p)  = -1 + F1(p) + F2(p),
C = (K1+K2)/(2(N+1)),
W = (K2-K1)/(N+1).
```

The archived baseline remains:

```text
scoreable sizes:        10/10
larger point estimate: K1 at 10/10
magnitude class:        K1-dominant 9, shared 1, K2-dominant 0
K2 reinforcing:         N=65,85,130,145,170,185,290
K2 cancelling:          N=265,325,425 (each negative term individually |z|<2)
```

The integrated K2 area is positive at all ten sizes. N265/N325/N425 are local lobes with stable nearby nodes, not reversals. The P205 component score is

```text
joint component model: H4/H4
chi-square / df:       2.585155 / 4, p=.629455
A1, A2:                .508581, .295266
K2 signed H4 share:    36.732%
interaction:           reinforcing at N=25,50,125
```

and the existing-data transport/shape split is

```text
D_i(p) = T_i(p) + R_i(p),
T_i(p) = A_i fbar_i(p),
integral T_i = A_i,
integral R_i = 0.
```

At `p_bar`, K2 translation is positive and deformation negative at all ten sizes; the N265/N325/N425 deformation-energy shares are about `.833/.894/.870`. The full 180x180 dependency-group covariance remains one coordinate on the old archives, not new primary evidence.

The narrow line/local candidates are now resolved:

```text
mean J_D4 q2 one-step phase target: rejected twice,
local (R4-R2) landing-D4 plus trained thermal shell: rejected,
connected f(q) times a root-independent gate: exact contact control.
```

The live escape is the branch-only bulk Euler residue

```text
O_ext = C_black^NN-C_white^matching-q = V-E_NN+F0.
```

Exact equal-q/unequal-`O_ext` witnesses put it outside `sigma(q)`. The completed N325/N425 2M reveal finds `O_far J_D4` amplitudes `31.007/32.673`, versus radius-two near amplitudes `.229/.108`; the far two-dimensional chi-squares are about `639684/590521`. The response is therefore genuinely bulk and not a q-contact or root-neighbour artifact. However the preregistered D/S transfer-phase contrast is `-.01293+/-.03120` radians (`z=-.414`), and JD/JS is nearly real negative at both sizes. This establishes a bulk Euler-to-rank-birth response, not Q4 epsilon or an exponent.

Main-integrated PR #293 supplies a second cheap coordinate. For equal-multiplicity same-N orientations, NN-edge, diagonal-pair, face and right-angle motif differences have exact zero mean at every fixed rank K. They may enter a paired Newman-Ziff curve without empirical recentering. They do not yet have a useful target covariance or variance-reduction claim, and the exact gate currently covers N65/85/130/145/170 only.

The immediate decision sequence is:

| Step | Output | Why it changes the mechanism space |
|---|---|---|
| completed exact multi-orbit discriminator | N13 has axis/diagonal line orbits with exact birth/exit census; signed shares `75.574/24.426` reinforce at `p_ref` | proves the finite H4 derivative is not confined to one orbit and removes this acquisition from the queue |
| completed N65 F3 boundary reuse | PR #267 splits signed character birth and rank-one exit with full covariance; H reinforces at crossing/reference and nearly cancels at `.65` | selects raw completing winding/lift plus transporter and ambiguity metadata as the missing fresh fields before any stabilizer-C3 character |
| completed N680 heldout | `02080a4` scores 120M/shape after freeze `ba4ca6f`: two-mode residual `-.586` measurement SE, free-single `+1.563`, fixed-single `-2.048`, scale-neutral `+12.110`; actual variance is `1.99x` projection | favors overshoot/return and closes no decay, but leaves the first two mechanisms unresolved rather than licensing a fifth generation |
| completed four-generation fit | `7263fbc` gives recurrence `.077/1`, `p=.781`; free-single `1.979/2`, `p=.372`; descriptive AIC differs by only `.098`; `lambda1=.27068+/-.12801` is marginal | moves the question from total-amplitude recurrence to which archived component carries the correction |
| highest zero-production output | N85/N170/N340/N680 all store `tau1,tau2,kind,ell_x,ell_y,count`; `fee3328` proves K1 age matters and `DIRECT_RANK2=1[K1=K2]` | fit K1-conditioned exit hazard, two-time rank source, line-weighted completion and collision modulation using full within-pair covariance; do not pool generations as replicas |
| cross-lineage control | N130 stores `k1,k2,direct_0_to_2,ell_u,ell_v` in only 20k/20 batches and maps onto the N170 common subset | use N130/N170 to challenge lineage transport, not to raise recurrence precision; publish missing enriched path as `not_scoreable` |
| frozen, currently low-efficiency N1360 | recurrence/free-single forecasts differ by only about `.000382` against prediction errors near `.00030/.00033`; exact child geometry is saved | no production is authorized or running; redesign only after component reuse, with a frozen separation/power contract |
| existing-data | push the cover/size tail vector and the directional K2 deformation through one Bernstein-to-standardized-quantile Jacobian | tests one lifetime/shape fiber versus separate thermal and H4 directions |
| small paired pilot | N130 target/control cross-covariance for K1/K2 indicators against the four exact-zero motif contrasts, using the exact main control Gram and cross-fitted weights | tests held-out variance gain and local-motif projection without changing the expected curve |
| completed stored-data source plane | `J_Dperp` retains 99.8792%/99.9144% of far-D magnitude; transfer `1.054088+/-.001956` at phase `1.373908+/-.002803` | direct JS projection is not the explanation; no source-energy fraction or Q4 identity follows |
| completed occupation-clock decision | exact `mu_ext(n_occ)` plus the frozen local conditional mean accounts for 53.14%/56.16% of raw far-D magnitude; fixed-`n_occ` plus JS leaves 46.64%/43.65%, joint `chi2=390067.1/4` | pure density clock plus direct JS is closed in this metric; the survivor is not yet a second source and remains in the same dependency group |
| completed two-observer decision | `26cecb4` scores the frozen N325/N425 2M blocks: `chi2_4=4.279`, `p=.370` | current rank-one lane survives; repeating the basis has low information |
| next source construction | first test K1 age/K2 completion/`DIRECT_RANK2` where scoreable, then exact-gate `n12` or seam/winding as a new column/readout | only a mechanism-independent direction outside `sigma(n_occ)+span(J_S)` can change coupling rank |

Repeats of `qJ`, `q^2J`, the rejected one-step mean-line phase, the same R4-R2 selector, a larger-N copy of the same Euler scalar row or an N60-only C3 increment currently have low mechanism gain. A new parameterization remains welcome when it introduces a genuinely new observer, separation or exact map.

The reproducible Draft result set includes [`results/two-activation-h4/latest.md`](../results/two-activation-h4/latest.md), [`results/activation-curve-nodes/latest.md`](../results/activation-curve-nodes/latest.md), [`results/activation-transport-shape/latest.md`](../results/activation-transport-shape/latest.md), [`results/two-activation-prism/latest.md`](../results/two-activation-prism/latest.md), [`results/f3-activation-flux/latest.md`](../results/f3-activation-flux/latest.md) and [`results/euler-occupancy-clock/latest.md`](../results/euler-occupancy-clock/latest.md). Their source lifecycles differ; F3 views remain one N65 dependency block and all Euler decompositions remain one N325/N425 block.

## Target 2 contract: P321 modulus shape plus exact Q=4 seam

The two subrows answer different questions and should travel together without being pooled. P321 asks whether one transparent modulus function explains the ordinary root shift. The seam asks which typed module/map carries the response and whether its spatial kernel is identifiable.

The N144 20k branch established a non-diagonal five-shape root covariance. Direct child `8206087` then froze fresh N=144,576,1296 x five shapes x 100k, the exact conversion `C_width=C_N/rho^2`, fixed `N^-2+N^-3` corrections and complete primary-score covariance. Branch `ca72994` has now scored that contract once: the scale gate is `2.00023/4`, `p=.7357`, and the conditional E4 residual is `4.02746/3`, `p=.2585`. The frozen one-function target included

```text
C_width(16/9)/C_width(1) = 0.689248108004...
```

The deep independent pool at `533ea6f` confirms the same boundary: fixed `N^-2+N^-3` and conditional E4 remain compatible, while no declared geometry resolves a nonzero correction ratio at 99%. Exact follow-up work changes the next target. `c2a5e2d` leaves `F_t(tau)` unknown; `9dda24e` identifies crossed closure minus trivial closure; and `d0fca79` realizes an exact graded direct sum while proving that regular Q differentiation is semisimple and does not select one Jordan line. Main `2845d9c` now supplies noncrossing detach/join state operations. The next target is generic-Q weighting, closure traces, sector transport and the affine-Hom/Gram gate—not ordinary-shape replication or another codec. The historical TgFr7R provenance remains branch-only.

The branch-only preflight already verifies the tiny transfer, direct spin sum, central projectors, seam action and partition normalization:

```text
Z_identity = 33024,
Z_transposition = 21568,
unnormalized [4] character ratio = 1,
unnormalized [2,2] character ratio = 0.
```

Normalized expectations alone obscure the character: the singlet ratio is `516/337` and must be multiplied by `Z_transposition/Z_identity=337/516`. The existing `d_log(v)` insertion validates plumbing but aliases scalar and fourth-harmonic stencil components; it is not a V22 measurement.

Main-integrated PR #291 makes “orientation-resolved” precise. On one C4 orbit, including horizontal/vertical axes, `exp(-4 i theta)` is constant, so scalar and nominal spin 4 have character rank one. Under a shared, radially/type-calibrated two-component model, axis and diagonal orbits give

```text
[[1,1],[1,-1]], determinant -2,
scalar = (axis+diagonal)/2,
spin4 = (axis-diagonal)/2.
```

This is a necessary angular-rank repair, not a pure H4 certificate: raw orbit amplitudes may differ and H12-like `4 mod 8` harmonics remain aliased. Alternatively, supply an exact transformation law for typed/internal edge data. Then emit

```text
Z_identity, Z_transposition,
N_H4_[4]_identity, N_H4_[4]_transposition,
N_H4_[2,2]_identity, N_H4_[2,2]_transposition.
```

When the kernel is direction-only, save the calibrated orbit constituents as well. Report the spatial angular-rank gate and the restored colour-seam ratios `1/0` as separate decisions. No free exponent, discrete-holomorphic claim, pure-H4 identity or production-scale framework belongs in this gate.

## Target 3 contract: context rectangle

The informative comparison is not “fit another rank everywhere.” It uses the same declared source/readout basis on a rectangular subset of:

```text
contexts = {Gaussian scale/Smith/deck, annulus radius/geometry},
outputs  = {ordinary or typed matching row, shared covariance coordinates}.
```

Use a spanning source/readout basis and measure both mixed orders. After removing the declared spin rotation, one generator implies

```text
[A,U]=0,
A^p=U^q when p*t_A=q*t_U.
```

Fit on a strict subset and hold out at least one Gaussian and one annulus cell. Compare:

1. one common generator/state realization;
2. the smallest context- or morphism-enriched realization that adds an intermediate filtration/label.

The annulus design should maximize separation among shared-J2, parity-specific-J2, gap-one and rotating-plane candidates; more replicas of the same N365 row do not resolve them. P200's frozen source-fibre coefficients can challenge a new static context cell, but its raw batches do not contain chronology. The final CRT endpoint cannot be used as a path-order label. If chronological memory is claimed, retain `h0,h2,h5,h25`, dynamic lineage or both mixed path orders.

## Target 4 contract: charged complex C3 and Z5 fusion

The explicitly non-`A_top` primitive-line N112 row on the `rho` parent and three registered degree-2 children has now been scored. The exact child characters are

```text
r=0: (1,1,1),
r=1: (1,zeta,zeta^2),
r=2: (1,zeta^2,zeta).
```

Keep real and imaginary covariance together; a real-only row aliases r=1 and r=2. N112 rejects zero and every pure E4/E6/E4-squared row; r0/r1 are resolved, r2 alone is not, and E4+E6 narrowly fails (`6.0645/2`, `p=.04821`). The opposite-Pell N60 completion is unresolved, and the frozen power calculation proves that N60-only replication cannot cross the fixed N112-zero boundary. Strengthen N112 or choose a nondegenerate geometry covector.

The N325 primitive Z5 cubic stream is now implemented, produced and revealed on branch `be80f25`. Exact charge bookkeeping leaves `113` and `122` plus conjugates; their disconnected low-order pieces vanish and the closure-first null is

```text
C113_plus*C122_minus - C113_minus*C122_plus = 0.
```

The compact cubic production remains compatible with zero despite surviving closure, and the normalized-separation follow-up closes the R1-R4 local-annulus family. The root-marked projective-leg chain then advances much further: fresh N325 support retains the pair but not the d2 cubic; independent N505 data resolve d1--d5 and reject all frozen one-component shapes; T alone has a minimal rank-two recurrence, but scalar T/A tomography fails. Draft PR #267 reuses one N505 80k/160-batch dependency group and rejects a shared rank-two recurrence (`p=3.88e-6`) while shared rank three closes training (`p=.207`) and strict d5 holdout (`p=.658`). That finite-line closure is a reusable coordinate, not the current model choice.

The independent bivariate frontier supersedes immediate `T_x,T_y,R,D` production. Branch `analysis/p250-projective-leg-bivariate-state-20260830@4c1f5d8` contains an N505 80k/400-batch block with 41 displacements, 328 real coordinates and a passing signed-C4 check. Branch `analysis/p250-projective-magnetic-translation-20260830@ded78bb` rejects the tested canonical Weyl and favorable free shared-commuting models globally, but does not select noncommutation.

The model-free Hankel question is now answered at the available truncation. Branch `analysis/p250-model-free-hankel-rank-20260830@a770ac9` includes defective/Jordan commuting transfers and finds each hand's two-charge block compatible with rank five (`p=.0543` plus, `.0655` minus), while the raw all-channel rank-five null fails (`p=2.84e-5`). This does not imply a physical sixth state: the shared vertical stack asks the two one-dimensional annihilator lines to coincide in one raw column chart. The result is compatible with two five-dimensional truncated quotients, but whether any cross-hand map repairs the joint residual is unresolved. Order-two flat extension is still unscoreable because total-degree-six moments are absent, and endpoint decomposition is constructionally path-order blind.

Branch `analysis/p250-two-sector-annihilator-bridge-20260830@a46ed63` transports the two covariance-aware annihilator lines but remains nonidentifying. Its independent radius-five continuation at `11130ae` adds 1.2M preregistered replicas and 400 batches. Both hand-specific annihilators extend; identity-plus-conjugation fails and only `Alexander_R2_conjugation` survives among the five frozen maps. R2's `p=.013379` and R3's `.009686` straddle that marginal direction gate narrowly; this is one projected survivor set, not a selected gauge or a complete algebra.

The **candidate-constrained joint null** is now complete at `99d23a7`. Its identity-linear replay exactly reproduces `a770ac9`, and it retains annihilation residual magnitude discarded by the line scorer. It rejects R2 and keeps R3, whereas the radius-five direction gate keeps R2 and rejects R3. The survivor intersection is empty. Because plus/minus/charges/displacements share one delete-one family and the published radius-five score reuses the old 80k moments, these are correlated mechanism constraints rather than independent votes.

That augmented discriminator is now complete. The retrospective `40 x 6` Schur gate keeps the old 80k and fresh 1.2M influence covariances separate, exactly replays the old residual block and rejects all five primary plus eleven secondary maps. Its plus-hand `20 x 6` rank-at-most-five chart also rejects (`p=6.01e-7`).

Branch-only `33c557b` subsequently completes the frozen radius-six Level-S rank ladder: `rank<=5,6,7` reject in plus and frozen minus-R2 coordinates, while `rank<=8` is first compatible (`p=.1978/.1214`). The class is gauge-invariant endpoint-Hankel rank, but the evidence remains one old4/radius5/radius6 dependency group. It certifies a statistical lower bound of eight—not rank-eight flatness, a physical dimension, a full R2 bridge or a field. The forward discriminator is now next-order rank-eight flatness/border-basis closure versus one ordered/path-enriched covector, followed by a candidate-independent cross-hand projector if flatness survives.

The joint decision compares the C3 character/zero order with the conjugate-paired fusion closure. The existing `71/21/8` row is one charged likelihood block, not four independent votes or a spin-8 discovery.

## Target 5 contract: rank-four mixing closure and Q-tangent control

Main-integrated PR #288 gives four independent coefficients:

```text
P4_S      -> I:f0 N^-1,
P4_D      -> T:f0 N^-13/8,
P4_Sprime -> T:f1 N^-5/4,
P4_Dprime -> I:f1 N^-5/8.
```

The structural map has rank four. A two-amplitude prediction requires two external relations, `f_I1/f_I0` and `f_T1/f_T0`, derived from continuum dynamics or frozen on training geometries before a held-out score. Parity itself cannot supply them.

The comparison is maximally diagnostic when the saved vector separately exposes:

```text
measure score,
finite confluent projector contribution,
explicit field/insertion derivative,
boundary conformal-prefactor derivative where applicable,
forced Pascal Q-jet subtraction before interpreting higher derivatives.
```

Use the Target 2 seam as the typed partition/projector normalization control, the Ward `4:-6:3` row to test thermal descendant versus primary and the projector residue direction to test singlet/`[2]` confluence. For the boundary route, score the anchored cross-ratio tangent after removing the single amplitude gauge.

Do not spend this lane rederiving the continuum-family Q velocities already exact on `main`: at Q=1 the four-leg `V_(2,+/-2)` and thermal `Q4 epsilon` rows differ by `sqrt(3)/(4 pi)` in `dx/dQ`. The spin-8/spin-12 generic-loop rows in that oracle are controls, not the H8/H12 angular aliases. What remains is to connect that exact fingerprint to a lattice observable through the measure + projector + explicit-field decomposition and a nonzero typed overlap.

The branch-only P263 chain already establishes useful design facts: 200k/500k pilots do not reject the predicted shape; event-J crossfit is an exact variance-one no-op; the localized stopped-transcript estimator is exactly unbiased; and its unpaired 20k pilot did not resolve a variance advantage. A paired same-outer-transcript variance ratio can decide compute efficiency in parallel; it is not a semantic permission gate.

## Target 6 contract: gauge-free cross-microscopic control

The triangular `kappa_proxy` and a bare square-H4/triangular-E6 amplitude ratio remain gauge-fixed coordinates rather than universal numbers. The square global line `Gamma` is now a contact/projective control, so the square side must use a separated-local, charged or otherwise typed field row.

The next rows cancel the relevant gauges:

- normalized top-plus-two-spin statistic for `CL` and its shear ratio;
- two-modulus, weight-12 H4/E6 polynomial null across the same C3 children;
- optional `LL,LD,DD` Gram invariant only with its finite-cutoff shear boundary stated.

The current open-PR result already establishes a nonzero cutoff shear within its declared gauge. Repeating the same two-point production without a new normalization axis is lower information. The exact factor-four collision rule in the hypothesis board is a parallel symbolic sprint, not a premise.

## Target 7 contract: radical and improved-action exact work

Powers `q^3,q^4,...` reduce exactly to the three rows `1,q,q^2`, so they are existing coordinates rather than new source axes. Coefficient changes or ordinary bimodules on the same separable scalar algebra likewise leave the Hochschild class trivial. Branch `b024177` classifies the dual-number partition Gram module exactly: one unit pivot and `Bell(n)-1` nonzero epsilon pivots give a nondegenerate first-jet radical. Bare join/detach dynamics either miss the Jordan action or produce a deterministic transient incompatible with the Gram structure.

Branch `theory/p333-dual-number-gram-extension-20260830@6c60b0e` supplies the missing exact positive control at the algebraic level. The connected weighted-history operator

```text
K = D1 + J01 - D1*J01 - J01*D1 = (D1-J01)^2
```

is nonzero rank one, satisfies `K^2=0`, is self-adjoint for the first-jet Gram form, and has an explicit isotropic bottom plus partner. Thus “add a generator and classify its radical action” is complete, not a next target. Its current boundary matters: `K` is signed rather than a positive stochastic transfer, and the construction alone is not an LCFT identification.

The next algebraic move is to transport this connected-history nilpotent into a positive or otherwise physical Q-dependent transfer. Save the full Q lift, sector weights, normalization, Q-v path, insertion, projector and field descriptor; then test whether its selected radical direction pairs with the known dimension velocity. Typed junctions and threshold defect vectors remain useful comparison backends: shared selected support would unify two constructions, while disjoint support would cleanly separate them.

In parallel, derive an exactly critical oblique family with a negative-phase orbit before simulating it. Three common-frame coupling points around its H4 zero distinguish a linear ordinary response, a quadratic charged response and an independent charged field. This is an opportunity to test coupling tunability, not a claim that the microscopic proxy equals the renormalized coupling.

## Secondary opportunity pool

These lines remain active and may overtake the default order whenever they obtain a sharper discriminator or low-cost acquisition:

| Line | Current boundary | High-value continuation |
|---|---|---|
| Norm-4 / norm-10 | q2 rejected; the frozen generation-four target passes Jordan plus one even mode, but lambda `0`, `1/2` and `1` remain aliased | add a discriminating geometry/operator coordinate or use this Gaussian block in the common context rectangle |
| same-N norm-5 coalescence | exact amplitude-free interpolation is ready | use when its new Smith class fills the context rectangle efficiently |
| local pivotal | current N130/N170 rows nearly rank one | add a thermal-null or representation-typed row, not merely replicas |
| modular-scalar modulus | channel typing is `main_integrated` | one new typed `cross/either` shape with a parameter-free prediction |
| microscopic H4 origin | axis-only nonnegative fourth-moment proxy cannot cancel; axis/integer-diagonal cancellation is `4:1` only in the declared microscopic normalization | derive an exactly-critical family with a negative-`cos(4 theta)` oblique orbit before simulation; keep the proxy/renormalized-coupling boundary explicit |
| microscopic threshold origin | main maps the known exact-route obstructions, freezes a synthetic-only location/scale/shape contract, exact block-event semantics, O(`s^2`) evaluator and a 400-trial/cutoff-373 plan; no empirical bound or collapse | acquire fresh IID trials under the frozen plan; use held-out curves plus covariance before shape-collapse claims; keep law-preserving decorated/correlated exact constructions open |
| post-annihilator corrections | composite algebra rules out some shortcuts but not amplitudes | joint harmonic/phase sidebands rather than exponent alone |

## Recovered frontier sources

| Node | Lifecycle pointer |
|---|---|
| digital Alexander proof | `main_integrated` `2da5855`, `notes/digital-alexander-duality-proof.md` |
| essential-H1 filtration oracle | `main_integrated` PR #282 `3bb4054`, `notes/digital-alexander-filtration-oracle.md` |
| essential-birth archive reconstruction | `main_integrated` PR #283 `bedc94b`, `results/essential-birth-histogram/latest.json` |
| cached quotient frontier through index 9 | `main_integrated` PRs #284/#285/#286/#287 `84035cf`, `results/digital-alexander-quotient-frontier/latest.json` |
| cached quotient frontier through index 13 | `main_integrated` PR #330 / merge `c4a9955` / scientific commit `247c537`, `results/digital-alexander-quotient-frontier/latest.md`; finite regression/counterexample locator only |
| unrestricted rational Digital Alexander theorem | `branch_only` `theory/p269-unrestricted-degenerate-quotient-20260830@73d4960`, `notes/digital-alexander-unrestricted-degenerate-quotients.md` |
| unrestricted integral carrier saturation claim | `branch_only` `theory/p269-integral-saturation-20260830@c1a72e5`, `results/digital-alexander-integral/SCIENTIFIC_CARD.md`; no PR, pending proof review |
| P334 orbit currents, age/collision and ULC theorem | `branch_only` atlas `62edfe7`; BA+TM theorem `a9da7cd`; corrected reservoir `6a7b042`; phase-twin compression `2b5d706`; birth-age/collision review `fee3328`. Uniform mixed arbitrary-HNF saturation plus BA remain; age/collision is a separate zero-sample path-law lane |
| P337 finite-abelian tomography and natural current | `branch_only` exact bridge `770fe27`, R-odd source `539b629`, N65 `f3384f4`, N85 `8783977`, N145 `75f74f9`, crosswalk `186d72a`, N170 `0db21b7`, N340 `e819f5e`, three-generation fit `4024a7c`, N680 freeze `ba4ca6f`, heldout reveal `analysis/p337-n680-heldout-production-20260830@02080a4`, four-generation fit `analysis/p337-four-generation-recurrence-fit-20260830@7263fbc`, theory-fixed adversary `analysis/p337-theory-fixed-dressing-adversary-20260830@efd6d31`; N130 archive `634040d`. Historical HZsCM6 appears only as provenance and must never be contacted. Four-generation component reuse is scoreable; enriched path is `not_scoreable`; N1360 is frozen but unauthorized |
| homological-balance root consistency | `branch_only` `theory/p276-homological-balance-convergence-20260830@23fd682`, `notes/homological-balance-convergence.md` |
| P321 finite projector, modulus and closure extension | `branch_only` parent `bc343f4`, fresh result `ca72994`, deep pool `533ea6f`, TTbar obstruction `c2a5e2d`, two-closure trace `9dda24e`, graded extension `d0fca79`; main state operations `2845d9c`; next is generic-Q weights/closures plus Hom/Gram gate |
| fixed-rank paired motif certificate | `main_integrated` PR #293 / `6265931`, `results/paired-motif-microcanonical/latest.md` |
| descriptor registry and scorer-adoption inventory | `main_integrated` through PR #386 / `e7d63b4`: 59 total = 28 direct + 27 covered + 3 migration + 1 utility. Draft PR #267: 64 = 28 + 27 + 8 + 1 after five channel-bearing scorers; the new migrations guide future typed reuse and do not change their evidence role |
| odd-jet linear-metric primitive | `main_integrated` PR #339 / `2e4c2b7`, `analysis/odd_jet_invariant_contract.json`; exact linear-coordinate invariant and delta-method Jacobian, synthetic fixture only |
| generic-Q lift transport and first-jet radical | `branch_only` sibling heads `502719c` and `c34c838`, shared Phase-A parent `de02198`; Gram classification `b024177`; exact connected weighted-history nilpotent `theory/p333-dual-number-gram-extension-20260830@6c60b0e`, `notes/p333-dual-number-gram-extension.md`. Signed algebraic control only; physical/Q-dependent transfer remains |
| production-first elimination and Operator Passport | `open_pr` #267, `notes/production-first-model-elimination-20260830.md`; E_top full-covariance/outer-set, P250 augmented and Euler occupancy-clock results plus six separable coordinates, neither a lifecycle grade nor an exploration gate |
| Issue-27 rational-width prereveal | `main_integrated` PR #340 / `1c7f54b`, `analysis/rational_stage_b_quarantine_manifest.json`; target absent, no score |
| C4 tangent and Issue43 typed wrappers | `main_integrated` PR #341 / `3aebee0` and PR #344 / `4a77650`; exact identity transport only, no new data or score |
| GPU rental gate | `main_integrated` PR #342 / `b6c81d0`; exact 5x end-to-end threshold, output equality and determinism, no GPU measurement or science |
| P50 S-prime and P48 new-geometry typed overlays | `main_integrated` PR #345 / `40ffafd` and PR #346 / `76ebad5`; post-score semantic migrations with no new data/score/science |
| two activation | `branch_only` `theory/p28-two-activation-rank-mixture-20260829@b8004bc`, `notes/two-activation-rank-mixture.md` |
| endpoint, full-curve-node, transport/shape, E_top elimination and P205 activation splits | `open_pr` #267, `results/two-activation-h4/latest.json`, `results/activation-curve-nodes/latest.json`, `results/activation-transport-shape/latest.json`, `results/etop-production-elimination/latest.json`, `results/two-activation-prism/latest.json` |
| P205 ordinary quotient prism | `branch_only` `results/p205-quotient-character-prism-20260829@fc14817` |
| essential-birth C/W clock | `branch_only` `analysis/p276-essential-birth-clock-20260829@195a198` |
| rank-birth S/D and A-top coupling | `branch_only` `analysis/p215-rank-birth-parity-channels-20260829@dabe28e`; `analysis/p215-atop-q4-coupling-proxy-20260829@078bd61` |
| marked-birth contact closure and completed mean-JD4 chain | `branch_only` `analysis/marked-birth-stream-20260829@936cff2`, `results/server-20260829/mean-jd4-q2-chain/REPORT.md`, `notes/topology-insertion-algebra-no-go.md` |
| local D4 UV q2 rejection | `branch_only` `research/p275-q2-local-d4-uv-annihilator-20260829@eea85c4`, `results/server-20260829/P275-q2-local-d4-uv/REPORT.md` |
| P275 global-line and Cartan contact decomposition | `branch_only` `research/p275-atop-field-identity-pipeline-20260829@4d7ecd5`; `analysis/p275-cartan-contact-background-20260829@7faa317` |
| external Euler bulk bridge and source-orthogonal result | `branch_only` `analysis/external-observer-stream-20260829@48a8818`, `results/server-20260830/P267-target1-external-Euler-2m/GRAM_ORTHOGONAL_REPORT.md` |
| external Euler occupancy-clock residual | `open_pr` #267 / `c34dd98`, `results/euler-occupancy-clock/latest.json`; exact clock explains 53%/56%, 47%/44% survives fixed-occupation plus JS, same N325/N425 dependency group |
| two-observer source-rank freeze/result | `branch_only` freeze `9f2033a`, completed result `26cecb4`; N325/N425 2M joint `4.279/4`, `p=.370`, current rank-one lane survives |
| threshold tail-flow production reuse | `branch_only` `analysis/p122-production-shape-collapse-20260829@bc19bcb`, `results/threshold-shape-collapse/production-norm2-norm5.md` |
| same-semantics context rectangle | `branch_only` `analysis/p267-target3-context-rectangle-20260829@5873de8`, `results/server-20260830/P267-gaussian-annulus-context/REPORT.md` |
| primitive C3 multi-character response | `branch_only` `research/p267-rho-c3-primitive-h4-20260829@63d81fb`, `results/server-20260830/P267-rho-C3-primitive-H4-independent/REPORT.md` |
| primitive Z5 cubic and projective-leg vector state | compact result `be80f25`; local R1-R4 closed at `842400d`; fresh chain through `186393e`; `open_pr` #267 axis rank result is one N505 dependency group. Independent bivariate block `4c1f5d8`; rejected model families `ded78bb`; hand-specific Hankel rank split `analysis/p250-model-free-hankel-rank-20260830@a770ac9`; non-identified two-sector bridge `analysis/p250-two-sector-annihilator-bridge-20260830@a46ed63`. Next is a candidate-constrained joint null on the same 400 batches, not another Hankel construction or immediate word production |
| global selection | `branch_only` `analysis/p257-global-singlet-selection-20260829@9320649` |
| small-width selection | `branch_only` `analysis/p120-operator-spectroscopy-20260829@9cf2373` |
| Q=4 seam numerator preflight | `branch_only` `analysis/p257-p258-q4-seam-preflight-20260829@192e794`, `results/q4-seam-numerator-preflight/SCIENTIFIC_CARD.md` |
| spin-4 direction-character alias gate | `main_integrated` PR #291 / merge `83e98fc`, `results/discrete-holomorphic-spin4-alias/latest.md` |
| ordinary H4/H12 opposite-alias acquisition design | `main_integrated` PR #325 / `c1a353a`, `analysis/h4_h12_orthogonal_design_manifest.json`; design only, no target data |
| ordinary H4/H12 opposite-alias result | `branch_only` `analysis/issue55-h4-h12-acquisition-20260830@86e77db`, freeze parent `0b8bb63`, `results/server-20260830/P55-h4-h12-orthogonal-production/REPORT.md`; 600M/design leaves H4 and zero compatible and H12 unresolved |
| Gaussian norm2/norm5 commuting-square contract | `main_integrated` PR #328 / `033ebe1`; source-only power gate `branch_only` `analysis/p158-gaussian-root-character-power-20260830@2cfde0b`, projected 27.21B/55.18B per lineage, no target roots |
| double-projector staircase | `branch_only` `theory/p275-double-projector-selection-20260829@ddf41aa` |
| norm-4 reveal / generation-four score | `open_pr` #273 `8b26a30`; #277 `3e855ce`, `results/server-20260829/P154-norm4-generation4-pilot/REPORT.md` |
| Q=1 spin-sector velocity oracle | `main_integrated` PR #264 `540a115`, `results/q-velocity-spin4/latest.md` |
| four-channel operator-mixing rank-four boundary | `main_integrated` PR #288 / `a38f76b`, `results/operator-mixing-parity/latest.md` |
| microscopic anisotropy stencil gate | `main_integrated` PR #272 `a0dcc8f`, `results/anisotropy-stencil-gate/latest.md` |
| norm-5 plus/minus shell and matched-delta controls | `open_pr` #247 `9d35fa6`, `results/server-20260829/P225-norm5-multiradius/REPORT.md` |
| annulus held-out recurrence | `branch_only` `analysis/p253-annulus-recurrence-20260829@8e91c90` |
| annulus sector spectroscopy | `branch_only` `analysis/p253-spectrum-sector-test-20260829@3123b73`, `results/p253-annulus-spectrum/SCIENTIFIC_CARD.md` |
| ordered filtration | `branch_only` `analysis/p200-path-ordered-filtration-20260829@fb82de4` |
| static context map / mixed-clock gate | `branch_only` `analysis/p200-context-morphism-map-20260829@7fc89df`; `analysis/p255-two-clock-generator-20260829@356fd884` |
| triangular log pair | `open_pr` #246 `7f9dcd8`; normalization audit `branch_only` `ceb7c6e` |
| charged norm-5 | `branch_only` `analysis/norm5-chiral-hecke-phase-20260829@cc1d43c` |
| Q/projector/Ward/Q-jet controls | `branch_only` `1055e22`, `d006f9c`, `fa73f5d`, `2a15e63`, `0709011` |
| P263 stopped-score chain | `branch_only` `analysis/p263-local-stopped-qscore-20260829@8187970` |
| C3 character and invariants | `branch_only` `57b59be`, `a6120aa` |
| relative-source closure/rigidity | `branch_only` `94dd7f9`, `094ee36` |
| typed-site surface-polynomial classification | `branch_only` `theory/p144-surface-polynomial-classification-20260830@5d8343d`, `notes/p144-surface-polynomial-classification.md` |
| terminal-partition canonicalizer | `main_integrated` PR #329 / merge `7da6013`, `analysis/terminal_partition_canonical_manifest.json`; encoding primitive only |
| exact-rational comparison certificate | `main_integrated` PR #331 / merge `c05efb9`, `analysis/synthetic_gadget_comparison_certificate.json`; synthetic protocol only, no real gadget or bound |
| four-terminal star reliability baseline | `main_integrated` PR #384 / `0dc4430`, `results/terminal-reliability/star4/latest.json`; exact 16-configuration partition polynomials for one independent-bond star, not a search, domination result or bound |
| bounded terminal-gadget census | `main_integrated` PR #386 / `e7d63b4`, `analysis/bounded_gadget_census.json`; exhaustive one-internal-vertex structural filter leaves 4/27 connected internal-degree-at-least-three three-/four-terminal orbits, without probability, planarity/tiling, ranking or bound |
| block-event confidence chain | `main_integrated` PRs #278-#281 through `d433f53`, `results/pc-confidence-preregistration/latest.md` |
| threshold shape-collapse contract | `main_integrated` PR #289 `8e5a236`, `results/threshold-shape-collapse/latest.md` (synthetic fixtures only) |
| exact site-threshold mechanism map | `main_integrated` PR #290 `9d21f36`, `notes/solvable-site-maps-and-square-obstruction.md` |
| bounded integer-relation search protocol | `main_integrated` PR #319 / `fc68d8a`, `analysis/pslq_search_contract.json`; no search result |

See `docs/STATUS.md` for full branch names, artifact paths and claim boundaries.

## Evidence-aware re-ranking cues

- A post-reveal coordinate on an old stream remains correlated reuse rather than a new primary block.
- The K2 pointwise sign split is now positive same-area translation plus negative zero-area deformation; explanations should target the deformation rather than another root-only shift.
- Any root-independent insertion connected only to `f(q)` lies in the exact contact closure; fixed-q, separated-local, charged or modular observers outrank nonlinear q-only variants.
- The exact occupation clock already removes the global-density nuisance from external Euler; any continuation that is only another function of `n_occ` or a reversible JS rotation has low mechanism gain. The 44%--47% survivor needs birth-history, connectivity or winding semantics before it can be named.
- The finite defect is a typed-site rank-image quotient, not a direct named product-local edge-subset specialization under the declared contract; exact theory should target its recurrence/coefficients and connectivity radical rather than repeat substitution searches.
- PR #386 has already bounded the one-internal-vertex terminal-gadget space. Choose and certify one of the 27 four-terminal structural survivors before expanding the census.
- Extra N365 samples have lower information while uncertainty is dominated by the inherited recurrence coefficients; a new context or source can overtake them.
- N85/N170/N340/N680 form a scoreable four-generation component archive. Fit K1-conditioned age, first birth and line-weighted completion jointly; `DIRECT_RANK2=1[K1=K2]` is collision mass without a projective line rather than a third H4 component. N130/N170 remain a cross-lineage control, and enriched path stays `not_scoreable` until winding/lift, basis, transporter and ambiguity are recorded.
- N680 favors recurrence and closes scale-neutral, but free-single, fixed identity dressing and same-base Jordan remain viable; `lambda1` is marginal. N1360 is unauthorized and source-covariance limited across the live adversaries; production is not the default next step.
- P250's augmented operator rejects all declared bridges, and branch-only `33c557b` rejects endpoint-Hankel rank<=5,6,7 in both hands. Rank<=8 is first compatible only. Stop R2/R3 and five-state voting; test rank-eight next-order flatness versus ordered/path memory, with no exact dimension or field conclusion.
- P334 proper Hall cuts and naive configuration switches are resolved reductions. Its live theorem is uniform saturation of the corrected base-exchange plus transverse-mark orbit graph and separate BA. P333 still requires physical/Q-dependent transport of its exact signed nilpotent.
- Production distance may eliminate a declared class before an exact/interval certificate is available. Stronger reconstruction is required only for the stronger proof claim; numerical failure stays `inconclusive`, and no certificate controls whether another lane may proceed.
- Operator Passport coordinates expose which representation, algebra, propagation, fusion, modulus and microscopic links exist; they are neither a checklist nor a completion or priority gate.
- Path order lives in an intermediate filtration or lineage, while a final unmarked CRT join is order-blind.
- Ordinary and charged norm-5 rows retain separate operator amplitudes and dependency semantics.
- Gauge-fixed agreement becomes a universality test only after a normalization-free statistic is available.
- A two-amplitude mixing model needs externally fixed `f_I1/f_I0` and `f_T1/f_T0`; parity supplies channel powers but not those ratios.
- The roadmap never forbids a mathematically distinct attempt; new information axes can re-rank any lane immediately.

The target is a faster sequence of scientific separations, not a smaller number of ideas.
