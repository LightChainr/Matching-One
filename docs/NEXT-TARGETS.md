# 下一步分析：增加能区分机制的信息

**2026-08-31 17:20复核版。** [STATUS](STATUS.md)保存已有答案；[最新核验](../notes/progress-and-compute-20260831.md)记录刚完成的分析、五机状态及本地/GitHub同步差异。以下已删除首次U_dot、两阶段、首1M端点、首次九层和首次SS分解等过时待办。

| 主线 | 下一份实质交付 | 近期算力用途 |
|---|---|---|
| **P0 #154** | rank-1内部primitive winding的簇源响应，对照共同E加时钟预测 | 先一个原取向对、一个新增标记，不重复六N扩样 |
| **P0 #334** | 已存接触坐标对mixed主贡献和SS余量的机制分解 | 先复用40批字段，不新增全量后缀 |
| **P2 #398** | 本轮固定η干预已完成；保留cross传播与T4近似结果 | 已结束占机，不默认扫描或扩宽 |

其余P1是储备和接口池，不同时生产。五台16核32GB机器已恢复连接，空闲机器按用户要求恢复Ready，需要时开机；实例状态与是否已有科学结果分别报告。

## 1. P154：进入rank-1内部，检验共同E候选的实际预测

**已经完成。** 六N全链源导数、100k两阶段估计，以及N260/N340各1M旧排列标记和评分均已交付到本地固定提交。两阶段未形成稳定降噪，端点误差主要在源标记侧。随根移动的rank-1组成响应显著非零，纯共同温度换元已不足；原U源q2/Jordan仍未分辨，共同E加时钟尚未被排除。最新数值与尚待推送的提交见[核验](../notes/progress-and-compute-20260831.md)。

**一个真正新增的读出。** 在rank-1构形上定义物理嵌入下primitive winding的四重character `O4=cos(4θ)`，考察 `ell_g=E[O4|rank=1]` 的根随动簇源响应。固定一个原取向对、明确primitive线与物理角度的映射，保留first/second配对；HNF端点必须先落实其嵌入，不能直接把整数坐标角当物理角。

对已命名的共同E加时钟候选，以既有rank-1响应确定 `d_N=r_s/r_E` 后，预测

`ell_dot_s,g = d_N * p0_dot[E] * partial_p ell_g`。

E在rank-1内部为常数，但pooled根移动仍产生thermal transport；这一项不能漏掉。测量实际簇源响应与该预测的差，保留共同协方差。这新增了q/E三态之外的方向信息；不把有限非零自动命名成局部能量场或连续生成元。

**缺的具体字段。** 现有逐K表只有q/E/s/qs/Es，不能恢复O4及其与s的混合矩。对旧排列补同流rank-1方向标记即可，不需重算已完成q/E生产。先交付一个尺寸的可辨别读数，再决定其他尺寸是否需要补标记。

**已经算过的等价变形不再立项。** 用P1定E-loading后预测U，其残差就是已算ΔE除以一个响应幅度；重写公式不增加证据。原源刚性关系也不会自动对新源成立。共同微观fugacity比较使用bulk `s=CB+CW`，不能混入按N归一化的密度源单位。

## 2. P334：解释mixed主贡献，并定位safe余量

**已经完成。** 完整A/E九层、共享next-label Gamma、SS/mixed/BB与raw-orientation/CRN交叉分解都已完成。canonical Gamma约78%来自mixed，SS约7%–8%。SS指两候选位置在两取向都不立即改变rank，它估计的是 `E[p_safe² Cov(m1,m2|safe,Z)]`；mixed不是纯即时birth因果贡献。

**下一份交付直接使用959a7fa2。** 两尺寸各320k个已采next-label的接触坐标已经补齐，覆盖40个原批。将这些字段连接到原quartet响应，回答哪类接触及绕行地址承担mixed主贡献，哪些rank保持不变的结构仍保留SS协同。按实际接触机制选择少数可解释分层，保留全体原分母、20批配对误差和各层精确重构，先不重采全部后缀。

标签样本中某种direct-jump架构计数为零，不能替代理论不可能性。若既有字段已吸收当前机制差异，就交付这个结论；只有明确缺失的空间信息才值得追加有针对性的标记或干预。动工前查看该分支是否已有并发新结果。

## 3. P398：固定干预已完成，本轮结题

[本次单机结果](../experiments/p398-rate-intervention-20260831/README.md)已实际运行η=0,±1/4，完整1430态在联合186维扇区传播，并重算各η平稳分布。旧U在正lag出现cross传播；T4将14维字典扩至原16维后，最大C0归一化误差改善4.36倍，弱minus的lag4相对误差仍约−12.64%。

这给出固定几何字典在一次新速率下的条件压缩结果：借用各η的完整数值平稳分布，不声称仅由η=0矩阵盲预测。±1/4有限差商不等于η=0精确导数，零频积分本轮未算；这些余项不自动成为下一张生产任务。

#398降为P2并保留一般数学问题。本轮不再占机；只有明确的square-site源/读出映射，或具体理论问题确实需要精确切线时再调用。不默认扩width10、加mark或扫速率。

## 执行与交接

每条主线先交付一个能改变判断的结果。负结果、反例和有明确精度的零读数都可以完成一轮；不再默认续接新的验证框架或规模扫描。

更新回同一个主Issue和实际成果分支，注明输入commit、结果位置以及仍缺的一个读数。本地已提交与GitHub已推送分开报告；不代推其他任务未交接的分支。使用新目录和不重叠counter，保留原数据与配对结构。

[Issue清理记录](REPOSITORY-TRIAGE-20260831.md)继续有效：两个P0不变，其余任务按需调用。文档仍在Draft #509，**不合并、不删除研究分支或数据**。
