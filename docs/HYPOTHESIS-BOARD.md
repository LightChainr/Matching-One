# Hypothesis and Decision Board

**Frontier date:** 2026-08-31

This board turns the repository into a mechanism-discovery surface. It does not grant permission, lock tasks, close alternatives or promote branch results to `main`. Priority means expected information gained per unit of shared acquisition.

## What changed the decision surface

The newest results make the next outputs more specific:

1. **P267/E_top: compare mechanisms on existing production now.** The N50 factorial resolves interaction (`chi2=236.756/4`, `p=4.63e-50`), but `E_top` alone remains unresolved. Use its covariance and norm-4/norm-5 archives to separate response-direction change from amplitude rescaling, publish actual model survivors and numerical predictions, and identify the most discriminating third tau/map. Another factorial or synthetic fixture is not the next output.
2. **P334: replay the saved safe-insertion graph.** Branch-only `6147e22` supplies real scalar-state nonclosure, recoverable successor H2 and counters that reconstruct microstates. Draft `2b0844a` (source `0662fd5`) finds within-checkpoint directional contrasts `(3.61607 +/- 3.25069)e-6` at N325 and `(-1.53489 +/- 2.08635)e-6` at N425. Nonclosure is established for the declared scalar tuple; the direction carrier is unresolved. Explain the degree distribution/2-star structure from deterministic checkpoint replay, not another `b2` acquisition.
3. **P439: seek a measurable transported response.** The same-stream source is in open PR #451; Draft `8498d62` completes the [direct/plateau split](../results/p439-direct-plateau-transport/REPORT.md). Both terms are weak (`joint zero chi2=4.69005/8`, `p=.79013`), rather than resolved large cancelling terms. Target a resolvable loading of `M` or a concrete line-relative landing readout; more `K_A`-only sizes do not answer that question.
4. **P398: use the module, not another projected closure.** Branch-only `5389200` couples rooted7 to the existing landing doublet, uniquely closing the declared nine-mark/23-dimensional accumulator intersection. `afc619c` gives `X2=0`, `J2=0`, and shows higher fixed-radical projected-Gram passes are automatic. The next scientific object is a positive-measure width-four rooted/landing 2x2 connected-response matrix; physical transfer and continuum identification remain open.
5. **P250: preserve the actual state result.** Draft `eb29446` finds 30.97% held-out predictive gain from pre-outcome typed state beyond the declared residue baseline. Complete terminal spectrum is absent and both enriched residuals retain `k=36`. A future narrow shared block should address those two coordinates, not repeat the archive.
6. **Phase E: choose a genuinely distinct microscopic singlet.** The integrated score aliases E_top, and the completed radius-1 mixed row is not selected. A new local definition should provide an actual predicted contrast to A/E/C rather than another name for the same row.
7. **rho-C3 and ordered contexts remain distinct opportunities.** Use a nondegenerate complex character row and, separately, actual `AU/UA` or intermediate filtration; the static rectangle does not identify chronology.
8. **W5 has enough finite algebra to attempt a concrete comparison.** Main `b47d99e`, `eca7d4e` and `8f61cc7` supply disk embeddings, gluing and exact Mobius event inversion. The deterministic disk Kreweras complement is not W5 self-duality. Produce one ordered periodic comparison using these inputs.

Current main is `05af691`. New analyses and exact constructions remain explicitly `open_pr` or `branch_only`; completed tools are inputs rather than new physical evidence. This update starts no new Monte Carlo acquisition.

## Scientific outputs first; methods support them

Attention is **1 P267/E_top existing-data survivors, 2 P334 checkpoint graph replay, 3 P439 M/line-relative response, 4 P398 physical 2x2 response, 5 P250 full-spectrum/common-k36, 6 distinct Phase-E singlet, 7 rho-C3, 8 AU-UA, 9 W5 periodic comparison**. The first #370 real production adapter at `f5779b9` is complete and supports the first analysis. Exact identities, covariance and optional interval/SOS strengthening serve a named scientific question; synthetic/compiler/validator work is not an independent prerequisite.

The Operator Passport is a dashboard with six separable coordinates: representation/projector, finite algebra, propagation, sphere fusion, torus/modulus and microscopic control. Any coordinate may redirect the next experiment; “passport complete” is not a status, gate or admission checklist.

The literature boundary remains narrow. Normalization-free loop-model three-point/OPE ratios and torus one-point modulus ratios are candidate discriminators for Matching One, not proven fingerprints for its current lattice observable or arbitrary Jordan/defect fields. Exact finite-lattice singular-vector relations currently provide an ADE RSOS/root-of-unity TL template; transport to percolation/Q4, and convergence to the intended Virasoro relation, remain new theorem work.

## Three-object synthesis

```text
microcanonical nuisance layer
    exact-zero motif contrasts at fixed K
                 |
                 v
rank-one lifetime and shape fiber
    K1/K2, C/W, tail mode, I01/I12, ell, landing marks
                 |
                 | acted on by geometry/context
                 v
low-dimensional context action
    Gaussian A, annulus U, mixed orders AU and UA
                 |
                 | scalar q quotient forgets
                 v
connectivity / defect radical and external observers
    Euler residue, partial partitions, junctions, singular projectors, charged seams
```

This is a conjectural organizing skeleton, not a theorem. It is valuable because each piece has a small, decisive observation that can break it.

## Active hypotheses

| ID | Bold mechanism | Current support and boundary | Smallest decisive observation | Sharp falsifier | Attention |
|---|---|---|---|---|---:|
| H-NORM4-ENERGY-PARTNER | A microscopic local singlet replaces part of the measured clock C in the surviving A/E plane | Six-size Phase D is nonzero; integrated energy duplicates E_top; the completed radius-1 mixed row has unresolved sign-changing `J_bulk` and improves A/E/C by only `1.981<4` | Give a distinct microscopic singlet, scale or field interface with a predicted contrast to A/E/C | Every genuinely distinct local definition remains inside the A/E/C span, or no stable transport exists | 6 |
| H-BIRTH-EXIT-POLARIZATION | The directional completion current contains a line-addressed response not captured by current endpoint transport | `E_top'=J12-J01`; risk/hazard split and ambient-line pilot are complete. P439's direct/plateau terms are both weak, joint zero `4.69005/8`, `p=.79013`; this is not large-term cancellation or transport closure | Resolve the loading of `M` in shared transported coordinates or calculate a concrete landing response relative to the lifted line | The specified new loading vanishes or the frozen line-relative response does not transport | 3 |
| H-LIFETIME-POLARIZATION | The local negative `K2` lobes and the replicated upper-tail deformation are two projections of a low-dimensional lifetime/shape fiber, not reversal of the second birth mechanism | PR #267 has positive integrated K2 area at 10/10 sizes; the branch-only threshold-shape result finds a replicated near-rank-one tail vector, but the two directions have not been compared in one covariance metric | Push both perturbations through the same Bernstein-to-standardized-quantile Jacobian, then condition K2 on `W`, `ell`, landing and exact-zero motif contrasts | The whitened K2 and tail directions are independent and none of the typed birth coordinates predicts the K2 node | parallel |
| H-EXTERNAL-EULER-BRIDGE | A physically independent current-geometry/connectivity source can split the bulk Euler response even though the present matrix is projectively rank one | Exact K-centering removes all degree-one product-noise projections; 47%/44% still survives the production fixed-occupation+JS subtraction. P267 marked birth is Palm-like, only fixed-K `O_ext` supports Johnson, and `O_far`/high-pass are not scoreable | Use the scoreable fixed-K `O_ext` coordinate, then exact-gate one new current-geometry, `n12` or winding/seam source without imputing the Palm/`O_far` rows | The residual vanishes under the new source split, or every independently defined source projects onto the same lane after held-out exact gates | parallel |
| H-TAIL-FLOW-FIBER | Cover multiplication and ordinary size transfer advance one universal standardized tail-deformation coordinate after median/IQR removal | Four cover comparisons and an independent N185-to-N265 block have cosines `.9915` to `.9996`; amplitudes do not yet follow one exponent | Freeze the tail vector and predict one fresh lineage; jointly regress its amplitude on the existing thermal-jet and activation-shape coordinates | A fresh lineage rotates outside the frozen covariance cone, or amplitude requires lineage-specific directions | parallel |
| H-ETOP-TAU-TOPOLOGY | Geometry changes the finite A/E/C/W response direction, not just its amplitude | The completed N50 factorial resolves interaction at `236.756/4`, `p=4.63e-50`, but interaction alone does not distinguish direction rotation from amplitude rescaling; `E_top` alone remains unresolved | Compare live models on existing factorial/norm-4/norm-5 covariance, publish survivor predictions and the most separating third tau/map | An amplitude-only model explains all matched rows, or the direction-changing model fails a genuinely held-out prediction | 1 existing data |
| H-RHO-C3-CHARACTER | A nondegenerate complex `rho`-C3 observer separates the scalar-Q4 null from weight-4 and conjugate weight-8 sector responses | Real ordinary rows alias conjugate characters; the complex character is parameter-free, while N60-only power is inadequate | Strengthen N112 or change typed observer/geometry with full real-imaginary covariance and frozen character phases | The complex response remains zero or the candidate characters remain identical on a held-out nondegenerate row | 7 |
| H-COMMON-CONTEXT-GENERATOR | One effective transfer generator can act on the same local pivotal rows across Gaussian-cover and annulus-radius contexts, while chronology may require ordered composition | The first same-semantics rectangle gives only `Delta=.2162`, composite-null `p=.4002`; `6de148b` finds a common matching-odd static residual, but no `AU/UA` path order was measured | Choose a Mahalanobis-maximin row for actual `AU` and `UA` cells or an intermediate state in the shared schema | A context-enriched model wins on a held-out row, or `AU-UA` is resolved away from zero | 8 |
| H-W5-DISK-DUAL | W5 and its actual disk-relative cell may support a useful ordered periodic comparison, without being a self-dual cell | Main supplies candidate polynomials, disk embeddings `b47d99e`, gluing `eca7d4e` and Mobius event inversion `8f61cc7`. Branch `5f5afd3` distinguishes spherical orbit exchange from the deterministic disk Kreweras complement; the relative cell is a cycle with leaves | Produce one concrete ordered composition, its exact connected-event differences and a stochastic/comparison map | The specified composition fails planar support, independence or its law-preserving comparison; this rejects that construction, not all future maps | 9 |
| H-ODD-RADIAL-PLANE | The annulus matching-odd sector is a rotating rank-two plane while the plus control lies on a simpler boundary | Branch-only sector score rejects bounded-window R1 for `A_minus` (`p=.0192`), all named R2 classes close, and separated J2 gains weak support; `A_plus` does not require R2 | One source/readout chosen to separate shared-J2, parity-specific-J2 and gap-one classes, not more replicas of the same shell row | A held-out parity-sensitive cell is predicted by one common scalar/rank-one generator | parallel |
| H-SEAM-TYPED-SPIN4 | A spatially identifiable angular row and a colour seam can jointly separate ordinary singlet from charged spin-4 candidates before an exponent fit | Exact Q=4 preflight gives colour ratios 1/0; main-integrated PR #291 proves one C4 orbit aliases scalar/spin 4 | Add calibrated phase diversity or typed internal data, report the necessary angular-rank gate separately, then emit constituent numerators and six raw seam `Z/N` values | Angular rank remains one after calibration, or the independent colour ratios fail 1/0 | parallel |
| H-P321-E4-SHAPE | The ordinary root drift is carried by one modulus-shaped thermal block, while its physical module may require the P333 typed marked/direct-sum interface | Deep `533ea6f` remains E4-compatible but leaves the correction ratio unidentifiable. Exact closures require two modules; scalar detach and one endpoint mark fail the Gram gate | Use P321 as an independent modulus coordinate after a P333 full-Q marked source exists; do not use another ordinary shape sample to define the module | The modulus curve and every surviving physical marked module require incompatible sector transport, or no nonzero overlap survives | parallel |
| H-Q-LIFT-CONNECTION | A bottom-field-projected scale/modulus log slope is invariant under admissible generic-Q lift changes even though raw Q tangents are not | P333 exactly finds a `-pi0D` first-jet shift between two lifts and supplies a full descriptor; the counterterm is not yet a Jordan gauge map | Boundary or VJS positive control with complete lift/projector/field descriptor, representation projection and held-out log-slope | Projected slopes still depend on the admissible lift or no common bottom-field connection exists | parallel |
| H-H4-H12-ORDINARY-CONTRAST | The ordinary direction response may contain an A12 component distinguishable from A4 by opposite alias signs at fixed N | Main PR #325 freezes the geometry; branch `86e77db` completes 600M paired replicas per N305/N325 design. H4-only survives, zero also survives and `A12=.1474+/-.1911` is unresolved | Introduce an independently motivated higher-amplitude matching-odd source while retaining the opposite-alias coordinate; use the third alias only when source strength makes it informative | A12 remains unresolved under the stronger source or changes sign across held-out sizes | parallel opportunity |
| H-CHARGED-VECTOR-STATE | P250 adaptive order contains information beyond a low-dimensional spatial-residue projection, with a remaining high-frequency coordinate | The completed ten-fold clustered comparison gives 30.97% held-out gain from pre-outcome state in both hands; endpoint periodograms are absent and both enriched residuals peak at `k=36` | Preserve the result; a future narrow block retains complete terminal periodograms, the same state and a frozen common-`k=36` readout | Complete terminal spectrum absorbs the state gain, or the common high-frequency residual does not transport | 5 future targeted |
| H-P334-TOPOLOGICAL-ULC | Safe-insertion graph organization, rather than scalar checkpoint summaries, carries cooperative branching variation | `6147e22` proves real-checkpoint scalar-state nonclosure; successor H2 is recoverable. Draft `2b0844a` finds within-checkpoint direction contrasts `(3.61607 +/- 3.25069)e-6` and `(-1.53489 +/- 2.08635)e-6`, unresolved despite a positive mean excess | Deterministically replay saved checkpoints and explain degree second moments/2-star overlaps through boundary organization and direction; Hall remains parallel | The proposed graph statistic does not distinguish the exact collisions or explain the declared response beyond the existing degree moment | 2 replay |
| H-MOTIF-ORTHOGONALITY | Much of the noisy directional tail/K2 deformation is correlated with exact-zero fixed-rank local motif contrasts, while the residual isolates nonlocal topology | Main proves fixed-K zero means and now supplies exact control/control Gram blocks for certified N65/85/130/145/170 production pairs; target/control covariance and useful variance reduction are unmeasured | One paired N130 target/control cross-covariance pilot using the exact control Gram, cross-fitted weights and held-out reconstruction of the complete K1/K2 curves | Held-out directional variance does not improve, or only one-orientation levels improve while contrasts do not | shared method |
| H-PRIMITIVE-STABILIZER | Every connected carrier image is saturated, so `iota=1` is an exact state identity even on degenerate quotients | Parent `73d4960` claims rational duality; child `c1a72e5` claims integral closure by honest carriers on every `qL` cover and a coprime-prime Smith contradiction. Main PR #330 supplies index-13 regression, not a proof | Review matching-diagonal component preservation, carrier embedding and `H_up=H intersect qL`; use the finite backend as a counterexample alarm | One failure of the carrier/descent lemma or one exact state with `iota>1` | parallel exact |
| H-HOMOLOGY-SCALING-WINDOW | The consistent homological balance root has a near-critical window governed by the percolation `L^-3/4` scale | Branch-only `23fd682` proves `p_L->p_c` without a rate; fixed-subcritical bounds are exponential but do not enter the critical window | Two-sided bounds for black/white ambient-H1 activation at `p_c+/-lambda L^-3/4` | A different tightness scale is required for homological balance | theory opportunity |
| H-CONNECTIVITY-RADICAL | The completed rooted/landing response module has independently excitable physical connected responses | `5389200` uniquely closes the inherited nine-mark intersection; `afc619c` fixes `X2=0`, `J2=0` and makes higher fixed-radical projected-Gram gates automatic. A physical transfer and unprojected Gram closure are not established | Calculate the positive-measure width-four rooted/landing charge-neutral 2x2 connected-response matrix, cross term and determinant | The specified positive-weight realization cannot reproduce the declared readouts, or their response matrix has only one excitable direction | 4 finite exact response |
| H-CROSS-MICRO-JORDAN | Triangular log-pair and square spin-4 response may be different microscopic realizations of one descendant extension | Triangular cross-cutoff control is positive; raw `kappa_proxy` and raw amplitudes remain gauge-dependent; square global `Gamma` is not a valid field row | A normalization-free top-field statistic and two-modulus H4/E6 polynomial null using a separated-local or typed square observer | Both systems pass internally but disagree after all allowed shear and metric gauges are cancelled | parallel |
| H-IMPROVED-ACTION | An exactly critical oblique family can tune ordinary H4 through zero; a quadratic charged response would vanish to second order there | Main proves only the microscopic stencil feasibility gate; no exact-critical tunable family yet exists | Exact construction first, then three common-frame coupling values bracketing the zero | The exact microscopic proxy crosses zero without a large-scale ordinary sign change | opportunity |
| H-FACTOR-FOUR | The factor four between the main Q-velocity gap and the boundary high-branch log coefficient is a Ward-descendant collision rule | `sqrt(3)/(4pi)` versus `sqrt(3)/pi` is an exact numerical coincidence across separate derivations | Symbolically apply the full Q4 Ward operator to the colliding generic-Q pair, retaining projector and explicit-field terms | A different residue, colour tensor or Q parametrization removes the factor four | exact sprint |

## Narrow mechanisms now resolved

These outcomes lower repetition value without closing broader sectors.

| Declared parameterization | Current outcome | What remains open |
|---|---|---|
| connected `f(q)` times a root-independent rank-birth gate | exact contact closure | fixed-q variation, external Euler/Betti, charged and separated readouts |
| primitive-line mean-`J_D4` with one-step q2 phase `-2^-13/8` | rejected on both q2 edges | matrix/two-step transfer only with a new preregistration and independent coordinate |
| radius-4 minus radius-2 local landing-D4 plus one trained thermal shell | rejected at held-out N130 | other genuinely separated local covectors and retained multiradius mechanism analysis |
| separate Gaussian/annulus lambdas on the first same-semantics rectangle | no evidence over one shared lambda | better-separated rows, actual `AU/UA`, intermediate state and other sectors |
| opposite-Pell sign-flipped completion | not selected; survives only at a collapsed amplitude boundary under alpha `.01` | a higher-power phase covector; no `.05` rescore of the same reveal |
| correction-free direct-birth line `D_N=A N^-5/6` | rejected across the four-scale P337 line (`p=7.46e-4`); the last doubling ratio remains compatible with `5/6` | a correction-bearing `5/6` asymptotic and separate state-current recurrences |
| “every direct rank-two birth is exactly a six-arm carrier” | false: exact carriers split into theta and figure-eight types, giving only upper six/eight-arm implications | lower arm bounds, gluing and universality; carrier type is absent from production archives |
| P334 birth marks, `iota`, birth-site Smith class, cheap geometry or compact bottleneck shape explain the age slope | rejected as sufficient recorded controls; real checkpoint collisions now also refute the declared scalar branching tuple | safe-insertion graph degree dispersion/2-star organization from existing checkpoint replay; no new `b2` calibration is needed |
| minimal scalar generic-Q detach and one terminal mark | both fail the exact width-3/4 Gram gate; the newer nine-mark rooted/landing accumulator closes the inherited intersection | physical connected response and unprojected/full-Q realization, not another identical projected closure |
| P250 fixed bridges, endpoint rank<=7 and full rank-eight R2-plus-conjugation plane | rejected; endpoint rows are order-blind, fixed overwrites degenerate, and completed adaptive production plus cross-fit shows a retained pre-outcome state increment | complete-terminal-spectrum versus state and the shared `k=36` residual remain open |
| W5 disk self-duality through the spherical terminal partition | rejected; spherical projection loses cycle data, while the actual cycle-with-leaves disk dual has the unique Kreweras-complement partition | concrete ordered W5/relative-dual periodic composition using the completed disk-embedding and gluing inputs |
| more bounded four-terminal probability, disk-order or generic composition census | complete inputs include all 27 polynomials, vulnerability, disk embeddings, gluing and Mobius event inversion | one concrete periodic composition, stochastic comparison and any resulting threshold consequence |

## Minimum decision portfolio

The next work consumes existing artifacts first. New acquisitions in the lower rows are future options, not jobs started by this update.

| Work package | Reuse first | Concrete scientific output | Decisions covered | Cost |
|---|---|---|---|---:|
| 1. E_top production survivors | Completed N50 factorial plus norm-4/norm-5 archives and covariance | Amplitude-only versus direction-changing survivor comparison, numerical predictions and the most separating third tau/map | H-ETOP-TAU-TOPOLOGY | existing data |
| 2. P334 checkpoint graph | Complete cooperative counts, exact scalar collisions and reproducible checkpoint counters | Safe-insertion degree/2-star structure tied to boundary organization and direction, using deterministic replay | H-P334-TOPOLOGICAL-ULC | no new samples |
| 3. P439 transported response | Completed same-stream direct/plateau decomposition; both components weak | Resolvable loading of `M` or a concrete line-relative landing contrast with its shared covariance | H-BIRTH-EXIT-POLARIZATION | reuse / finite calculation |
| 4. P398 physical response | Completed nine-mark module and automatic higher projected-Gram identity | Positive-measure width-four rooted/landing 2x2 connected-response matrix, cross term and determinant | H-CONNECTIVITY-RADICAL; informs H-Q-LIFT-CONNECTION | low exact |
| 5. P250 high-frequency/full-spectrum discriminator | Completed N505 200k/400 and 30.97% state-increment result | Future narrow block with full terminal periodograms, the same pre-outcome state and a frozen common-k36 covector | H-CHARGED-VECTOR-STATE | future acquisition |
| 6. Phase-E distinct singlet | Completed canonical A/E, exact alias and completed mixed radius-1 row | A distinct microscopic singlet/scale/field interface with an explicit contrast to A/E/C | H-NORM4-ENERGY-PARTNER | theory / exact |
| 7. Typed `rho`-C3 character | Parameter-free complex characters, aliased real ordinary rows and inadequate N60-only increment | A nondegenerate N112 or alternative typed response retaining complex covariance | H-RHO-C3-CHARACTER | future targeted pilot |
| 8. Ordered context action | Same-semantics Gaussian/annulus rectangle, annulus R2 rows and common static matching-odd residual | One separating readout for actual `AU` and `UA`, or an intermediate filtration when endpoints lose order | H-COMMON-CONTEXT-GENERATOR, H-ODD-RADIAL-PLANE | future targeted acquisition |
| 9. W5 disk-relative dual | All 27 polynomials, disk embeddings, gluing, Mobius inversion and the spherical/disk distinction | Concrete periodic composition, exact connected-event differences and a law-preserving comparison map | H-W5-DISK-DUAL | low exact / theory |
| #370 support to attention 1 | Completed first real production-confidence adapter and exact primitives | Portable exclusion for the actual E_top survivor comparison when useful; no independent synthetic/compiler/validator milestone | method support | on demand |
| Operator Passport dashboard | Existing projector zeros, TL/partition actions, word moments, continuum ratios and microscopic controls | Fill whichever of the six coordinates currently separates the live model pair; never require dashboard completion | all operator-identification lines | variable |
| Activation shape, external bridge and motif control | Existing ten K1/K2 archives, completed Euler occupation residual and two-observer rank-one null | Shared-Jacobian K2/tail conditioning plus one exact-gated current-geometry, connectivity or winding source outside the completed nuisance span | H-LIFETIME-POLARIZATION, H-EXTERNAL-EULER-BRIDGE, H-MOTIF-ORTHOGONALITY | low -> moderate |
| Tail-flow holdout | Existing norm-2/norm-5 and independent P43 curves | Freeze one tail vector and score only its amplitude on a fresh lineage, with thermal-jet cross-moments | H-TAIL-FLOW-FIBER | none -> moderate |
| Typed exact spin-4 | Existing Q=4 seam preflight and main-integrated PR #291 alias gate | Calibrated multi-phase or typed-edge kernel; spatial angular rank and colour-seam character scored separately | H-SEAM-TYPED-SPIN4; supplies a positive Q-tangent control | low |
| P321 modulus coordinate | E4-compatible deep result and exact two-closure/graded-extension certificates | Score the modulus curve against a physical P333 marked source after that source exists; do not use another scalar detach to define the module | H-P321-E4-SHAPE | low |
| Generic-Q transport | Existing Q velocities, VJS/boundary controls and P333 shared exact bundle | Complete descriptor, bottom-field projection and held-out scale/modulus log slope | H-Q-LIFT-CONNECTION, H-FACTOR-FOUR | low |
| Mixed context order | Completed same-semantics Gaussian-annulus rectangle, annulus R2 fits and P200 static residuals | A maximin readout with actual `AU/UA` order or intermediate state | H-COMMON-CONTEXT-GENERATOR, H-ODD-RADIAL-PLANE | moderate |
| Exact theory sprint | Rational-cover parent, integral-saturation child, root-consistency proof, Q velocities and Ward row | Review all-`qL` carrier proof and derive near-critical homology bounds | H-PRIMITIVE-STABILIZER, H-HOMOLOGY-SCALING-WINDOW, H-FACTOR-FOUR | low -> high |
| Terminal-gadget comparison | PR #384 star baseline, PRs #394--#396 probability/vulnerability/planarity, main disk/gluing/Mobius inputs and W5 dual obstruction | Use the attention-9 concrete W5/relative-dual periodic comparison, not another generic algebra certificate | threshold-origin opportunity without a field claim | low -> moderate |
| Ordinary H4/H12 contrast | Main opposite-alias design and completed branch production `86e77db` | Preserve the coordinate; pair it with a stronger independent matching-odd source before a third alias | H-H4-H12-ORDINARY-CONTRAST | moderate -> high |
| Gauge-free cross-microscopic control | Existing triangular Phase B sufficient statistics | Normalized top statistic plus typed square row at two moduli | H-CROSS-MICRO-JORDAN | moderate |

## Reinterpretation rules

- Exact contact closure reclassifies `gamma_D4`; it does not negate the observed finite response or the utility of the marked stream.
- The completed mean-line and two-radius local tests reject their declared q2 carriers; they do not reject ordinary global H4, external Euler coupling, charged four-leg or every separated landing observer.
- The Euler occupation clock closes only `sigma(n_occ)+span(J_S)` sufficiency in its declared metric. Its surviving residual is not automatically a second microscopic source, Q4 field or exponent, and all decompositions remain one N325/N425 dependency group.
- Annulus R1 failure is sector-specific. It does not imply that every plus/minus row needs the same rank-two generator.
- The first shared-context rectangle supports one effective generator only for its local pivotal rows. P200 and the rectangle still lack `h0,h2,h5,h25` or both path orders, so they do not identify chronology.
- Descriptor `migration_required` means future semantic transport is not yet machine-checked. It is not an error label, evidence demotion or reason to block a mathematically distinct analysis.
- Fixed-rank motif contrasts are exact-zero controls, not explanations of `p_c` or proof of useful variance reduction.
- Main's rank-four mixing audit rejects an assumed amplitude closure, not the exact parity zero pattern or the four powers.
- P321's fresh pass is compatibility, not thermal-Q4 identification. P398 now completes a nine-mark retained-response intersection; its later projected-Gram passes are automatic, not repeated independent support. Physical source/readout and unprojected/full-Q realization remain distinct questions, while P321 supplies an independent modulus coordinate.
- A raw Q tangent is not defined by its Q=1 endpoint alone. P333's lift counterterm is exact transport data, not Jordan evidence before bottom-field projection and field connection.
- P250's completed reanalysis shows retained pre-outcome state adds 30.97% held-out gain beyond the declared residue baseline. It does not beat the unavailable complete terminal spectrum, and both enriched residuals retain `k=36`. Preserve this positive result; default attention for its next acquisition targets those two sharper coordinates.
- P337's state-current prism gives `E_top'=J12-J01`; `J02` is not a third additive E component. Its midpoint split and ambient-line pilot are complete. P439's newer direct/plateau transport terms are both weak; null compatibility does not establish closure or large-term cancellation. Target `M` loading or a line-relative landing response.
- `DevEnvC_HZsCM6` is historical provenance for committed P337 artifacts only. Never connect to, inspect, stop, resume or retrieve from it. N1360 has no authorization or running production.
- P333's signed connected-history `K` is an algebraic control, not a positive stochastic transfer or LCFT field. P398's coupled rooted/landing accumulator now passes the inherited intersection, but the unprojected Gram residuals remain nonzero. The next physical response calculation uses a positive underlying configuration measure; signed charged readouts need not themselves be nonnegative.
- The completed #370 adapter supports the actual production survivor comparison; optional interval/SOS strengthening and the Operator Passport serve that analysis, not a separate preparation queue.
- P158's sample counts are a source-only power forecast, not N650/N850 observations. They lower current production value without forbidding a target variance pilot or future redesigned estimator.
- PRs #394--#396 complete the bounded family's polynomials, vulnerability and abstract planarity. Later main supplies disk embeddings, finite gluing and Mobius inversion; the actual disk-relative partition is known. The remaining scientific object is a concrete periodic comparison, not another terminal-cofaciality or generic algebra task.
- P429's cross-fit remains a model-conditional predictive allocation, not mediation or full sampling uncertainty. P334's later archive already recovers successor H2 and exact cooperative counts, and its checkpoint collisions prove scalar-state nonclosure. Draft directional contrasts remain unresolved. Replay the safe-insertion graph rather than recapture these observables; mixed arbitrary-HNF Hall and BA remain separate.
- The P267 factorial resolves a finite `tau x topology-map` interaction, not H4 or a modular law. The immediate scientific comparison uses existing covariance to distinguish amplitude and response-direction models and choose a third pair; selecting that pair does not start a new acquisition.
- Issue55 production is now complete: H4-only and zero both survive and A12 is unresolved. This lowers the value of the third alias under the same weak source; it does not rule out H12 under a stronger source.
- A lower-ranked route stays open and can immediately rise when it offers a sharper observer, exact map or shared acquisition.

## Team handoff

Lead each work block with its scientific output: a number, model comparison, exact object or explanatory result. Attach source commit, dependency/covariance and observer boundaries at delivery. “More samples” is useful when uncertainty, rather than an already-recoverable coordinate or model alias, is the decision bottleneck.
