# Hypothesis and Decision Board

**Frontier date:** 2026-08-30

This board turns the repository into a mechanism-discovery surface. It does not grant permission, lock tasks, close alternatives or promote branch results to `main`. Priority means expected information gained per unit of shared acquisition.

## What changed the decision surface

The newest results close several proposed shortcuts while opening cheaper shared coordinates:

1. The completed three-generation mean-`J_D4` q2 chain rejects the frozen negative-real `-2^-13/8` one-step transfer on both edges. The bare primitive-line mean source is no longer the live H4 carrier, although global H4 and externally marked birth responses remain open.
2. The frozen two-radius landing-D4 selector also rejects its q2 thermal-H4 map (`72.8633/2`, `p=1.51e-16`). This excludes that cutoff parameterization, not every separated-local observer.
3. A bulk Euler residue `O_ext` is not a function of the three-state rank charge. The completed two-observer N325/N425 production gives `chi2_4=4.279`, `p=.370`, so the current source/observer basis does not reject one projective lane. The cleanest continuation is a mechanism-derived cross-boundary, winding or endpoint-hazard source—not another acquisition or rotation of the same matrix.
4. The first same-source/same-readout Gaussian-annulus rectangle finds no evidence for separate context generators (`Delta chi2=.2162`, composite-null bootstrap `p=.4002`). It supports transportability at current resolution but identifies neither `lambda` nor path order.
5. Retrospective production curves reveal a nearly rank-one standardized tail deformation: norm-2, norm-5 and the independent N185-to-N265 block have direction cosines `.9915` to `.9996`. This nominates a shared shape fiber rather than exact finite-size collapse.
6. The N112 primitive C3 row is genuinely multi-character: every pure character fails and E4+E6 narrowly misses, while opposite-Pell N60 is unresolved and cannot overcome the fixed N112-zero power boundary by N60-only replication.
7. P321 identifies a common finite `P2-P0` projector and proposes a parameter-free fixed-aspect `E4(i rho)` fingerprint. Fresh N144/576/1296 x five-shape data pass the fixed scale gate and do not reject conditional E4; the pilot tension does not reproduce. This is compatibility, not a measured exponent, transparency or field identity, so the next row must change sector/projector rather than add ordinary-shape samples.
8. Main reaches the finite Alexander frontier through index 13. Branch child `c1a72e5` now claims the missing integral saturation by all-`qL` carrier descent; it has no PR and remains a proof-review target, not a shared theorem.
9. Exact N13 line orbits reinforce the same projective H4 derivative. P337 identifies one A4 triplet and complete F3 curves; PR #267 shows signed birth minus first-line exit reinforces H at the crossing/reference but H nearly cancels by `.65`. A fresh N130 child must add raw completion winding/lift, transporter and ambiguity metadata before a stabilizer-C3 completion character is meaningful.
10. P250's local R1-R4 normalized family is closed, while a root-marked projective-leg observer passes low-sample propagation at `d=1,2`. The next question is fresh unchanged-operator support and only then phase—not which compact-cubic template ranked first.
11. P333 proves raw Q tangents depend on the chosen generic-Q lift/section. Its two sibling heads share one exact Phase-A bundle; the next meaningful Q observable needs a complete descriptor and bottom-field projection, not another untransported derivative.
12. P158 projects norm-10 commuting-square target cost to roughly 27.21B/55.18B samples per lineage with negligible ideal-CRN gain. It has no target data and moves large production down, leaving only a target variance/runtime pilot.

Main also adds exact fixed-rank motif controls. Through `79c9c22`, the live scorer corpus contains 54 paths: 23 direct, 22 wrapper-covered, 8 migrations and one utility. The newest prequential, P231 and v14-root wrappers add no data or scores and preserve their historical or retrospective boundaries; PRs #339/#349/#354 are synthetic exact methods and PR #351 is a bounded regression, while PR #342 remains a resource gate. These improve variance design, resource discipline and claim composability; they do not add scientific evidence, demote untyped historical scores or turn protocol migration into a permission gate.

## Three-object synthesis

```text
microcanonical nuisance layer
    exact-zero motif contrasts at fixed K
                 |
                 v
rank-one lifetime and shape fiber
    K1/K2, C/W, tail mode, I01/I12, ell, landing marks
                 |
                 | acted on by geometry/context
                 v
low-dimensional context action
    Gaussian A, annulus U, mixed orders AU and UA
                 |
                 | scalar q quotient forgets
                 v
connectivity / defect radical and external observers
    Euler residue, partial partitions, junctions, singular projectors, charged seams
```

This is a conjectural organizing skeleton, not a theorem. It is valuable because each piece has a small, decisive observation that can break it.

## Active hypotheses

| ID | Bold mechanism | Current support and boundary | Smallest decisive observation | Sharp falsifier | Attention |
|---|---|---|---|---|---:|
| H-BIRTH-EXIT-POLARIZATION | The ordinary matching H4 numerator is a difference of first-homology birth influx and first-line exit at rank-two completion, while a section-aware completion route may carry an additional stabilizer-C3 character | Exact N13 line orbits reinforce. On the reused N65 block, birth and exit reinforce H at the crossing/reference and H nearly cancels at `.65`; the fixed charged projection is descriptive and no gauge-audited complement is recorded | One prereveal N130 child with `ell_birth`, raw completion winding/lift, transporter, ambiguity flag, full A4 covariance and frozen crossing/dual | H reinforcement/upper cancellation do not replicate, or the gauge-covariant completion character is zero while no other typed boundary coordinate predicts the curve | 1 |
| H-LIFETIME-POLARIZATION | The local negative `K2` lobes and the replicated upper-tail deformation are two projections of a low-dimensional lifetime/shape fiber, not reversal of the second birth mechanism | PR #267 has positive integrated K2 area at 10/10 sizes; the branch-only threshold-shape result finds a replicated near-rank-one tail vector, but the two directions have not been compared in one covariance metric | Push both perturbations through the same Bernstein-to-standardized-quantile Jacobian, then condition K2 on `W`, `ell`, landing and exact-zero motif contrasts | The whitened K2 and tail directions are independent and none of the typed birth coordinates predicts the K2 node | 1 |
| H-EXTERNAL-EULER-BRIDGE | A physically independent connectivity/defect source can split the bulk Euler response even though the present matrix is projectively rank one | The completed N325/N425 determinant does not reject rank one (`4.279/4`, `p=.370`); this closes the current basis, not all source directions | Exact-gate one `n12`, winding/seam or K1/K2-conditioned Euler source and pair it with the current far/separated observers | Every independently defined source projects onto the same lane after held-out exact gates | 3 |
| H-TAIL-FLOW-FIBER | Cover multiplication and ordinary size transfer advance one universal standardized tail-deformation coordinate after median/IQR removal | Four cover comparisons and an independent N185-to-N265 block have cosines `.9915` to `.9996`; amplitudes do not yet follow one exponent | Freeze the tail vector and predict one fresh lineage; jointly regress its amplitude on the existing thermal-jet and activation-shape coordinates | A fresh lineage rotates outside the frozen covariance cone, or amplitude requires lineage-specific directions | 2 |
| H-COMMON-CONTEXT-GENERATOR | One effective transfer generator can act on the same local pivotal rows across Gaussian-cover and annulus-radius contexts, while chronology may still require ordered composition | The first same-semantics rectangle gives only `Delta=.2162`, composite-null `p=.4002`; all lambdas remain weakly separated and no `AU/UA` path order was measured | Choose a Mahalanobis-maximin row, then acquire actual `AU` and `UA` cells or an intermediate state in the shared schema | A context-enriched model wins on a held-out row, or `AU-UA` is resolved away from zero | 3 |
| H-ODD-RADIAL-PLANE | The annulus matching-odd sector is a rotating rank-two plane while the plus control lies on a simpler boundary | Branch-only sector score rejects bounded-window R1 for `A_minus` (`p=.0192`), all named R2 classes close, and separated J2 gains weak support; `A_plus` does not require R2 | One source/readout chosen to separate shared-J2, parity-specific-J2 and gap-one classes, not more replicas of the same shell row | A held-out parity-sensitive cell is predicted by one common scalar/rank-one generator | 3 |
| H-SEAM-TYPED-SPIN4 | A spatially identifiable angular row and a colour seam can jointly separate ordinary singlet from charged spin-4 candidates before an exponent fit | Exact Q=4 preflight gives colour ratios 1/0; main-integrated PR #291 proves one C4 orbit aliases scalar/spin 4 | Add calibrated phase diversity or typed internal data, report the necessary angular-rank gate separately, then emit constituent numerators and six raw seam `Z/N` values | Angular rank remains one after calibration, or the independent colour ratios fail 1/0 | 2 |
| H-P321-E4-SHAPE | The ordinary matching-root drift is carried by one Virasoro-transparent thermal-Q4 block, so its descaled aspect-ratio amplitude follows `E4(i rho)` | Fresh branch `ca72994` passes the fixed scale law and does not reject E4, but amplitude, exponent and transparency remain unresolved; TgFr7R provenance also remains branch-only | A resolved-homology/typed-seam or independent defect/Jordan projector on the same geometry convention | The typed row requires a second modulus function or the transparent-sector map fails despite ordinary scalar compatibility | 2 |
| H-Q-LIFT-CONNECTION | A bottom-field-projected scale/modulus log slope is invariant under admissible generic-Q lift changes even though raw Q tangents are not | P333 exactly finds a `-pi0D` first-jet shift between two lifts and supplies a full descriptor; the counterterm is not yet a Jordan gauge map | Boundary or VJS positive control with complete lift/projector/field descriptor, representation projection and held-out log-slope | Projected slopes still depend on the admissible lift or no common bottom-field connection exists | 5 |
| H-H4-H12-ORDINARY-CONTRAST | The ordinary direction response contains an A12 component distinguishable from A4 by opposite alias signs at fixed N | Main PR #325 freezes the geometry; branch `0b8bb63` supplies a target-blind variance/power pilot and 600M/design standby. No target score exists, and two rows/two columns have zero residual degrees of freedom | Acquire only the frozen paired blocks and report the covariance-aware A12 contrast after the H4-only zero test | The A12 contrast is unresolved or reverses across the two sizes; no omnibus or H12-only claim follows either way | parallel high cost |
| H-CHARGED-QUADRATIC | The charged H8 preference may be a quadratic `E4^2` response carried more efficiently by a projective-leg insertion | P226 linear data prefer H8; compact cubic support is unresolved; the local R1-R4 family is closed, while the projective-leg smoke propagates at `d=1,2` | Fresh unchanged-operator projective-leg `d=1,2` support-first block with the linear row when feasible | Fresh pair support fails, or supported closure/phase drifts with separation or disagrees with the linear eigenphase | 2 |
| H-MOTIF-ORTHOGONALITY | Much of the noisy directional tail/K2 deformation is correlated with exact-zero fixed-rank local motif contrasts, while the residual isolates nonlocal topology | Main proves fixed-K zero means for equal-multiplicity NN, diagonal, face and right-angle contrasts at N65/85/130/145/170; useful covariance is unmeasured | One paired N130 joint-Gram pilot with cross-fitted motif weights and held-out reconstruction of the complete K1/K2 curves | Held-out directional variance does not improve, or only one-orientation levels improve while contrasts do not | shared method |
| H-PRIMITIVE-STABILIZER | Every connected carrier image is saturated, so `iota=1` is an exact state identity even on degenerate quotients | Parent `73d4960` claims rational duality; child `c1a72e5` claims integral closure by honest carriers on every `qL` cover and a coprime-prime Smith contradiction. Main PR #330 supplies index-13 regression, not a proof | Review matching-diagonal component preservation, carrier embedding and `H_up=H intersect qL`; use the finite backend as a counterexample alarm | One failure of the carrier/descent lemma or one exact state with `iota>1` | exact priority |
| H-HOMOLOGY-SCALING-WINDOW | The consistent homological balance root has a near-critical window governed by the percolation `L^-3/4` scale | Branch-only `23fd682` proves `p_L->p_c` without a rate; fixed-subcritical bounds are exponential but do not enter the critical window | Two-sided bounds for black/white ambient-H1 activation at `p_c+/-lambda L^-3/4` | A different tightness scale is required for homological balance | theory opportunity |
| H-CONNECTIVITY-RADICAL | Q-tangent singularities and threshold nonmonotonicity may both live in connectivity information discarded by the semisimple `1,q,q^2` quotient | Scalar relative source has rank-three closure and `HH^2=0`; branch-only `5d8343d` proves the terminal defect is a typed-site rank-image quotient requiring a four-terminal `J4`. Main-integrated PR #329 supplies a stable partition-orbit key only; the common-radical interpretation remains conjectural | Add typed port/occupation labels, define D4-preserving junction composition, quotient the scalar blocks and project both defect vectors into the resulting radical/Jordan components | The extension remains semisimple or the two defects lie in unrelated simple components | 5 |
| H-CROSS-MICRO-JORDAN | Triangular log-pair and square spin-4 response may be different microscopic realizations of one descendant extension | Triangular cross-cutoff control is positive; raw `kappa_proxy` and raw amplitudes remain gauge-dependent; square global `Gamma` is not a valid field row | A normalization-free top-field statistic and two-modulus H4/E6 polynomial null using a separated-local or typed square observer | Both systems pass internally but disagree after all allowed shear and metric gauges are cancelled | 6 |
| H-IMPROVED-ACTION | An exactly critical oblique family can tune ordinary H4 through zero; a quadratic charged response would vanish to second order there | Main proves only the microscopic stencil feasibility gate; no exact-critical tunable family yet exists | Exact construction first, then three common-frame coupling values bracketing the zero | The exact microscopic proxy crosses zero without a large-scale ordinary sign change | opportunity |
| H-FACTOR-FOUR | The factor four between the main Q-velocity gap and the boundary high-branch log coefficient is a Ward-descendant collision rule | `sqrt(3)/(4pi)` versus `sqrt(3)/pi` is an exact numerical coincidence across separate derivations | Symbolically apply the full Q4 Ward operator to the colliding generic-Q pair, retaining projector and explicit-field terms | A different residue, colour tensor or Q parametrization removes the factor four | exact sprint |

## Narrow mechanisms now resolved

These outcomes lower repetition value without closing broader sectors.

| Declared parameterization | Current outcome | What remains open |
|---|---|---|
| connected `f(q)` times a root-independent rank-birth gate | exact contact closure | fixed-q variation, external Euler/Betti, charged and separated readouts |
| primitive-line mean-`J_D4` with one-step q2 phase `-2^-13/8` | rejected on both q2 edges | matrix/two-step transfer only with a new preregistration and independent coordinate |
| radius-4 minus radius-2 local landing-D4 plus one trained thermal shell | rejected at held-out N130 | other genuinely separated local covectors and retained multiradius mechanism analysis |
| separate Gaussian/annulus lambdas on the first same-semantics rectangle | no evidence over one shared lambda | better-separated rows, actual `AU/UA`, intermediate state and other sectors |
| opposite-Pell sign-flipped completion | not selected; survives only at a collapsed amplitude boundary under alpha `.01` | a higher-power phase covector; no `.05` rescore of the same reveal |

## Minimum decision portfolio

The next work should share acquisitions instead of opening one stream per hypothesis.

| Work package | Reuse first | Smallest new acquisition | Decisions covered | Cost |
|---|---|---|---|---:|
| Activation shape, projective twist flux, external bridge and motif control | Existing ten K1/K2 archives; exact N13 orbit flux; P334/P337 N65 block; completed two-observer rank-one null | One fresh N130 two-line completion child, common-Jacobian projection and one exact-gated independent connectivity/defect source; at most one motif cross-fit | H-BIRTH-EXIT-POLARIZATION, H-LIFETIME-POLARIZATION, H-EXTERNAL-EULER-BRIDGE, H-MOTIF-ORTHOGONALITY | low -> moderate |
| Tail-flow holdout | Existing norm-2/norm-5 and independent P43 curves | Freeze one tail vector and score only its amplitude on a fresh lineage, with thermal-jet cross-moments | H-TAIL-FLOW-FIBER | none -> moderate |
| Typed exact spin-4 | Existing Q=4 seam preflight and main-integrated PR #291 alias gate | Calibrated multi-phase or typed-edge kernel; spatial angular rank and colour-seam character scored separately | H-SEAM-TYPED-SPIN4; supplies a positive Q-tangent control | low |
| P321 modulus shape | Completed branch-only scale/E4-compatible fresh result | One resolved-homology/typed-seam or independent second-function projector, plus transparency proof/obstruction | H-P321-E4-SHAPE, H-SEAM-TYPED-SPIN4 | low -> moderate |
| Generic-Q transport | Existing Q velocities, VJS/boundary controls and P333 shared exact bundle | Complete descriptor, bottom-field projection and held-out scale/modulus log slope | H-Q-LIFT-CONNECTION, H-FACTOR-FOUR | low |
| Mixed context order | Completed same-semantics Gaussian-annulus rectangle, annulus R2 fits and P200 static residuals | A maximin readout with actual `AU/UA` order or intermediate state | H-COMMON-CONTEXT-GENERATOR, H-ODD-RADIAL-PLANE | moderate |
| Charged complex geometry | Existing N325 linear engine, N112 character rows, compact cubic and projective-leg selector | Fresh projective-leg `d=1,2` support-first block; if support passes, add joint linear-cubic phase covariance; strengthen N112 in parallel | H-CHARGED-QUADRATIC | moderate |
| Exact theory sprint | Rational-cover parent, branch-only integral-saturation child, main index-13 regression, root-consistency proof, Q velocities, Ward row, scalar closure and terminal-partition key | Review the all-`qL` carrier proof, derive near-critical homology bounds, symbolic residue and typed junction radical | H-PRIMITIVE-STABILIZER, H-HOMOLOGY-SCALING-WINDOW, H-CONNECTIVITY-RADICAL, H-FACTOR-FOUR | low -> high |
| Ordinary H4/H12 contrast | Main opposite-alias design and branch target-blind variance pilot | Frozen paired production only when this high-cost ordinary-sector contrast is chosen for attention | H-H4-H12-ORDINARY-CONTRAST | high |
| Gauge-free cross-microscopic control | Existing triangular Phase B sufficient statistics | Normalized top statistic plus typed square row at two moduli | H-CROSS-MICRO-JORDAN | moderate |

## Reinterpretation rules

- Exact contact closure reclassifies `gamma_D4`; it does not negate the observed finite response or the utility of the marked stream.
- The completed mean-line and two-radius local tests reject their declared q2 carriers; they do not reject ordinary global H4, external Euler coupling, charged four-leg or every separated landing observer.
- Annulus R1 failure is sector-specific. It does not imply that every plus/minus row needs the same rank-two generator.
- The first shared-context rectangle supports one effective generator only for its local pivotal rows. P200 and the rectangle still lack `h0,h2,h5,h25` or both path orders, so they do not identify chronology.
- Descriptor `migration_required` means future semantic transport is not yet machine-checked. It is not an error label, evidence demotion or reason to block a mathematically distinct analysis.
- Fixed-rank motif contrasts are exact-zero controls, not explanations of `p_c` or proof of useful variance reduction.
- Main's rank-four mixing audit rejects an assumed amplitude closure, not the exact parity zero pattern or the four powers.
- P321's completed fresh pass is compatibility, not thermal-Q4 identification. The next typed sector/independent-projector row asks a different question; ordinary shape replication does not prove transparency.
- A raw Q tangent is not defined by its Q=1 endpoint alone. P333's lift counterterm is exact transport data, not Jordan evidence before bottom-field projection and field connection.
- P158's sample counts are a source-only power forecast, not N650/N850 observations. They lower current production value without forbidding a target variance pilot or future redesigned estimator.
- The Issue55 variance pilot determines cost and covariance, not a target harmonic. Its future two-row A12 statistic has zero residual degrees of freedom.
- A lower-ranked route stays open and can immediately rise when it offers a sharper observer, exact map or shared acquisition.

## Team handoff

Every new result should state `state/source/observer/geometry/acquisition`, lifecycle status, immutable source commit, dependency group, exact model pair separated, and the next observation that would change the conclusion. “More samples” is not a next target unless the current uncertainty, rather than model aliasing or missing semantics, is the decision bottleneck.
