# Hypothesis and Decision Board

**Frontier date:** 2026-08-30

This board turns the repository into a mechanism-discovery surface. It does not grant permission, lock tasks, close alternatives or promote branch results to `main`. Priority means expected information gained per unit of shared acquisition.

## What changed the decision surface

The newest results close several proposed shortcuts while opening cheaper shared coordinates:

1. Draft `7e5fcf1` completes the P337 state-current prism: `E_top'=J12-J01`, N170 resolves reinforcing first-birth/second-completion currents, N340 localizes the resolved part to completion, and J02 cancels from fixed-p E. Completion-age moments are directional, but the conditional age-hazard contrast is unresolved. The next zero-sample split is midpoint risk composition versus hazard, separate J01/J12 recurrences and age-standardized `mark12_h4`.
2. The correction-free direct-birth law rejects. Exact carrier topology is theta or figure-eight; `cfb3ead` adds the bounded reverse surgery only for globally typed arms. Ordinary six arms remain insufficient, and uniform separation, typed frequency and figure-eight share remain open.
3. P250's fixed maps, endpoint rank<=7 and R2 rank-eight plane reject, but PR #416 and branch `dbeb29c` show that the complete N505 endpoint rank is compatible with an ordinary positive spatial spectrum. Exact `af7dd01` proves endpoint abelianization; exact `3128e3e` proves fixed-site D/J overwrites are order-degenerate; exact `6fbbe5e` supplies a viable adaptive state-dependent D/J witness. The next object is its N505 production pilot with retained support, intermediate typed state, defined/tie rates and joint periodogram covariance.
4. P333 scalar detach/marks fail. The C3 charge-one doublet repairs width 3, but the revealed width-4 C4 charge-one gate `7b40ec7` fails exactly at Gram self-adjointness. The minimum untested type is the frozen one-dimensional C4 charge-two alternating landing character `a572058`.
5. P334 age slopes survive recorded controls, and the same-path kernel rejects rank-one separability. Cheap current geometry and the compact bottleneck vector fail common absorption. Main `d9f813b` now proves that two N16 states with the same complete unbranched survival law can differ after one common update and cloning by exactly `1/98`; the live production row is therefore branching continuation, not another proxy or an intrinsic-memory claim.
6. Branch `62585f2` rejects one universal cross-geometry A/E ray relative to four lineage rays (`Delta chi2=14.7004/3`, `p=.00209`) without rejecting H4 or a modular law. The live design is `tau x topology-map` with fixed-area, twist and modular-equivalent controls.
7. The static Gaussian/annulus rectangle does not reject one generator, and `6de148b` finds a common matching-odd residual, but neither archive records chronology. Only actual `AU` and `UA` cells or an intermediate filtration can distinguish context action from path memory.
8. Main PRs #394--#396 complete all 27 four-terminal reliability polynomials, vulnerability and bounded abstract planarity. Branch `5f5afd3` preserves spherical W5 orbit exchange but rejects disk self-duality: the relative dual is a cycle with four terminal leaves. The next object is joint primal/relative-dual boundary connectivity plus a periodic comparison map.
9. The bulk Euler occupation clock removes 53%/56% of raw far-D magnitude but leaves 47%/44%; exact K-centering removes every degree-one product-noise projection. Acquisition gates classify marked birth as Palm-like, fixed-K `O_ext` as Johnson-scoreable, and `O_far`/high-pass as `not_scoreable`. The survivor still needs one new current-geometry, cross-boundary or winding source.
10. P321's E4-compatible modulus result, the triangular gauge-free continuation, stronger ordinary H4/H12 sources, P333 charge-two, near-critical homology theory and proof-carrying elimination remain parallel lanes. They may rise whenever they supply a sharper discriminator; none is a prerequisite for the seven default attention lines.

Main through `5ac456d7397bdf311c01bad2f6510bd425c93e0f` contains 59 scorer paths: 28 direct, 27 wrapper-covered, 3 migrations and one utility. The #370 framework lands at component tip `cc3c204`; observer/geometry/scale/acquisition filters through PR #437 and the direct N16 branching counterexample add exact design boundaries without production observations. Draft PR #267 contains 65 = 28 direct + 27 covered + 9 migration + 1 utility after six Draft-only channel-bearing scorers. Main's exact controls and Draft's semantic adapters do not create physical transfer, a new bound or field evidence. Semantic inventory improves claim composability; it neither demotes a score nor turns migration into a permission gate.

## Production-first method lane and six-coordinate dashboard

The default elimination ladder is exact identities/minors, complete production covariance, outward interval/algebraic reconstruction, then SOS or new acquisition only for classes that survive. Canonical topological Phase D covers the ten-size cross-geometry archive and all six declared P154 norm-4 blocks. Here `E_top=1-P1` means Alexander-even topology, not an energy operator. The actual Phase-E `J_top/J_bulk` projection is unidentifiable because P154 lacks a same-batch ordinary-energy/singlet row and its cross-covariance; acquiring that row is now the first production output. The #370 fixtures and basic exact certificate primitives land by `cc3c204` and remain in current main; what remains is a real production-covariance adapter, arbitrary typed problem building, independently implemented final verification, physical M3c, full gauge coverage and optional interval/SOS. The later exact oracles are usable only inside their declared product-noise, signed-Pell or jointly covariant four-size semantics. Missing tools or observers never gate exploration.

The Operator Passport is a dashboard with six separable coordinates: representation/projector, finite algebra, propagation, sphere fusion, torus/modulus and microscopic control. Any coordinate may redirect the next experiment; “passport complete” is not a status, gate or admission checklist.

The literature boundary remains narrow. Normalization-free loop-model three-point/OPE ratios and torus one-point modulus ratios are candidate discriminators for Matching One, not proven fingerprints for its current lattice observable or arbitrary Jordan/defect fields. Exact finite-lattice singular-vector relations currently provide an ADE RSOS/root-of-unity TL template; transport to percolation/Q4, and convergence to the intended Virasoro relation, remain new theorem work.

## Three-object synthesis

```text
microcanonical nuisance layer
    exact-zero motif contrasts at fixed K
                 |
                 v
rank-one lifetime and shape fiber
    K1/K2, C/W, tail mode, I01/I12, ell, landing marks
                 |
                 | acted on by geometry/context
                 v
low-dimensional context action
    Gaussian A, annulus U, mixed orders AU and UA
                 |
                 | scalar q quotient forgets
                 v
connectivity / defect radical and external observers
    Euler residue, partial partitions, junctions, singular projectors, charged seams
```

This is a conjectural organizing skeleton, not a theorem. It is valuable because each piece has a small, decisive observation that can break it.

## Active hypotheses

| ID | Bold mechanism | Current support and boundary | Smallest decisive observation | Sharp falsifier | Attention |
|---|---|---|---|---|---:|
| H-NORM4-ENERGY-PARTNER | The norm-4 Alexander-even response has a measurable ordinary energy/singlet overlap rather than being exhausted by topological activation coordinates | Six-size Phase D proves a nonzero A/E response but the archive contains no `J_bulk`; no energy projection has yet been measured | Same-batch ordinary bulk-energy row, `B2,A*B,E*B`, full covariance and explicit common-transfer `J_top/J_bulk` images | The preregistered energy projection is zero while a held-out topological coordinate predicts the response, or no semantically valid bulk row can be constructed | 1 |
| H-BIRTH-EXIT-POLARIZATION | The directional completion current mixes cooperative branching state, changing risk-age composition and a genuine conditional hazard, with a separate completion cause | `E_top'=J12-J01`; shape absorbers fail. Main `d9f813b` shows equal complete unbranched survival can hide a `1/98` common-update-then-clone branching gap; conditional hazard remains unresolved | Record checkpoint `b2`, one common update and two clone continuations; independently apply `Delta J12=hbar Delta R+Rbar Delta h` and score `mark12_h4` | Branching is predicted by the unbranched/cooperative row on held-out checkpoints and the proposed hazard split fails exact reconstruction | 1 / 4 |
| H-LIFETIME-POLARIZATION | The local negative `K2` lobes and the replicated upper-tail deformation are two projections of a low-dimensional lifetime/shape fiber, not reversal of the second birth mechanism | PR #267 has positive integrated K2 area at 10/10 sizes; the branch-only threshold-shape result finds a replicated near-rank-one tail vector, but the two directions have not been compared in one covariance metric | Push both perturbations through the same Bernstein-to-standardized-quantile Jacobian, then condition K2 on `W`, `ell`, landing and exact-zero motif contrasts | The whitened K2 and tail directions are independent and none of the typed birth coordinates predicts the K2 node | parallel |
| H-EXTERNAL-EULER-BRIDGE | A physically independent current-geometry/connectivity source can split the bulk Euler response even though the present matrix is projectively rank one | Exact K-centering removes all degree-one product-noise projections; 47%/44% still survives the production fixed-occupation+JS subtraction. P267 marked birth is Palm-like, only fixed-K `O_ext` supports Johnson, and `O_far`/high-pass are not scoreable | Use the scoreable fixed-K `O_ext` coordinate, then exact-gate one new current-geometry, `n12` or winding/seam source without imputing the Palm/`O_far` rows | The residual vanishes under the new source split, or every independently defined source projects onto the same lane after held-out exact gates | parallel |
| H-TAIL-FLOW-FIBER | Cover multiplication and ordinary size transfer advance one universal standardized tail-deformation coordinate after median/IQR removal | Four cover comparisons and an independent N185-to-N265 block have cosines `.9915` to `.9996`; amplitudes do not yet follow one exponent | Freeze the tail vector and predict one fresh lineage; jointly regress its amplitude on the existing thermal-jet and activation-shape coordinates | A fresh lineage rotates outside the frozen covariance cone, or amplitude requires lineage-specific directions | parallel |
| H-ETOP-TAU-TOPOLOGY | The finite A/E direction is a function of both modulus `tau` and the discrete topology map, rather than one universal cross-geometry ray | `62585f2` rejects one global ray relative to four lineage rays (`Delta chi2=14.7004/3`, `p=.00209`) while its absolute fit and H4 remain open | A fixed-area, matched-twist, modular-equivalent `tau x topology-map` design with registered cover/size maps and full lineage covariance | The topology-map interaction vanishes and one modulus-only ray predicts held-out maps, or no stable factorization survives | 3 |
| H-RHO-C3-CHARACTER | A nondegenerate complex `rho`-C3 observer separates the scalar-Q4 null from weight-4 and conjugate weight-8 sector responses | Real ordinary rows alias conjugate characters; the complex character is parameter-free, while N60-only power is inadequate | Strengthen N112 or change typed observer/geometry with full real-imaginary covariance and frozen character phases | The complex response remains zero or the candidate characters remain identical on a held-out nondegenerate row | 5 |
| H-COMMON-CONTEXT-GENERATOR | One effective transfer generator can act on the same local pivotal rows across Gaussian-cover and annulus-radius contexts, while chronology may require ordered composition | The first same-semantics rectangle gives only `Delta=.2162`, composite-null `p=.4002`; `6de148b` finds a common matching-odd static residual, but no `AU/UA` path order was measured | Choose a Mahalanobis-maximin row, then acquire actual `AU` and `UA` cells or an intermediate state in the shared schema | A context-enriched model wins on a held-out row, or `AU-UA` is resolved away from zero | 6 |
| H-W5-DISK-DUAL | W5's useful exact object is a joint primal/disk-relative-dual boundary state, not a self-dual terminal partition polynomial | Main PRs #394--#396 complete candidate polynomials, vulnerability and abstract planarity. Branch `5f5afd3` proves spherical orbit exchange and `r+s=1`, but the disk-relative dual is a distinct cycle-with-leaves cell and terminal projection loses internal-cycle data | Compute joint primal/relative-dual boundary connectivity and give an explicit periodic embedding plus stochastic/comparison map | No periodic embedding or law-preserving comparison exists, or the enriched joint state still fails disk complement closure | 7 |
| H-ODD-RADIAL-PLANE | The annulus matching-odd sector is a rotating rank-two plane while the plus control lies on a simpler boundary | Branch-only sector score rejects bounded-window R1 for `A_minus` (`p=.0192`), all named R2 classes close, and separated J2 gains weak support; `A_plus` does not require R2 | One source/readout chosen to separate shared-J2, parity-specific-J2 and gap-one classes, not more replicas of the same shell row | A held-out parity-sensitive cell is predicted by one common scalar/rank-one generator | parallel |
| H-SEAM-TYPED-SPIN4 | A spatially identifiable angular row and a colour seam can jointly separate ordinary singlet from charged spin-4 candidates before an exponent fit | Exact Q=4 preflight gives colour ratios 1/0; main-integrated PR #291 proves one C4 orbit aliases scalar/spin 4 | Add calibrated phase diversity or typed internal data, report the necessary angular-rank gate separately, then emit constituent numerators and six raw seam `Z/N` values | Angular rank remains one after calibration, or the independent colour ratios fail 1/0 | parallel |
| H-P321-E4-SHAPE | The ordinary root drift is carried by one modulus-shaped thermal block, while its physical module may require the P333 typed marked/direct-sum interface | Deep `533ea6f` remains E4-compatible but leaves the correction ratio unidentifiable. Exact closures require two modules; scalar detach and one endpoint mark fail the Gram gate | Use P321 as an independent modulus coordinate after a P333 full-Q marked source exists; do not use another ordinary shape sample to define the module | The modulus curve and every surviving physical marked module require incompatible sector transport, or no nonzero overlap survives | parallel |
| H-Q-LIFT-CONNECTION | A bottom-field-projected scale/modulus log slope is invariant under admissible generic-Q lift changes even though raw Q tangents are not | P333 exactly finds a `-pi0D` first-jet shift between two lifts and supplies a full descriptor; the counterterm is not yet a Jordan gauge map | Boundary or VJS positive control with complete lift/projector/field descriptor, representation projection and held-out log-slope | Projected slopes still depend on the admissible lift or no common bottom-field connection exists | parallel |
| H-H4-H12-ORDINARY-CONTRAST | The ordinary direction response may contain an A12 component distinguishable from A4 by opposite alias signs at fixed N | Main PR #325 freezes the geometry; branch `86e77db` completes 600M paired replicas per N305/N325 design. H4-only survives, zero also survives and `A12=.1474+/-.1911` is unresolved | Introduce an independently motivated higher-amplitude matching-odd source while retaining the opposite-alias coordinate; use the third alias only when source strength makes it informative | A12 remains unresolved under the stronger source or changes sign across held-out sizes | parallel opportunity |
| H-CHARGED-VECTOR-STATE | P250 may require an ordered adaptive morphism state, while present endpoints are largely ordinary spatial spectrum and fixed overwrites probe a commutative/absorbing quotient | PR #416/`dbeb29c` supply the positive-spectrum baseline; `af7dd01` kills endpoint order, `3128e3e` kills fixed D/J, and `6fbbe5e` gives nonzero adaptive `R_minus` on every defined exact rectangle | Run the adaptive N505 pilot and retain `S_D,S_J,L_D,L_J,L_DJ,L_JD`, `R_plus,R_minus`, defined/tie rates, complement pairs and periodograms in one covariance | Unconditional adaptive response vanishes or is predicted by the spatial-spectrum control, or no stable physical support semantics survives production | 2 |
| H-P334-TOPOLOGICAL-ULC | A separate mixed carrier transport enforces BA/TM while cooperative branching geometry explains part of the production temporal modes | Shape absorbers fail; equal complete unbranched survival can hide a branching gap; BA+TM implies ULC while pure channels/naive switches fail | Test checkpoint `b2` plus common-update-then-clone branching; independently prove mixed arbitrary-HNF orbit Hall or derive its minimum-cut bound | Branching adds no held-out separation, or a new exact HNF gives a Hall witness | 1 / parallel exact |
| H-MOTIF-ORTHOGONALITY | Much of the noisy directional tail/K2 deformation is correlated with exact-zero fixed-rank local motif contrasts, while the residual isolates nonlocal topology | Main proves fixed-K zero means and now supplies exact control/control Gram blocks for certified N65/85/130/145/170 production pairs; target/control covariance and useful variance reduction are unmeasured | One paired N130 target/control cross-covariance pilot using the exact control Gram, cross-fitted weights and held-out reconstruction of the complete K1/K2 curves | Held-out directional variance does not improve, or only one-orientation levels improve while contrasts do not | shared method |
| H-PRIMITIVE-STABILIZER | Every connected carrier image is saturated, so `iota=1` is an exact state identity even on degenerate quotients | Parent `73d4960` claims rational duality; child `c1a72e5` claims integral closure by honest carriers on every `qL` cover and a coprime-prime Smith contradiction. Main PR #330 supplies index-13 regression, not a proof | Review matching-diagonal component preservation, carrier embedding and `H_up=H intersect qL`; use the finite backend as a counterexample alarm | One failure of the carrier/descent lemma or one exact state with `iota>1` | parallel exact |
| H-HOMOLOGY-SCALING-WINDOW | The consistent homological balance root has a near-critical window governed by the percolation `L^-3/4` scale | Branch-only `23fd682` proves `p_L->p_c` without a rate; fixed-subcritical bounds are exponential but do not enter the critical window | Two-sided bounds for black/white ambient-H1 activation at `p_c+/-lambda L^-3/4` | A different tightness scale is required for homological balance | theory opportunity |
| H-CONNECTIVITY-RADICAL | A symmetry-typed source/landing or rooted-history full-Q module carries the connected-history direction discarded by the scalar quotient | `K=(D-J)^2` is an exact signed control; charge one repairs width 3 but fails width 4. The minimum missing C4 charge-two character is frozen | Run charge two; then derive full-Q `P0/P1/P2` source and positivity/velocity, adding rooted history only if the gate fails | Charge two and every minimal rooted lift fail, or the physical cone/dimension velocity excludes the action | parallel exact |
| H-CROSS-MICRO-JORDAN | Triangular log-pair and square spin-4 response may be different microscopic realizations of one descendant extension | Triangular cross-cutoff control is positive; raw `kappa_proxy` and raw amplitudes remain gauge-dependent; square global `Gamma` is not a valid field row | A normalization-free top-field statistic and two-modulus H4/E6 polynomial null using a separated-local or typed square observer | Both systems pass internally but disagree after all allowed shear and metric gauges are cancelled | parallel |
| H-IMPROVED-ACTION | An exactly critical oblique family can tune ordinary H4 through zero; a quadratic charged response would vanish to second order there | Main proves only the microscopic stencil feasibility gate; no exact-critical tunable family yet exists | Exact construction first, then three common-frame coupling values bracketing the zero | The exact microscopic proxy crosses zero without a large-scale ordinary sign change | opportunity |
| H-FACTOR-FOUR | The factor four between the main Q-velocity gap and the boundary high-branch log coefficient is a Ward-descendant collision rule | `sqrt(3)/(4pi)` versus `sqrt(3)/pi` is an exact numerical coincidence across separate derivations | Symbolically apply the full Q4 Ward operator to the colliding generic-Q pair, retaining projector and explicit-field terms | A different residue, colour tensor or Q parametrization removes the factor four | exact sprint |

## Narrow mechanisms now resolved

These outcomes lower repetition value without closing broader sectors.

| Declared parameterization | Current outcome | What remains open |
|---|---|---|
| connected `f(q)` times a root-independent rank-birth gate | exact contact closure | fixed-q variation, external Euler/Betti, charged and separated readouts |
| primitive-line mean-`J_D4` with one-step q2 phase `-2^-13/8` | rejected on both q2 edges | matrix/two-step transfer only with a new preregistration and independent coordinate |
| radius-4 minus radius-2 local landing-D4 plus one trained thermal shell | rejected at held-out N130 | other genuinely separated local covectors and retained multiradius mechanism analysis |
| separate Gaussian/annulus lambdas on the first same-semantics rectangle | no evidence over one shared lambda | better-separated rows, actual `AU/UA`, intermediate state and other sectors |
| opposite-Pell sign-flipped completion | not selected; survives only at a collapsed amplitude boundary under alpha `.01` | a higher-power phase covector; no `.05` rescore of the same reveal |
| correction-free direct-birth line `D_N=A N^-5/6` | rejected across the four-scale P337 line (`p=7.46e-4`); the last doubling ratio remains compatible with `5/6` | a correction-bearing `5/6` asymptotic and separate state-current recurrences |
| “every direct rank-two birth is exactly a six-arm carrier” | false: exact carriers split into theta and figure-eight types, giving only upper six/eight-arm implications | lower arm bounds, gluing and universality; carrier type is absent from production archives |
| P334 birth marks, `iota`, birth-site Smith class, cheap geometry or compact bottleneck shape explain the age slope | rejected as sufficient recorded controls; both acquired geometry proxy families fail common absorption | cooperative continuation (`b2`, overlaps/triangles/minimal triples), conditional path law and genuinely nonlocal history |
| minimal scalar generic-Q detach and one terminal mark | both fail the exact width-3/4 Gram gate; within the one-mark family w3 needs at least two total marks and w4 at least three | typed multi-mark/direct-sum connectivity with a full-Q source |
| P250 fixed bridges, endpoint rank<=7 and full rank-eight R2-plus-conjugation plane | rejected; endpoint rows are exactly order-blind, fixed-site D/J overwrites are degenerate, and the exact adaptive witness supplies a nonzero ordered interface | production adaptive response, exact dimension and physical mechanism remain open |
| W5 disk self-duality through the spherical terminal partition | rejected; the disk-relative dual is a cycle with four terminal leaves | joint primal/relative-dual boundary state and an explicit periodic comparison map |
| more bounded four-terminal probability or abstract-planarity census | complete on `main` through PR #396: all 27 polynomials, vulnerability and 26/27 abstract planarity are recorded | terminal-cofacial disk embedding, periodicity, stochastic comparison and threshold consequences |

## Minimum decision portfolio

The next work should share acquisitions instead of opening one stream per hypothesis.

| Work package | Reuse first | Smallest new acquisition | Decisions covered | Cost |
|---|---|---|---|---:|
| 1. True norm-4 energy Phase E plus P334 cooperative continuation | Six-size P154 A/E covariance, immutable batch semantics, production age paths, two failed shape absorbers and exact cooperative/branching controls | Recover or minimally acquire the same-batch ordinary energy/singlet row with cross-moments and explicit `J_top/J_bulk` images; in parallel record checkpoint `b2`, one common update and two clone continuations | H-NORM4-ENERGY-PARTNER; H-P334-TOPOLOGICAL-ULC | low recovery -> moderate acquisition |
| 2. P250 adaptive morphism rectangle | Positive spatial-spectrum baseline, endpoint rank/map failures and exact endpoint/fixed-toggle order obstructions | State-dependent cut/connector supports, typed intermediate states, defined/tie rates and connected/antisymmetric observability rows on one dependency basis | H-CHARGED-VECTOR-STATE | low design -> moderate acquisition |
| 3. E_top `tau x topology-map` | Four branch-only lineage rays, their full covariance and the rejected universal-ray comparison | Fixed-area, matched-twist and modular-equivalent cells crossing modulus with the discrete topology map | H-ETOP-TAU-TOPOLOGY | moderate |
| 4. P337 midpoint/current recurrence | Complete ten-size covariance, `7e5fcf1` state-current prism, N170/N340 path archives and N130 mapped control | Recompute `Delta J12=hbar Delta R+Rbar Delta h`, fit J01/J12 component recurrences and score age-standardized `mark12_h4`; do not substitute N1360 | H-BIRTH-EXIT-POLARIZATION | none |
| 5. Typed `rho`-C3 character | Parameter-free complex characters, aliased real ordinary rows and the inadequate N60-only increment | Strengthen N112 or change typed observer/geometry with full complex covariance | H-RHO-C3-CHARACTER | low targeted pilot |
| 6. Ordered context action | Same-semantics Gaussian/annulus rectangle, annulus R2 rows and the common static matching-odd residual | One maximin readout with actual `AU` and `UA`, or an intermediate filtration when end states cannot record order | H-COMMON-CONTEXT-GENERATOR, H-ODD-RADIAL-PLANE | moderate |
| 7. W5 disk-relative dual | All 27 exact polynomials, vulnerability/planarity controls and the spherical W5 orbit exchange | Joint primal/relative-dual boundary connectivity, explicit periodic embedding and a law-preserving comparison map | H-W5-DISK-DUAL | low -> theory |
| Parallel exact: P333 charge-two/full-Q radical | Exact `K=(D-J)^2`, scalar failures, width-3 charge-one success and width-4 charge-one failure | First run the frozen one-dimensional C4 charge-two alternating landing gate; full-Q/rooted state only if it fails | H-CONNECTIVITY-RADICAL; informs H-Q-LIFT-CONNECTION | none -> high |
| Production-first elimination | Completed cross-geometry/norm-4 E_top, P250 and Euler distances plus main #370 fixtures and basic exact certificate primitives | Connect one real frozen covariance problem to the framework; add arbitrary typed building or an independent final verifier only when the production survivor requires it | method lane, not a hypothesis gate | low -> high |
| Operator Passport dashboard | Existing projector zeros, TL/partition actions, word moments, continuum ratios and microscopic controls | Fill whichever of the six coordinates currently separates the live model pair; never require dashboard completion | all operator-identification lines | variable |
| Activation shape, external bridge and motif control | Existing ten K1/K2 archives, completed Euler occupation residual and two-observer rank-one null | Shared-Jacobian K2/tail conditioning plus one exact-gated current-geometry, connectivity or winding source outside the completed nuisance span | H-LIFETIME-POLARIZATION, H-EXTERNAL-EULER-BRIDGE, H-MOTIF-ORTHOGONALITY | low -> moderate |
| Tail-flow holdout | Existing norm-2/norm-5 and independent P43 curves | Freeze one tail vector and score only its amplitude on a fresh lineage, with thermal-jet cross-moments | H-TAIL-FLOW-FIBER | none -> moderate |
| Typed exact spin-4 | Existing Q=4 seam preflight and main-integrated PR #291 alias gate | Calibrated multi-phase or typed-edge kernel; spatial angular rank and colour-seam character scored separately | H-SEAM-TYPED-SPIN4; supplies a positive Q-tangent control | low |
| P321 modulus coordinate | E4-compatible deep result and exact two-closure/graded-extension certificates | Score the modulus curve against a physical P333 marked source after that source exists; do not use another scalar detach to define the module | H-P321-E4-SHAPE | low |
| Generic-Q transport | Existing Q velocities, VJS/boundary controls and P333 shared exact bundle | Complete descriptor, bottom-field projection and held-out scale/modulus log slope | H-Q-LIFT-CONNECTION, H-FACTOR-FOUR | low |
| Mixed context order | Completed same-semantics Gaussian-annulus rectangle, annulus R2 fits and P200 static residuals | A maximin readout with actual `AU/UA` order or intermediate state | H-COMMON-CONTEXT-GENERATOR, H-ODD-RADIAL-PLANE | moderate |
| Exact theory sprint | Rational-cover parent, integral-saturation child, root-consistency proof, Q velocities and Ward row | Review all-`qL` carrier proof and derive near-critical homology bounds | H-PRIMITIVE-STABILIZER, H-HOMOLOGY-SCALING-WINDOW, H-FACTOR-FOUR | low -> high |
| Terminal-gadget comparison | PR #384 star baseline, PRs #394--#396 probability/vulnerability/planarity completion and W5 dual obstruction | Use the attention-7 disk-relative-dual/periodic object; do not reopen the bounded census | threshold-origin opportunity without a field claim | low -> moderate |
| Ordinary H4/H12 contrast | Main opposite-alias design and completed branch production `86e77db` | Preserve the coordinate; pair it with a stronger independent matching-odd source before a third alias | H-H4-H12-ORDINARY-CONTRAST | moderate -> high |
| Gauge-free cross-microscopic control | Existing triangular Phase B sufficient statistics | Normalized top statistic plus typed square row at two moduli | H-CROSS-MICRO-JORDAN | moderate |

## Reinterpretation rules

- Exact contact closure reclassifies `gamma_D4`; it does not negate the observed finite response or the utility of the marked stream.
- The completed mean-line and two-radius local tests reject their declared q2 carriers; they do not reject ordinary global H4, external Euler coupling, charged four-leg or every separated landing observer.
- The Euler occupation clock closes only `sigma(n_occ)+span(J_S)` sufficiency in its declared metric. Its surviving residual is not automatically a second microscopic source, Q4 field or exponent, and all decompositions remain one N325/N425 dependency group.
- Annulus R1 failure is sector-specific. It does not imply that every plus/minus row needs the same rank-two generator.
- The first shared-context rectangle supports one effective generator only for its local pivotal rows. P200 and the rectangle still lack `h0,h2,h5,h25` or both path orders, so they do not identify chronology.
- Descriptor `migration_required` means future semantic transport is not yet machine-checked. It is not an error label, evidence demotion or reason to block a mathematically distinct analysis.
- Fixed-rank motif contrasts are exact-zero controls, not explanations of `p_c` or proof of useful variance reduction.
- Main's rank-four mixing audit rejects an assumed amplitude closure, not the exact parity zero pattern or the four powers.
- P321's fresh pass is compatibility, not thermal-Q4 identification. Exact children require two closure modules and show regular Q differentiation is semisimple. P333 has now rejected the minimal scalar detach and one-terminal-mark quotients at widths 3/4; the live construction is typed multi-mark/direct-sum connectivity with a source derived from the full Q-family, while P321 remains an independent modulus coordinate.
- A raw Q tangent is not defined by its Q=1 endpoint alone. P333's lift counterterm is exact transport data, not Jordan evidence before bottom-field projection and field connection.
- P250's combined gate rejects fixed bridges, endpoint rank<=7 and the rank-eight R2 plane. Exact endpoint abelianization and fixed-toggle obstruction show that neither more endpoint words nor natural fixed overwrites can measure order. The next discriminator is an adaptive cut/connector with retained selected support and intermediate typed states.
- P337's state-current prism now gives the exact fixed-p identity `E_top'=J12-J01`; `J02` is not a third additive E component. N170 resolves reinforcing J01/J12 and N340 localizes the resolved contribution to completion. The live existing-data analysis is the midpoint composition/hazard split, separate component recurrences and age-standardized `mark12_h4`; a fifth generation is not automatically informative.
- `DevEnvC_HZsCM6` is historical provenance for committed P337 artifacts only. Never connect to, inspect, stop, resume or retrieve from it. N1360 has no authorization or running production.
- P333's signed connected-history `K` is a Gram-compatible Jordan positive control, not a positive stochastic transfer or LCFT field. Width-4 charge-one failure identifies charge two as the minimum untested irrep; it does not yet prove rooted history or the final physical dimension.
- Production-first elimination and the six-coordinate Operator Passport are parallel methods. Exact identities and complete production covariance precede optional interval/SOS strengthening; neither method is a permission gate, and missing or inconclusive coordinates are not negative evidence.
- P158's sample counts are a source-only power forecast, not N650/N850 observations. They lower current production value without forbidding a target variance pilot or future redesigned estimator.
- PRs #394--#396 complete the bounded family's 27 exact reliability polynomials, vulnerability census and abstract planarity classification (26 planar, K5 nonplanar). These are shared exact controls, not 27 independent threshold claims. Terminal cofaciality, a disk-relative dual state, periodic embedding and a comparison theorem remain open.
- P334's production age slope survives recorded birth marks, all-one `iota`, the birth-site Smith class and two acquired current-geometry proxy families. This removes those summaries as sufficient explanations, not intrinsic memory. The next path-law coordinate is checkpoint-clustered cooperative continuation with two future orders; mixed arbitrary-HNF orbit Hall and BA remain a separate exact lane.
- The global E_top comparison rejects one universal cross-geometry A/E ray relative to four lineage rays, not H4 or a modular law. Cross `tau` with the discrete topology map before assigning one operator story.
- Issue55 production is now complete: H4-only and zero both survive and A12 is unresolved. This lowers the value of the third alias under the same weak source; it does not rule out H12 under a stronger source.
- A lower-ranked route stays open and can immediately rise when it offers a sharper observer, exact map or shared acquisition.

## Team handoff

Every new result should state `state/source/observer/geometry/acquisition`, lifecycle status, immutable source commit, dependency group, exact model pair separated, and the next observation that would change the conclusion. “More samples” is not a next target unless the current uncertainty, rather than model aliasing or missing semantics, is the decision bottleneck.
