# Scientific Frontier Map

**Updated:** 2026-09-01

This map is the repository's scientific coordinate system. It is not a permission system: exact work, reanalysis, pilots, production and independent theory may proceed in parallel. Priority moves attention; it does not lock, close, veto or demote a task. `STATUS` owns claim/lifecycle language, `ROADMAP` preserves rationale, and [NEXT-TARGETS](NEXT-TARGETS.md) owns the single ranked decision queue.

**Current frontier: canonical joint Q activation reaches original U,
with a nonzero contribution beyond nearest neighbours.** The
[completed J2 result](../results/regular-pair-joint-u/REPORT.md),
`open_pr #267` at `f8e30859f05e86ef35d257fc900f97e74f41e21c`, gives
total `−.0055194314248394015`, NN `−.001751074454402799` and nonNN
`−.0037683569704366022`; all exact enclosures exclude zero.
Source: fixed Kreg=K2+K0, epsilon/N at each vacant vertex, joint physical
contractions. Observer: original moving-root/slope `∂logQ∂epsilon²U`.
Geometry: the fixed N25 axis/tilted pair. Acquisition: one 4.63-second
joint-moment run, 2^24 origin-vacant states per geometry with full 2^25
normalization; the old exact population is not an independent evidence block.
Execution `410015f5505dc2d8ca0e9ac904f656a4adc9fe86`, `branch_only`,
[reproduces total/NN/nonNN exactly](https://github.com/LightChainr/Matching-One/blob/410015f5505dc2d8ca0e9ac904f656a4adc9fe86/notes/regular-pair-joint-transmission-result.md)
with another complete 2^25 implementation and adds adjacent-kernel plus
coordinate-invariance arguments. It is an implementation cross-check on
the same populations, not an independent evidence block.
Global additive first-Q and NN-only global closures are excluded; all
finite-range contact mechanisms and unique field identification are not.

**The distinct noncontact spatial decision is also complete.** Execution
`a237968f1d7a82d26b46e83c58179dbba7f1a908`, `branch_only`,
[completes the C64/r16 decision](https://github.com/LightChainr/Matching-One/blob/a237968f1d7a82d26b46e83c58179dbba7f1a908/notes/regular-pair-spatial-transmission-result.md):
`C32/r8=3.6591796875e−5`, `C64/r16=6.85546875e−6`; the latter's
99% interval `[5.20339728e−6,8.50754022e−6]` excludes zero.
Source: two fixed canonical vacant-site tensors with per-vertex units.
Observer: connected joint colour Q susceptibility, not q/E-root U.
Geometry: L32/r8 and L64/r16 at the prescribed sampling parameter.
Acquisition: two separately seeded 200k configuration blocks; the 32
translation/direction pairs inside each configuration are correlated.

The exact [shared-component mechanism](https://github.com/LightChainr/Matching-One/blob/a237968f1d7a82d26b46e83c58179dbba7f1a908/notes/regular-pair-spatial-kernel.md)
gives g=0 for s<=1 and a signed contrast product at s=2. All observed
nonzero entries are s=2; rare s=3/4 contributions are not excluded.
Neither the positive mean nor its size/distance ratio identifies a
continuum field or turns the conditional Gram bound into a global-U bound.
The [PR #509 selection rule at `baa5d33b`](https://github.com/LightChainr/Matching-One/blob/baa5d33b2f87b2868aa0cb9d3f6518c93dbf3bff/experiments/p337-regular-spatial-support-20260901/RESULT.md)
makes the support condition exact
for nonadjacent pairs: at most one shared exterior occupied component gives
zero first-Q activation, while
`|Cxy|≤(43/16)Pr(vacant endpoints and at least two shared components)`.
An actual s=2 witness has `a=1/16`. This bounds the spatial colour
susceptibility's support, not original U numerically or a decay exponent.

The earlier execution `2ba8863f75e0ced211b7b5442e8cddbe2fbd3deb`, `branch_only`,
delivers [W_Q=−.04503611397592696](https://github.com/LightChainr/Matching-One/blob/2ba8863f75e0ced211b7b5442e8cddbe2fbd3deb/results/p337-regular-pair-activation/score/REPORT.md)
for `∂logQ∂epsilon U`, with direct Q1 epsilon response exactly zero.
Source: the specified regular K2+K0, not the old pure-K2 occupation tangent.
Observer: complete original moving-root/slope U. Geometry: the fixed N25
axis/tilted pair. Acquisition: one completed fixed-source exact score,
0.244 seconds with imported root; no independent stochastic evidence.

Two new results at `open_pr #267`
`21563da4b0cf721a2aa512901f6ffc966ffa8384` restrict the mechanism space.
The [one-site theorem](../notes/regular-one-site-q1-thermal-quotient.md)
covers entry-regular homogeneous original-binary-site tensors, including
vacant and occupied perturbations: Q1 has only two scalar weights, and
the common Bernoulli parameter cancels from U. A regular completion in
that class cannot preserve old direct V. The [activated Gram proof](../notes/regular-pair-counterterm-gram.md)
and [result](../results/regular-pair-counterterm/REPORT.md) give
`3/2+(alpha−1/2)²/2 >= 3/2` for identical real counterterms in the fixed
K2+c(Q)K0 family. This is a shared-four-line conditional interaction,
not an occupation-summed global-U bound or a universal norm.

The [next mechanism comparison](../notes/regular-pair-joint-size-decision.md)
keeps canonical Kreg fixed and projects the bilocal kernel onto one fixed
macroscopic window. Under its single-field assumptions,
`T_N=N²J2_macro~N^(29/8−x)`: `N→4N` predicts `2^(−5/4)` for
`x=17/4` and `2^(−13/4)` for `x=21/4`; J2_macro itself predicts
`2^(−21/4)` and `2^(−29/4)`. This window projection is not the
unfiltered second derivative of a homogeneous one-coupling family, and
the contrast is not an exponent measurement or field identification.
[Next Targets](NEXT-TARGETS.md) owns acquisition. Its one predeclared
secondary split may separate `s=2` from `s≥3` support inside the same
readout; the dependent parts are not new evidence or a descriptor menu. The
[completed joint-U calculation](../notes/regular-pair-joint-u-result.md)
uses the physical pair contraction, not covariance of single-mark scores.
Its negative J2 is neither the positive spatial C64 nor the conditional
Gram bound. NonNN on N25 can still be microscopically close: no automatic
K3, alpha search or continuum assignment follows from this result.
The bounded old occupation tangent's [W_N=N V_av, ratio 2 versus 1](../notes/local-pair-size-response-predictions.md)
remains a separate conditional comparison, not a scaling law for W_Q/J2.
The no-go does not cover specified singular completions or multi-site
vertices; these are distinct mechanisms, not a regular one-site rescue.

<details>
<summary>Completed pure-pair, stable seam and weak-Q predecessors</summary>

Execution's `branch_only` [pure-K2 result, `923f66b9`](https://github.com/LightChainr/Matching-One/blob/923f66b979a6b6132875f783106c041ed3c0c1a9/notes/local-four-port-transmission-result.md)
has `V_av=+.0018155512845251097` and proved local≠full seam.
[Fixed-cut recoupling](../notes/local-pair-crossing-sector-resolution.md)
activates all four H⊗H blocks; bare singlet/standard cancellation is not
RG thermal orthogonality. The [L17 physical two-hole obstruction](../notes/local-pair-two-insertion-obstruction.md),
`5864de49d19898e505e6aecc316b9cb824712c70`, has Gram residue 1/2 and
conditional mixed-log residue `1/[2(1+v_x)(1+v_y)]`. It excludes the
unrenormalized all-exterior regular family, not linear V or a necessarily
divergent homogeneous partition. Its recorded symbolic run took about
.114 seconds, with no MC, cloud work or old-score rerun. New no-go/Gram
definition/script pin: `c4e4a821ab8adfc2d628b4cd65b4ce1d52535b56`.

**The stable N25 full-central trace has a nonzero Q1 normalization
response and mixed Q derivative.** The [completed result](../results/n25-stable-colour-q1/REPORT.md),
`open_pr #267` at `5c1f9d3b7971a41d07db3c9fa4ac86529c90c199`, gives
`B1=−.001904836180602413` and `B1_logQ=+.005036496028411871`,
both with zero excluded. State: packing restricts essential components and
winding to `c|u|≤2`. Source: the fixed full stable central coefficient
beta(Q), not a fitted local source. Observer: original pooled-root/slope U;
the second response is `∂logQ∂epsilon U`, not total `∂logQ U`.
Geometry: the same N25 pair. Acquisition: 6.759-second old-count/saved-root
rational reduction, no new enumeration, MC, root search or tests.
The [packing theorem and explicit completion](../notes/n25-stable-colour-completion.md)
make the existing marks sufficient; the positive Q4 J22 remains the
`branch_only` [earlier score, `54352b2e`](https://github.com/LightChainr/Matching-One/blob/54352b2eefa651ca482ca84837053c792e82c71e/results/p337-s4-trace-transmission/score/score.json).
These dependent finite results do not identify one local continuum field
or prove a crossing on a common Q1-to-Q4 regular-root branch.

The [specialization comparison](../results/colour-specialization-gap/REPORT.md),
`open_pr #267` at `ef7c3f58b9bbe2e21b1b4a9f5d8f57688ef121c2`, adds
one 0.009302-second exact class sum on three specified physical closures.
Their Q4/Q5 multiplicities are `1/1`, `5/6`, `−1/0`: dimension-only
continuation fails for the full closure family. The counterexamples use
N36/N49 patterns, not an assertion about support of the scored N25 packet.
This is algebraic model elimination, not independent statistical evidence.
N25 packing excludes those exceptional patterns, so its stable-Q1 score
is complete. The subsequent local construction and its separation from
that full stable central trace are also complete.
The `branch_only`
[finite closure, `4a4390f2`](https://github.com/LightChainr/Matching-One/blob/4a4390f2aeff3e79855fb11ef1185ba52c80a43a/notes/closed-source-finite-torus-pair-closure.md)
and [removable interface, `f43b3674`](https://github.com/LightChainr/Matching-One/blob/f43b3674ce29e12629dd790bcbb7370abc5cefbc/notes/closed-source-removable-twist-jet-interface.md)
are completed inputs, not outstanding first-interface or Q-scan requests.

The [weak-Q path comparison](../results/weak-q-path-comparison/REPORT.md),
`e87d5de2d0bc83ac8e5c12af83a337b829c3323b`, retains the fixed B control:
`J_tied=+.06308268170708457`, `J_site=−.2698280267134874`,
`V_B=+.33291070842057197`. The [regular-endpoint zero](../notes/weak-q-paths-and-regular-selection.md)
still excludes activation within that endpoint family. The earlier finite-Q4
interface used the `branch_only` [hypergraph/twist realization, `977fea92`](https://github.com/LightChainr/Matching-One/blob/977fea9272c780aea19cc47f8d33324c28a1293e/notes/closed-source-hypergraph-rc-twist-projection.md);
the new N25 stable completion supplies its distinct Q1 packet without
equating either full colour trace with a unique local continuum field.

</details>

The independent #154/P334 decisions and one-hole split remain complete;
[Decision Experiments](DECISION-EXPERIMENTS.md) owns their unchanged states.
The Sstar/Sdrop N25 finite-coupling half-line is now complete as well:
`Ustar<0<Udrop` for all real m≥64. Separate fixed-m and joint-limit results
are mapped below; the canonical interaction's N25 J2 and nonNN response
are complete. The primary local target now is the fixed-window `T_N`
comparison above, with source, projection and two conditional predictions
held fixed before acquisition.
Other lines remain open without Issue locks or automatic feature expansion.

**Measured microscopic-to-global separation, Draft #267 `5483aa82047df34c2c9508d4930b6e20961ed9ca`:**
[the exact cycle/rank split](../results/decimation-cycle-rank/REPORT.md) gives
`V_Sstar=.12616536341416915`, `V_q=.05324753511465212`, and
`2V_beta_null=.07291782829951701`, with zero excluded by rational bounds.
The source is fixed by execution's `branch_only` [action dictionary,
`0d19179f`](https://github.com/LightChainr/Matching-One/blob/0d19179f6c6c36fdbb34b2d93e35a9d5fe10dad3/notes/decimation-closed-cluster-gas-action.md):
`Sstar=C+F4+Bvac=2*beta_null+q−3K+2N+2`. State: occupied graph cycle
space and its ambient-zero kernel, distinct from cellular holes. Observer:
the original common-root, slope-normalized U. Geometry: N25
Gaussian(5,0)/(4,3), different Smith classes. Acquisition: .281-second
reuse of exact coefficients, no new enumeration or random samples.

The `branch_only` [complete-source result, `ec01768f`](https://github.com/LightChainr/Matching-One/blob/ec01768f520e85f1acfd9d3fde9bcf855477254e/results/p337-closed-source-n25/REPORT.md)
already measures Sstar and `V_Bvac=.33291070842057197`; the earlier
[F4 result](../results/decimation-plaquette-u/score/REPORT.md), `b8d043fc`,
and [endpoint closure](../notes/decimation-closed-source-and-global-u.md)
remain completed inputs. The new separation excludes the explicit unit-q
alias, not arbitrary fitted rank sources or within-K/rank residual closure.

**Homogeneous N50 finite transmission, `open_pr #509`, `ef3b2c68`:**
[the completed exact result](https://github.com/LightChainr/Matching-One/blob/ef3b2c68f824e29421747c805ea7a505aca41908/experiments/p337-homogeneous-n50-20260831/RESULT.md)
has `U=1.0615603876876551` and `V=+.0543457826695583`, excluding finite
`V=0`. State/source/observer: the unchanged homogeneous N50 Sstar and
original pooled-root/slope U. Geometry: `(5,5)/(1,7)`. Acquisition: full
merged 2^50 populations for both geometries, about 49.85 CPU seconds.
This closes its finite contract without an automatic N100 extension,
scale law or field confirmation.

The distinct `branch_only` [one-hole result, `f5c4a74a`](https://github.com/LightChainr/Matching-One/blob/f5c4a74a20bad8589c39e1034cfb209462110dbe/results/p337-endpoint-defect/score/REPORT.md)
now measures interior first-jet transport on the N50 parents: actual
`U_st=10.755718407564073` versus source-independent gain`.5313680267777353`.
The hard-endpoint identity survives. [The exact defect interpretation,
`bc17b81d`](https://github.com/LightChainr/Matching-One/blob/bc17b81d502fb1ca3323f5c20f63c544bb31602d/notes/checkerboard-single-defect-global-u-result.md)
has `DeltaSstar=3−2k_null−ell`, `ell≤1`; only alternating child faces can
change rank. The [completed insertion split](../results/defect-reweight/REPORT.md),
`open_pr #267`, `e1b968959634b9b3999c727b83ed38d0b730cb20`, measures
baseline reweighting `+4.550327123236791` opposing weighted observable jump
`−15.306045530800864`; these add to the prior total Xi. The prescribed zero
prediction fails by exact bounds. Reweighting's covariance can contain both
rank types; it is not a rank-preserving population share or a new source.
Acquisition: one-eighth alternating subset (2^22 per geometry),2.036 seconds,
no random samples or root search. This is a dependent decomposition of
the N50 one-hole model, not a complete finite-interior law. Execution's
`branch_only` [matching result, `9057325d`](https://github.com/LightChainr/Matching-One/blob/9057325d86d54a8a909ebf4f0d5b15ae7e40f1a5/notes/checkerboard-defect-reweighting-decision.md)
uses `U_st=−Xi`; it shares the same finite population and is not another vote.

**Two fixed microscopic laws, opposite global-U tails:**
the [new exact readout](../results/projection-drop-tail/REPORT.md), `open_pr #267`,
`fbbaa2aacce9895f0113bfc12a70e37152aec507`, gives N25
`U_drop/A_N ~ +(625/384) lambda^(42/5)` against the original negative
`−(625/1152) lambda^11`. State: minimal winding stripes in the occupied graph.
Source: `Sstar=C+F4+Bvac` versus the already specified `Sdrop=Sstar+r`.
Observer: unchanged separately normalized pooled-root/slope U, evaluated
on each law's own root. Geometry: axis L×L plus same-area companion with
ell1≥L+2, L≥5, Delta>0. Acquisition: 36.6-millisecond exact reuse of N25
integer `(K,g,q)` histograms, no enumeration, randomness or root search.

The [proof and size table](../notes/topological-projection-reverses-global-u-tail.md)
give unmeasured N100/N225 predictions. Execution's later `branch_only`
[uniform theorem, `85d5e44b`](https://github.com/LightChainr/Matching-One/blob/85d5e44ba8aed471470373f972c670dc7c82bdcf/notes/closed-source-uniform-projection-tail.md)
now establishes `Ustar<0<Udrop` on the whole N25 real-m≥64 half-line.
This is a completed finite-coupling comparison, not a new size measurement.
At `branch_only` snapshot `2690f665bc8029cb2370d3f1efcef5eb2853705c`,
[axis Peierls](https://github.com/LightChainr/Matching-One/blob/2690f665bc8029cb2370d3f1efcef5eb2853705c/notes/closed-source-fixed-coupling-peierls.md)
proves fixed-integer-m≥256 winding suppression for 4|L, L≥16, uniformly
in activity. [Poisson double-scaling](https://github.com/LightChainr/Matching-One/blob/2690f665bc8029cb2370d3f1efcef5eb2853705c/notes/closed-source-poisson-double-scaling.md)
closes original pooled-U superpolynomial suppression, including oblique
geometry, only with growing systole and `N/m²→ζ<∞`. For fixed m the
remaining oblique quantities are [order-25 twist penalty Δk](https://github.com/LightChainr/Matching-One/blob/2690f665bc8029cb2370d3f1efcef5eb2853705c/notes/closed-source-oblique-twist-comparison.md)
and [restricted-sector odds mismatch d](https://github.com/LightChainr/Matching-One/blob/2690f665bc8029cb2370d3f1efcef5eb2853705c/notes/closed-source-pooled-sector-odds-bound.md).
These different limit orders are not interchangeable or independent votes.

**Earlier finite-coupling turnover is resolved:** `branch_only`
[`a70eeff0:REPORT.md`](https://github.com/LightChainr/Matching-One/blob/a70eeff09f51ce2fa0fea5ae637e9191efbf2e1f/results/p337-closed-source-finite-coupling/score/REPORT.md)
finds negative U_t at all prescribed m=2,4,8,16 after positive U_t(0),
excluding monotone amplification on N25. The same exact populations supply
the new tail readout; this is not independent evidence. The later winding
classification sharpens the earlier two-state `U→0` bound. Neither a fitted
decay exponent nor extra points near the resolved peak are needed next.

**Different acquisition, unresolved larger-N result:** `branch_only`
[F4 independent80M block, `25ca3635`](https://github.com/LightChainr/Matching-One/blob/25ca3635ea64655923c32adee4b62d683579cdcd/results/p337-f4-transmission-20260831/scored/REPORT.md)
has20M/100 batches at each N65/85/130/170. All four simultaneous intervals
contain zero; none lies wholly inside±.5. `NOT_EXCLUDED` and
`INCONCLUSIVE_STOP_FIXED_BLOCK_WITHOUT_TOP_UP` remain its decisions.
Finite N25 positivity is not larger-N confirmation or a continuum field;
these sources also remain distinct from the completed P154/P334 interventions.

**P334 source-normal response survives an independent finite intervention:**
execution `branch_only`, score `1164ba91035bb0ee37bcea52f700312475c257b5`,
[result `d0a9daf1132779205f119e9b4470f4eea9cb89c1`](https://github.com/LightChainr/Matching-One/blob/d0a9daf1132779205f119e9b4470f4eea9cb89c1/notes/p334-independent-normal-intervention-result.md).
With1M fresh prefixes, exact paired policies preserve safe-class masses,
immediate rank/Euler and both original score means. T=(3.0852005663±.3918738407)e−8
has3SE interval[1.9095790443,4.2608220883]e−8 above δ=1e−8: complete
two-score conditional-label mean closure fails, while the old3.6565e−8
forecast survives. This is stronger than a first-Jacobian comparison, which
is not itself excluded, and does not identify the global H4 field.

**A different P334 prediction fails on600k other fresh prefixes:**
`open_pr #509`, [first-order H-source residual projection at `14b2c98ed3a252a2fe79ce5e124d9484b23a264f`](https://github.com/LightChainr/Matching-One/blob/14b2c98ed3a252a2fe79ce5e124d9484b23a264f/experiments/p334-prospective-intervention-20260831/REPORT.md),
gives Rnew/Rold=.4988857 [.4360616,.5617098] /.5169035 [.4506760,.5831311].
Both frozen C0=[−.25,.25] and C1=[.75,1.25] bands fail. These ratios condition
on old training point values: no true-population halving or fitted half-amplitude
replacement follows. Its source, target and new dependency group differ from
the1M source-normal T experiment, so the two outcomes are not conflicting or
additive votes on one model.

The [exact crosswalk, `branch_only` `ccabada318b1eeb12ae28d53391b13ab44c116d2`](https://github.com/LightChainr/Matching-One/blob/ccabada318b1eeb12ae28d53391b13ab44c116d2/notes/p334-independent-interventions-crosswalk.md)
places first-score responses in `G b_F` and normal responses in the
orthogonal `r_F` of `m_F^c=H^T b_F+r_F`. Its algebra explains coexistence;
it does not identify a common effect, a new field or a complete mechanism.

**#154 source→channel→U has an independent finite decision, `open_pr #509` (Draft):**
[`f4999e29612da16a3650f24d124fb59137f053d7:REPORT.md`](https://github.com/LightChainr/Matching-One/blob/f4999e29612da16a3650f24d124fb59137f053d7/experiments/p154-prospective-transmission-20260831/REPORT.md)
delivers5M new N85 plus160M new N340 permutations. Both entry-dominant B and
completion-dominant C fail. Four simultaneous channel intervals are inside±.30;
net intervals[−.071640,.158581] /[−.157394,.278745] are inside±.50.
W remains `not_excluded`. All channels share the complete source's root/slope
motion and sum to original U. The declared lag1 policy leaves primary H4
attention at this finite resolution; no extra samples, changed lag/source or
rescue template follows. Other temporal sources and all-size H4 remain separate
questions, not rejected by these bounds.

The [completed M10/M11 secondary, `branch_only` `612df8ec1cbe3be3938ee2e1f6183a1aefc6510b`](https://github.com/LightChainr/Matching-One/blob/612df8ec1cbe3be3938ee2e1f6183a1aefc6510b/notes/p154-clock-line-secondary-result.md)
has all four simultaneous residual intervals containing zero. Both lines remain
`not_excluded`, neither is identified, and their joint covariance retains the
same fresh primary data. Outcome-blind secondary registration is distinct from
the pre-production primary freeze. High baseline sensitivity does not establish
source occupancy or rescue a failed dominant-channel prediction.

**Earlier #154 concrete model elimination**, `open_pr #267`, `3847a5cf`:
[pure cos4 relative birth-clock shifts](../results/p154-fixed-clock-models/REPORT.md)
fail the fixed entry/completion directional predictions by31–35 paired SE.
Scalar shifts remain compatible at the root but predict tiny U responses,
with [calibration uncertainty](../results/p154-clock-transmission-budget/REPORT.md)
covering zero. State: two activation clocks; source: lag1 rank-centered bulk
cluster count; observer: entry/completion versus original root-comoving U;
geometry: original N260/N340 direction pairs; acquisition: old-data model
comparison. This is neither a new field identity nor prospective confirmation.

**New norm4 temporal measurement, `open_pr #267`, `4daae57eef5c945aa050a95cd3d5d5d77582161b`:**
[A source centered within previous occupancy/rank](../results/norm4-lagged-source/REPORT.md)
changes the next activation and moving-root rank1 probability (−.008817 to−.002971,
15.5–37.8 SE). The N260/N340 original-U H4 derivatives remain+.37897±.72219 /
+1.27653±1.18227. T01/T12 and direct0→2 kernels are measured with96-dimensional
covariance from the original three groups. This one-step-before-readout injection
for each K is distinct from equilibrium final-configuration fugacity. These
discovery targets have now led to the completed independent finite test above;
they do not make a first-lag task or another campaign pending.

**Parallel P398 result, `open_pr #267`, `58db48dd7b6257930ee5e704a37e438c0cf2faf1`:**
[Primal/dual profiles](../results/p398-stationary-arrangement/REPORT.md) explain
99.7344% of stationary-score variance but fail exact transition closure in32/60
classes; a two-versus-four detach witness identifies missing incidence information.
The.2656% arrangement residual contributes10.17% of the final+− integral.
Incidence prediction remains parallel exploratory work, not a third default P0.
This finite state/source, P334's source policy and norm4's site protocol are
different objects; their resemblance neither pools evidence nor identifies a CFT field.

**Completed joint-clock coordinate, P334 `branch_only`:**
[`a6304bad`](https://github.com/LightChainr/Matching-One/blob/a6304bad15214cac841b76f41e7b61ac61838786/notes/p334-euler-thermal-dipole.md)
and [`e2ef9983`](https://github.com/LightChainr/Matching-One/blob/e2ef9983f426890a299f5a6e1a2eba8b6d072855/results/p334-euler-dipole-connected-clock/REPORT.md)
complete the thermal-moment interpretation. Intrinsic `Cov(tau1,tau2)` still
changes after exact removal of the shared uniform-order clock; N425's
minus-source direction contrast is `−1.08880e−6±1.61897e−7`.
`Cov(C,W)=(Var(tau2)−Var(tau1))/2` instead describes marginal-width imbalance.
Center/spread cancellation explains a weak dipole, while positive-baseline,
lifetime-normalized plateau ratios resolve later/broader plus-source response.
These are distinct correlated coordinates, not extra independent evidence or
a field count. The subsequent hierarchy (`44dc9e33`/`2bc35294`, `branch_only`)
has already separated within-prefix covariance from prefix-mean transport:
96–98% of the signed response lies between prefix means, with a resolved
same-rank-cell residual. The original8 broader receiver-R0 projection captured
80–99% of signed loading (`90226598`). The [current card](https://github.com/LightChainr/Matching-One/blob/bc0a18c207e3b09f49ea6b6af6601471114d654a/notes/p334-prefix-response-projection-scientific-card.md)
adds completed new64-response/old8-clock reuse on original00 prefixes:
four-contact shares78.20–80.36%, leaving about20% with same-sign residuals.
The different populations are explicit; neither share is response R² or prediction.
PR #509 `7beb99ce` also completes targeted64: local A determinant means are
`1.15937e−8±2.14282e−9` / `1.49767e−8±1.97301e−9`; E/gap remains weak.
[The contact/local-rank handoff](../notes/p334-contact-and-local-rank-handoff-20260831.md)
records the preceding state. The later #509 `04743caf` delivers held-out
`J=BG` (A risk reduction38–39%, center54–59%) and nonzero cell00
center-variance response, detailed in [Status](STATUS.md). These refine an
approximate local-mean prediction and challenge deterministic conditional
translation respectively. They remain old-prefix discovery and do not directly
judge the frozen source-normal complete-mean closure above.

The preceding discovery views retain the original twenty batches per N325/N425,
separate from both fresh-prefix experiments above. Their common covariance
extends `b582015e`→`e2ef9983`→`ce20158a` (20,754 coordinates per N),
then the completed contact join `172fbeb1ed28019b04f132859da3ea247942bb1c`.
Contact's726 raw/358 derived readout and new64 cross-stream reuse are delivered;
new64's standalone20×1200 factor is not claimed as a completed global merge.
New64 adds conditional suffixes on the same3053 prefixes, not an independent population.

## The five axes

**New source–observer connection, Draft #267:** [conditional primitive-line O4 is now measured](../results/norm4-source-line/REPORT.md) on all six sizes. Its complex response inside rank1 rejects the common E-plus-clock prediction, with moving-root transport included (four-real chi²=1049.13–8479.76). A post-analysis exact rotation to each torus frame leaves positive real residuals.088750–.110265 and imaginary magnitudes≤.0001164. This resolves a spatial source response beyond rank population; much of its laboratory-frame angular contrast follows the known frame rotation. It is not the original global U H4 observer or an energy-operator identification.

The preceding [million-mark endpoint result](../results/norm4-source-endpoint-1m/REPORT.md), execution `6bd46ad3`, separates three physical readouts. The common source moves the root by about+.029 per log Q; even at that moving root it reduces rank-1 probability by.09250–.09782 (73–160 SE); the H4 thermal U source response remains unresolved, now −5.9723±4.2736 /11.8594±9.1981 at N260/N340. A finite common thermal-clock shift cannot explain the rank-population response.

State: the three homology ranks plus within-sector cluster/occupancy structure and the rank1 winding line. Source: a common microscopic paired-cluster fugacity `s=CB+CW`, distinct from density `s/N` and from an assumed Potts/FK source. Observers: root position, root-comoving rank1, conditional physical-frame O4, and root/slope-normalized angular thermal U; their shared data do not create independent evidence votes. Geometry:65→130→260 and85→170→340 with the original HNF children. Acquisition:1M source/line-marked old permutations at the two endpoints,100k at each cyclic size, exact full-K Binomial integration and three dependency groups; the line replay reobserves2.4M old permutations with zero new Monte Carlo samples and saves396-dimensional covariance.

The q2/Jordan source-rigidity extensions (nominal p=.48573/.58735), common generator drift and earlier three-global-readout common q/E-plus-clock determinants remain unresolved; the conditional-line rejection concerns a different observer. [The completed two-phase estimator](../results/norm4-source-two-phase/REPORT.md) found no stable gain from the high-precision unmarked baselines. [The source quotient](../notes/p40-thermal-clock-source-quotient.md), [conditional O4 experiment](../results/norm4-source-line/REPORT.md) and [fixed-K decomposition](../results/norm4-source-line-fixed-k/REPORT.md) are measured. Fixed-K/rank1 spatial covariance remains strong (joint chi²=22534.76/24), beyond any source depending only on geometry-specific `K,q,E`. The soft `W±=R(1±Re[exp(-4i theta_period) O4])/2` bridge is completed at #509 `fb01c44a`, and the lagged-source kernels are measured. They do not themselves contain winding-contact marks. These are completed discovery stages; the subsequent independent165M result now bounds the declared lag1 global transmission, without prompting new contact features or a replacement lag.

The new [P40 even tangent](../results/p40-even-given-odd/REPORT.md)
connects source and observer explicitly: absolute-cluster fugacity changes
rank-1 probability even at fixed matching mean (raw C positive at110–138 SE).
This distinguishes a pure-q tangent in the finite marginal, not a field count.
Raw directional C is unresolved; the full-compensated auxiliary p=.04911
retains its correlated-view and geometry-adapted-source boundary.
The [original norm-4 interface](../notes/p40-even-response-norm4-interface.md)
requires the source derivative of root/slope-normalized thermal U, not bare C.

The preceding [P40 production q readout](../results/p40-absolute-cluster/REPORT.md)
now distinguishes **global source response** from **directional response**:
q couples strongly to the raw and separately compensated cluster source,
but their H4 difference is unresolved. The saved common-label Gram exposes
cancelling readout/source terms; that allocation depends on the coupling and
is not a basis-independent field decomposition. The new E_top/source
supplement above is a distinct measurement, not a square of the q response.

The [e increment](../notes/frontier-increment-20260831e.md) sharpens two
more coordinates: unmarked uniform blockade means close on the full clock,
whereas spatial conditional variance sees final-site collisions; complete
instantaneous current visibility does not close reversible hidden geometry
transport. These are physical observer distinctions, not new task gates.

The [absolute-cluster response](../results/p154-absolute-cluster/REPORT.md)
now separates **source variation** from **readout coupling**: `cb+cw` is not
removed by the declared clock/Euler span, yet its E_top H4 response is
unresolved at20k. The three-state q/E readout still sees this source only
through `E[S|q]`; nontrivial microscopic variation is not a count of fields.
This new-mark replay is an `open_pr #267` result using an existing dependency
block, not new Monte Carlo evidence. The high-statistics P40 q-only check
above is complete, as is the subsequent E_top supplement. Source responses
of the actual root-normalized thermal observable remain different information.

The [d frontier increment](../notes/frontier-increment-20260831d.md) extends
the **state** axis to complete147 continuation laws and irreducible triples,
the **observer** axis to actual conditional-noise and canonical-order results,
and the **generator** comparison to completed current deletion. The common
symmetric-kernel obstruction narrows the **rank-clock mechanism**, not the
frozen N900 width target. That [N900 target is now complete](../notes/frontier-increment-20260831f.md):
32M/800, Vz2.33946±.12039, both conditional predictions compatible with
correlated comparison errors. These are separately pinned completed results.

The [preceding context reconciliation](../notes/context-reconciliation-20260831c.md)
recovers completed N400 production and intrinsic rank-clock broadening,
width8 memory/motif decomposition, full physical/canonical birth clocks,
and the tiny VJS three-term derivative. These replace several stale first-work
requests. The [Issue/PR refresh](ISSUE-PR-INCREMENT-20260831c.md) records
integration separately; no new calculation is made by this map update.

The [preceding continuation](../notes/research-output-update-20260831b.md) makes
the observer axis concrete: N100 central A area and E dipole are resolved
even though the complementary moments are weak; P398's C0-only landing
innovation exposes a77.98% two-mode omission hidden by .90% matrix error.
Open PR #491 also supplies an update-closed finite cut network, not a bounded
continuum state. All three changes consume completed inputs rather than
create a new preparation layer.

Two new observations sharpen these axes. Draft `c0880c2`'s
[local-source replay](../results/p154-fixed-k-interaction/REPORT.md) separates
an occupation-count thermal-second-derivative contribution from fixed-K
arrangement response, but neither is resolved at the inherited 20k. Draft
`8f7a587`'s [width-five response](../results/p398-physical-two-point/REPORT.md)
has exact propagation rank8 while its [two slow modes](../results/p398-physical-two-point/mode-visibility.md)
approximate the whole matrix within .90% at d=1. Thus source independence,
exact state dimension, useful slow-mode compression and continuum field count
must remain distinct. The width-four `dbd4081` continuous-kernel fingerprint
is a separate finite-model statement, not a universal identification.

Every comparison must name its state, source, observer, geometry and acquisition. Shared exponents, harmonics or lattice sizes do not make two observers equivalent.

| Axis | Live coordinates | Boundary that must remain visible |
|---|---|---|
| **state** | finite ambient homology; rank-one lifetime and birth filtration; continuum Q4/Jordan or other module; low-dimensional transfer state; microscopic threshold structure | an exact finite state is not yet a continuum-field identification |
| **source** | Bernoulli thermal; absolute paired-cluster fugacity; rank-birth geometric tilt; join/detach rate tangent; Potts-Q measure; confluent projector; explicit field derivative; relative fugacity; deck charge; boundary source | absolute site-cluster source and the finite-generator rate tangent are not automatically Potts/FK; measure, projector and explicit-field Q derivatives are separate terms |
| **observer** | unmarked endpoint; rank-plane projector; `I01/I12` birth gate; matching-even/odd; line H4; landing H4; local pivotal; charged; boundary; cross-microscopic | line, landing, ordinary and charged rows are not interchangeable |
| **geometry** | Gaussian covers; annulus; modulus/Hecke children; boundary cross-ratio; triangular and square controls; finite transfer cylinder | a changed Smith class, modulus, path or marked context is part of the experiment |
| **acquisition** | existing-data reuse; exact construction; small targeted pilot; new production | correlated views of one stream remain one dependency group |

The typed Potts-Q interface is

```text
measure covariance
+ finite confluent-projector derivative
+ explicit bare-field derivative
+ boundary prefactor/gauge term where applicable.
```

## Production priority compass

**Source time is now measured.** The completed same-time angular bridge has
zero centered-source global response; the [one-step lagged protocol](../results/norm4-lagged-source/REPORT.md)
has nonzero future activation and moving-root rank1 response. Its original-U
directional H4 remained unresolved in discovery. The independent #154 result now
places all four entry/completion intervals inside±.30 and both net intervals
inside±.50 at N85/N340. Its completed clock-line secondary selects no clock. Signed T01/T12 kernels
remain discovery information, not interchangeable primary readout channels.
P334's completed immediate-rank-preserving interventions are distinct physical
protocols, not replications of this site-source experiment.

| Mechanism question | Current observation | Next separating observation |
|---|---|---|
| intervention-sensitive order versus spatial projection | P250 `0061e4e` plus Draft `eb29446` completes production and zero-sample reuse: pre-outcome state improves held-out MSE by 30.97% beyond the declared low-dimensional residue baseline, while complete terminal spectrum is absent and both residuals peak at `k=36` | preserve the state increment; pair full terminal periodograms with an orthogonal common-high-frequency readout |
| common spatial spectrum versus cross-radius deformation | [P418 normalized reuse](../results/p418-normalized-archive/REPORT.md) removes the old unequal-exposure common-mask rejection; common fits are stable and compatible. Radius5-only/sharing outputs are numerically unreliable, not new physical evidence | compare genuinely separating readout predictions of the normalized family; retain independent P250 state/rank and exact CRT results rather than replacing the old penalty with an unsupported mechanism |
| cooperative continuation: local dimension versus source/geometry mixture | Fresh1M source-normal T rejects complete two-score label-mean closure and retains the frozen forecast; separate fresh600k H-source residual ratios reject both their frozen bands. Old contact/local-rank views remain discovery | Both fixed decisions are completed inputs, with different sources and targets. Record the conditional scope of the600k ratios; no fitted half-amplitude rescue or duplicate intervention is the current default. [Decision record](DECISION-EXPERIMENTS.md) |
| completion hazard versus a new physical source | Risk/hazard and the F3/F5 sequence are complete. F5 `2d2a9ab` resolves W_line/JS response rank at N325/N425 (`149.93/4`, `246.93/4`); F3 lacked a second even contrast | transport the existing F5 rows across geometry and resolve matching loading; the first winding observer is not missing |
| modulus versus topology-map transport | PR #484 completes N1002M, N4008M and N90032M. N400 necessary density transport is unresolved, but96.895%±.507pp of broadening is intrinsic. N900 Vz2.33946±.12039 does not separate the two conditional width predictions | Make additional joint area/center/shape or physical-source predictions with shared covariance. No first N900 or de-smoothing task remains; two-point effective width is not an asymptotic exponent or scalar recovery |
| charged source versus observable overlap | P439 #451 plus Draft `8498d62` leave matching direct/plateau weak (`p=.79013`), despite separately resolved P337 F5 source-response rank | same-stream M loading and cross-geometry transport in an existing typed basis, not a first line observer or more K_A-only scales |
| finite module versus physical propagation | #267 `58db48dd` completes size/arrangement decomposition: paired sizes capture99.7344% static-score variance, but32/60 classes fail exact rates and the arrangement residual supplies10.17% of one integrated response | Parallel exploratory primal–dual incidence prediction, motivated by the two-versus-four detach witness; not a third default top-priority experiment or a site-field identification |
| common clock, finite rank population and temporal transmission | Independent165M result `f4999e29` excludes B/C; all four channel intervals lie within±.30 and both net intervals within±.50. W and the dependent M10/M11 restrictions remain `not_excluded` | Archive the finite decision and map its model scope. This lag1 policy no longer takes primary H4 attention; no additional samples, replacement lag/source or fitted rescue. Neither weak compatibility nor high baseline gain identifies a clock |

The earlier [P398 block-count result](../results/p398-block-count-measure/REPORT.md) (`7da1eeb0`) had fixed-score relative error34.27% and10.99% variance unexplained by any block-count function. [The completed profile hierarchy](../results/p398-stationary-arrangement/REPORT.md) now separates that information: sizes give strong static compression, while incidence remains necessary for exact transition rates. This is a finite-model mechanism, not a site-Matching field.

The [execution board](NEXT-TARGETS.md) remains the single ranked queue. The [decision register](DECISION-EXPERIMENTS.md) separates both completed P334 outcomes and the completed #154 primary/secondary results. The immediate work is the finite-model decision crosswalk, with no default new production or descriptor expansion. P398 incidence and other scientific lanes remain parallel exploration, without a third default P0. Full clocks, first lagged-source kernels, hierarchy, local A rank, tiny VJS and first F5/C3 observers are completed inputs, not another duplicate first calculation.

Semantic transport is deliberately **not a sixth scientific axis**. The main registry exhausts 200 legal observable descriptors and 952 exact affine maps. The historical `main@5ac456d` semantic-inventory snapshot recorded 59 = 28 direct typed entrypoints + 27 wrapper-covered kernels + 3 frontier/migration paths + 1 utility; the earlier Draft PR #267 snapshot recorded 65 = 28 + 27 + 9 + 1. These are not current totals after the new scientific scorers. The incorporated main snapshot is `e300609`; its newer exact controls add reusable support, not production evidence. Semantic migration follows useful analysis rather than delaying it. Historical mismatch/excluded states stay excluded, correlated retrospective blocks remain correlated, and implicit roots stay model-specific responses.

PR #339 adds `kappa_r=d_r/d_1^r` as an exact invariant of linear thermal-coordinate rescaling, together with a sparse Jacobian and delta-method covariance propagation. Its fixture is synthetic and the ratio still depends on observable amplitude and nonlinear thermal coordinates. It is therefore a transport primitive for the next typed shared-observable reanalysis, not evidence for H4, a Jordan partner or cross-lattice universality.

### Recovered completed controls

- P40 `291854a`, `analysis/p40_production_motif_projection_20260830.md`: actual 1M-per-pair N65/N85 motif/target covariance and OOF VR `1.363018/1.331388` are done. Exact zero means do not imply a 2x gain; the measured modest baseline is reusable.
- P218 `acd38cf`, `results/p218-production-coalescence/REPORT.md`: the actual ordinary/Jordan/generic coalescence comparison is complete but underpowered, not waiting for its first production adapter.
- P155 `c679c32`, `results/server-20260830/P155-general-period-thermal-null/REPORT.md`: injective R8 N260/N340 lambda z `.218/-.088` and near-thermal-parallel R4 are measured controls.
- P437 `386db0a`/`888af29`: the fixed-five-bond high-order support is measured; the earlier six-noise variance cost does not prove high orders unmeasurable.

All four are `branch_only` results and retain their original dependency groups. They inform a new physical discriminator; they are not a preparation queue.

## Four-lane operator tomography

These are decision matrices assembled from the five coordinates, not new ontology axes, permission gates or additive evidence grades. They are most informative in parallel: agreement sharpens an operator assignment; disagreement localizes mixing.

| Tomography lane | Matrix or frozen null | Current frontier | Next claim-bearing output |
|---|---|---|---|
| **activation boundary** | first-line character x birth/exit plus age, cooperative completion and collision | P334 cut-network explanation is now in open PR #491; P337 F5 source response is resolved, while population P334 directional loading remains unresolved | cut-covariant component/overlap response and transport/matching loading of existing F5 rows |
| **source rank** | observer x physically distinct source complex matrix | The old 2M Euler basis gave `4.279/4`, p `.370`; the later two-even-row F5 basis resolves W_line/JS rank at each size. The occupation residual is not itself a field | preserve the observer-specific contrast; test cross-geometry transport and M loading instead of repeating the old matrix or constructing the first winding observer |
| **modulus shape** | aspect ratio x frozen function shape after equal-area normalization | P321 remains E4-compatible; N100/N400/N900 topology and intrinsic rank-clock width are measured; P398 memory/motifs are complete. These are different observers | use additional joint shape/source predictions after completed N900, or changed-geometry/rate predictions from named P398 emissions; do not repeat first scale transport or equate these field tests |
| **typed charge-spatial** | charge/fusion channel x calibrated spatial phase or typed edge | Draft `eb29446` finds a 30.97% held-out pre-outcome state increment beyond a low-dimensional residue baseline; complete endpoint spectrum remains unobserved and both enriched residuals peak at `k=36` | preserve the state increment; default attention for the next block targets full terminal periodograms plus a common-high-frequency covector |

### Cross-cutting method lane: production-first model elimination

Issue #370, [`notes/proof-carrying-operator-passport.md`](../notes/proof-carrying-operator-passport.md) and [`notes/production-first-model-elimination-20260830.md`](../notes/production-first-model-elimination-20260830.md) define a method lane, not another scientific axis or a task gate. The five exact-rational fixtures and framework remain in `main@5ac456d`; branch `f5779b9` now completes the first real production-confidence adapter on eight high-statistics A/E rows. Physical M3c, full gauge coverage and interval/SOS are survivor-dependent layers, not reasons to delay a new observation.

The Matching-One applications are real: canonical E_top and #370 exclude declared images; P267's completed amplitude-only comparison rejects its four-coordinate model but not A/E alone; P154's actual radius-1 mixed row is not selected. P334's saved graphs and support/side allocation explain a selected scalar collision, P337 F5 resolves line-source response rank, and P439 matching loading remains weak. The central missing physical object is not another E_top score: it is an independent microscopic direction capable of testing the original norm-4 operator assignment.

## The main mechanism chain

The highest-information line is now a connected chain rather than a collection of H4 fits:

```text
Digital Alexander rank balance and cached index-2..13 frontier    [main]
    -> unrestricted rational four-sheet descent                   [branch_only]
    -> all-qL integral carrier saturation claim                   [branch_only review]
    -> essential H1 births K1,K2 and P0/P1/P2 reconstruction      [main]
    -> complement coordinates C (clock translation), W (lifetime) [branch_only]
    -> full finite K2 curve: positive area, three local node crossings [open_pr]
    -> positive translation + negative zero-area K2 deformation       [open_pr]
    -> E_top production elimination: pure odd/common line/fixed power fail [open_pr]
    -> P154 norm4 Phase-D A/E audit: six reinforcing K1-dominant rows  [open_pr]
       |- integrated energy = P4[S'] = P4[E_top']/2; mean pilot stops [branch_only]
       `- radius-1 mixed B/Jbulk pilot completes and is not selected      [open_pr]
    -> global ray fails; A+C survives; tau x topology-map interaction resolves [open_pr]
       `- amplitude-only four-vector excluded; explicit realizable model remains [open_pr/next]
    -> local gates I01,I12 and parity channels S,D                 [branch_only]
    -> production age predictor + two-time kernel + J12-J01 current [open_pr/branch_only]
       |- risk composition opposes H4; conditional hazard is same-sign    [open_pr]
       `- P429 100k nested fork + checkpoint H2 decomposition             [branch_only/open_pr]
          |- exact H2/b2-matched 2-star nonclosure and direction allocation [branch_only/open_pr]
          `- graph replay + selected support/side-capacity explanation    [branch_only/open_pr]
    -> typed theta/figure-eight bounded arm surgery and D_N scaling  [branch_only]
    -> marked-birth q2/P50 pilot with line and landing fields      [branch_only]
    -> mean-line q2 and local-UV q2 frozen targets reject          [branch_only]
    -> q-only contact closure and P275/Cartan reclassification     [branch_only]
    -> bulk external Euler-to-rank-birth response                  [branch_only]
    -> source-orthogonal J_D_perp coupling survives                [branch_only]
    -> operator tomography                                        [parallel]
         |- activation boundary: use completed cut/full-clock/channel-race laws for population direction loading
         |- source rank: old Euler basis blind; F5 rank resolved; matching loading remains
         |- modulus shape: AP/landing physical response completed; width/microstructure transport remains
         `- typed charge-spatial: retained state adds 31%; full spectrum and common k36 residual remain
    -> P205 ordinary H4/H4 K1/K2 prism                             [branch_only + PR #267 reuse]
    -> fixed-q shape/clock/lifetime + separated typed covector      [next]
```

### 1. Digital topology and essential births

On the declared regular square-cell class, Digital Alexander duality gives configurationwise

```text
r_black + r_white = 2,
q = r_black - 1 = A_top.
```

`main` contains the filtration oracle, essential-birth reconstruction and, after merged PR #330, exhaustive HNF checks through quotient index 13: 140 representatives and 101,140,028,118 weighted paths have zero birth/reflection/rank-sum/reconstruction/line failures; 285,020 cached subsets generate 500,805,335,024 plateau steps with maximum `iota=1` and no evolution. Honest and self-identifying faces are kept separate. This is exact finite regression, not an unrestricted theorem.

The `branch_only` parent `73d4960` uses the canonical four-sheet cover `T_(2L) -> T_L` and rational-homology descent to claim the same rank balance, reflections and two-activation identities for arbitrary nonsingular integer periods. Its child `c1a72e5` claims the integral refinement by classifying honest carrier images on every `qL` cover and choosing a prime coprime to any hypothetical downstairs Smith defect. The exact frontier has therefore changed shape: review component preservation under matching-diagonal pruning and the identity `H_up=H intersect qL`. If they hold, `iota=1` is theorem-driven and index 13 is a regression/counterexample alarm, not saturation evidence.

A second branch-only theorem proves that the unique honest-torus homological-balance root `p_L` converges to square-site `p_c`. This promotes the root from an empirical polynomial zero to a consistent topological coordinate, without supplying a rate. The next scaling theorem is a two-sided activation window around `p_c +/- lambda L^-3/4`, not another convergence check.

For a monotone site ordering,

```text
K1 = K_minus = first essential ambient-H1 birth,
K2 = K_plus  = second essential ambient-H1 birth,
P0 = 1-F1,  P1 = F1-F2,  P2 = F2,
M  = P2-P0 = -1+F1+F2.
```

The positive beta-density derivatives make the physical finite-volume matching root unique and simple. The neutral area is the mean rank-one lifetime. Existing endpoint histograms recover these marginals but do not contain the plateau line `ell`, Smith index `iota`, landing mark or same-sample global/local products.

### 2. Clock/lifetime coordinates and local birth parity

The paired activations have exact normalized coordinates

```text
C = (K1+K2)/(2N+2): complement-odd clock translation,
W = (K2-K1)/(N+1):  complement-even rank-one lifetime.
```

Across the ten archived direction pairs, the exact `Delta cos(4 theta)` normalization gives negative `C` and positive `W`. Those are correlated coordinates reconstructed from the same archives, not independent confirmations or fitted scaling exponents.

The Draft full-curve reuse sharpens the earlier three negative `K2` values at the pooled root. Its exact Bernstein-area identity gives the same positive integrated `K2` direction at all ten sizes. N=265/325/425 each have a delete-one-stable upper node near `.59462/.59635/.59694`, so the negative point value is a local lobe before a zero crossing rather than a reversal of the second activation as a whole. This finite node topology still cannot identify `ell`, `iota` or a continuum carrier.

The local insertion refines the two births:

```text
I01: rank 0 -> at least 1,
I12: rank at most 1 -> 2,
S = I01+I12 = Delta r and integrates to M',
D = I12-I01 = -partial_p P1.
```

A direct `0 -> 2` birth enters both gates, hence contributes `S=2` and `D=0`. Every nonzero `D` event has a canonical rank-one plateau line `ell`; `iota`, the projective line character `chi4(ell)` and the local landing H4 mark are additional typed fields. In particular,

```text
J_D4 = sum_v chi4(ell_v) (I12-I01)_v
```

is a complete matching-odd line-H4 observer. It is not the same object as landing H4.

### 3. The first finite bridge is a contact control, not a field identity

With `A_top=q=r-1`, the exact geometric tilt obeys

```text
partial_h <A_top>|h=0 = Cov(A_top, Re[e^(-4 i phi) J_D4]).
```

Tiny axis and Gaussian enumerations give nonzero real/complex couplings with exact complement sign controls. This closes a finite-lattice symmetry obstruction: the global rank projector can couple to the matching-odd line-birth source. It does not establish survival at large N, Q4 dominance or a Jordan logarithmic partner.

The missing stream has since been acquired and followed through three generations. It separates three effects:

- the completed N65/N130/N260 mean-line chain rejects both preregistered one-step negative-real `-2^-13/8` transfers; the apparently closed two-step phase is cancellation of incompatible edges, not a q2 carrier;
- a two-radius local landing-D4 UV annihilator also rejects its trained q2 transfer at held-out N130 (`chi-square=72.8633/2`), excluding that parameterization rather than every local H4 channel;
- the connected global-line response grows with birth mass and is dominated by contact/projective background rather than an independent field row.

The reason is exact. For `q in {-1,0,1}`,

```text
Cov(q,J_D)=J_S/2+(p-1/2-<q>)J_D,
Cov(q,J_S)=J_D/2+(p-1/2-<q>)J_S.
```

The all-order gate theorem extends this closure to every topology-only `f(q)` and every root-independent marked gate polynomial. A nine-geometry P275 production rejects the frozen global-line Q4/Jordan/H4 maps; its later Cartan decomposition places more than 99.8% of `Gamma` in the thermal contact direction and identifies no stable remainder.

The first successful escape is instead an external observer,

```text
O_ext = C_black^NN - C_white^matching - q = V-E+F0.
```

At 2M N325/N425, almost all of its enormous complex `J_D4` response survives removal of the frozen radius-2 local Euler nuisance: the far amplitudes are `31.007` and `32.673`. This proves a bulk Euler/rank-birth response outside `sigma(q)`, but D and S retain one transfer phase (`-.01293 +/- .03120`, `z=-.414`). A rule frozen before scoring then applies the source Gram projection

```text
J_D_perp = J_D - Re<JD,JS>/<|JS|^2> J_S,
```

with root and coefficient recomputed inside each delete-one batch. The result is decisive in its declared lattice metric: 99.8792%/99.9144% of the far-D magnitude remains, with two-dimensional `chi-square=641994/589232`; the transfer is `1.054088+/-.001956` at phase `1.373908+/-.002803`. Direct projection onto `J_S` therefore does not explain the coupling. The stored aggregates lack `|J_D|^2`, so no residual source-energy fraction is claimed, and Gram orthogonality is not CFT field orthogonality.

The retained batch-by-occupation tables now remove a more important nuisance exactly. If `n_occ=k` is the pre-insertion occupation count, then

```text
mu_ext(k) = k - 2N (k)_2/(N)_2 + N (k)_4/(N)_4.
```

The frozen radius-two local nuisance has its own exact conditional mean on these locally injective quotients. After subtracting both clocks, centering `J_D/J_S` within each `n_occ` and recomputing the JS Gram coefficient in every aligned delete-one batch, the clock accounts for 53.14%/56.16% of raw far-D magnitude at N325/N425. A coherent 46.64%/43.65% remains; its joint zero score is `chi-square_4=390067.1`, `log10 p=-84696.7`, and the JS step removes less than .5% of the fixed-occupation residual. This excludes `sigma(n_occ)+span(J_S)` sufficiency in the declared metric. It does not identify a second microscopic source, Q4 field, exponent or an independent evidence block. The risk-age versus conditional-hazard split and ambient-line determinant are now complete: `W_line` adds a real nearly JS-orthogonal state coordinate, but not a resolved coupling to `O_far/O_sep4`. That observer change is now measured by the F5 two-even-row follow-up `2d2a9ab` (N325/N425 chi2_4 149.93/246.93); next use those rows for cross-geometry transport and matching loading; `DIRECT_RANK2` remains a separate `A_top`/collision coordinate because it cancels from `E_top`.

Main PR #436 removes degree-one product-noise Euler projections; degrees 2/3/4 are kinematic, not fields. P267 marked birth is Palm-like and its aggregates still cannot score the proposed product-noise high-pass. Separately, P437 branch-only `1751e1c` measures six-noise variance inflation 1,298,313, while `386db0a`/`888af29` subsequently measure a fixed-five-bond contraction `B_S=(3.23893+/-.21639)e-6` at 14.97 SE, with 99.8485% of support energy at degree at least six. The new positive square-bond finite-support measurement is not the full high-pass or an Euler/CFT field; both production stages are completed.

Branch head `9f2033a` froze the two-observer lane and `26cecb4` completed 2M/100-batch blocks per size. Its `[O_far,O_sep4] x [J_Dperp,J_S]` result `4.27897/4`, p `.36957`, does not prove one source. Later F5 `2d2a9ab` changes the observer space and resolves source-response rank; this is not a rescore or contradiction of the old null. Retain both dependency/observer boundaries and ask about cross-geometry transport and matching loading.

P334 now supplies an actual path-resolved activation archive and an exact current atlas. N13/N17 show two active orbits, but the N7/N8 asymmetric-HNF continuation proves that the near-reference `76/24` share and close root spacing are not universal. The exact factorization is current timing times character Gram: scalar orbit nets determine balance intervals and Gram sign determines reinforcement/cancellation. Every scanned HNF row through index 12 has one simple root. A single slope-sign change is sufficient, while the obvious M-convex, real-rooted and normalized-matching proofs are false. The boundary-regular theorem `a9da7cd` proves BA+TM on both carriers implies ULC; translation regularity at `c8405b5` makes every proper Hall cut a consequence of aggregate TM, and `7d16c88` reduces the latter to `4DF<=M^2+4Y(T-D)`. Exact configuration gates `c2d8170 -> 6a7b042` then refute mark-only, union-preserving and base-only injections. One carrier-base exchange plus one free transverse mark saturates every minimal N6 row and the first Smith-(2,4) N8 gate, while full Hall equals factor-N compressed orbit Hall for arbitrary HNF. The open topology problem is uniform Alexander saturation of this corrected reservoir plus the independent BA switch. Larger cut scans and the refuted switch families add no theorem content.

The P334 age and branching chain localizes a finite state question, not intrinsic time memory. Old P429 whole-batch cross-fit attributes 70.4%/97.9% to H2; the newer `6147e22` has exact b2, recoverable successor H2 and matched scalar collisions. Graph replay `1b5a9de` is complete (22/22 bipartite, 11/22 Ferrers), as are minimal triples. The selected A/B baseline `1b5a9de` explains 84.0053% of its W2 difference. Draft `2e32fd0`, [capacity allocation](../results/p334-trigger-capacity-allocation/REPORT.md), refines this as `-195.657143` support/block localization, `+649.285885` side constraint and `+86.371257` residual, summing to 540. Side imbalance, not bipartiteness alone, drives the positive capacity contribution; these fixed-graph benchmarks are not physical interventions or population estimates. The next physical question is the topological meaning of the two sides/cuts. Draft `2b0844a` directional excess remains unresolved; another replay, scalar closure or capacity split adds no new result.

P337 identifies `[H,A,D]` as one A4 triplet and proves `A4=z_axis H_F3` coefficientwise at N13. Gaussian-ideal symmetry kills unweighted A/D, so explicit R-odd `q_A/q_D` sources are necessary. The N65 archive shows the orientation information sits in A net current, not common activity; independent N85 and held-out N145 blocks keep that natural current nonzero. N170 then resolves the exact negative H4 flip at `-11.389 sigma`, N340 flips positive, and N680 lands closest to the two-mode forecast while scale-neutral is strongly excluded. The four-generation recurrence, fixed identity dressing, same-base rank-three Jordan, free recurrence and free-single remain viable in their stated scopes; another generation is source-covariance limited.

The Draft birth-state-current result `7e5fcf1` now completes the zero-production decomposition:

```text
F1'    = J01 + J02,
F2'    = J12 + J02,
E_top' = J12 - J01,
A_top' = J01 + J12 + 2 J02.
```

Consequently the direct-collision current `J02` cancels exactly from fixed-p `E_top` and cannot be a third additive H4 line. N170 resolves reinforcing first-birth and completion terms; the resolved N340 share is completion-heavy; N85/N680 are noisier. Directional `J02` is unresolved at every primary size, while the completion-age first moment is nonzero. Those particular prism aggregates do not score all conditional/current-geometry rows; the later risk/hazard, ambient-line and F5 branches now supply distinct completed results. N130 remains a mapped control.

The collision lane is nevertheless geometrically sharper. Branch `1c93bed` proves every direct rank-two birth is either a one-carrier theta or a two-carrier figure-eight, giving respectively 6 or 8 alternating arms only to injectivity scale. Branch `cfb3ead` proves that ordinary six arms are insufficient and supplies the bounded reverse local surgery for globally typed theta-six and figure-eight-eight events. The type records exterior rank, component partition, relative deck-address determinant and a routable landing word; the local Bernoulli costs are exactly `p^6(1-p)^18` and `p^8(1-p)^16`. What remains is nonlocal: a uniform separation/nondegeneracy constant, the frequency of the required typed landing data and the figure-eight share. Production `D_N` still rejects a bare `N^-5/6` law. No exponent or continuum field is identified.

### 4. P205 resolves the ordinary character competition

The frozen P205 equal-area, Smith-changing prism at `N=25,50,125` selects one ordinary H4 amplitude over the frozen H8 and H12 alternatives. The retrospective activation-resolved reuse then scores the complete six-coordinate covariance:

```text
K1 line = H4,
K2 line = H4,
chi-square = 2.585155 / 4 df,  p = 0.629455,
K2 share of signed fitted H4 amplitude = 36.732%.
```

Both activations reinforce at every P205 size. The runner-up `H4/H12` is worse by `Delta chi-square=8.918835`; all other pairs are farther away. The result is decisive inside the frozen small-N ordinary H4/H8/H12 character family, but the component split is retrospective, the raw archives are `branch_only`, and neither a continuum operator nor an asymptotic theorem has been identified.

The primitive-line C3 branch `63d81fb` rejects zero and pure E4/E6/E4-squared at N112; E4+E6 nearly fits and N60-only replication has a power ceiling. The requested non-A_top strengthening is now complete: branch-only `experiment/p267-rho-c3-etop-20260830@2402a33`, `results/server-20260830/P267-rho-C3-Etop-N112-2M/REPORT.md`, records fresh 2M N112 square-bond configurations. E_top r1 resolves at `142.199/2`; the same-stream E_top/primitive-H4 determinant resolves at `21.202/2`, rejecting one common observer ray. These are distinct finite topology directions, not energy-field identification or another H4/H8/H12 vote. Consume the existing complex matrix for a transport/mechanism comparison, keeping square-bond and square-site claims separate.

### 5. The ordinary module question now requires an independent covector

Two independent selection steps narrow the regular ordinary carrier:

- vacuum/KdV spin 4 lies on the Alexander-even rank line and has exact zero `A_top` projection;
- the regular unlabeled four-leg `[2]` endpoint has an exact ordinary linear selection zero under its declared generic-Q hypothesis.

The transverse thermal `Q4 epsilon`/Jordan family is therefore the first listed candidate allowed by both selectors. “Allowed” is not “identified,” and P275 shows that the global `chi4(ell)` line is not the required local-field covector. A future overlap must use a separated-local, charged, modular/stress or other within-q observer and carry a module fingerprint. Main-integrated exact spectroscopy already fixes an additional Q-direction discriminator at Q=1:

```text
x'_[four-leg V_(2,+/-2)] = -5 sqrt(3)/(16 pi),
x'_[thermal Q4 epsilon]   = -9 sqrt(3)/(16 pi),
gap                       =  sqrt(3)/(4 pi).
```

The generic-loop spin-8 and spin-12 rows in that oracle are controls rather than H8/H12 angular-alias assignments. The other sharp fingerprint is the thermal-Q4 positive-mode Ward vector `4:-6:3`; an ordinary primary gives zero, and Jordan residues add a second gate. Main's PR #288 adds a rank-four identifiability boundary: parity fixes four powers but not the Taylor ratios `f_I1/f_I0` and `f_T1/f_T0`. Modulus/phase dependence, the log-partner response, an independent covector and those two external dynamical relations must accompany any scaling power.

P321 now supplies a parameter-free modulus fingerprint without promoting the field assignment. At Q=1, the graph polynomial and matching/homology root use the same finite `P2-P0` projector, while the TL open/closed crossing is its controlled infinite-length endpoint. Conditional on a Virasoro-transparent homology restriction and ordinary thermal `Q4 epsilon`,

```text
C_width(rho)/C_width(infinity) = E4(i rho),
C_width(rho) = C_N(rho)/rho^2.
```

The oracle certifies the modular numbers and normalization, not projector transparency. An additive `H4_defect(rho)` or logarithmic partner supplies a second modulus function. The frozen fresh branch `ca72994` completes N=144,576,1296, five shapes and 100k per shape with full within-N covariance and independent cross-N domains. The fixed scale gate passes and conditional E4 is not rejected; the pilot tension does not reproduce. Exact children identify the missing object more sharply: `c2a5e2d` retains the homology-resolved thermal one-point in the subleading correction, `9dda24e` identifies `Tr(TA)-QTr(A)` as a two-closure trace rather than one central defect, and `d0fca79` gives an exact graded direct sum whose regular Q derivative is semisimple. Singular Jordan extensions exist but are nonunique. P333 has now scored generic-Q detach and the minimal scalar endpoint-mark continuations; all still miss the Gram/source gate. The branch records acquisition on TgFr7R rather than the designated TV2N0X environment, so infrastructure/chronology review remains part of integration.

P333 makes the Q-direction interface equally explicit. Two generic-Q lifts share the same Q=1 endpoint but differ by `-(Q-1)W0D`, so their normalized first jets differ by `-pi0D`; raw Q tangents are section-dependent. The width-3/4 Gram/source intertwiner gate is empty for the physical affine family. Standard generic-Q detach has only two moduli, endpoint normalization forces `X0=T,V=0`, and no detach velocity repairs the zeroth-order obstruction.

The endpoint-mark chain `081a5ed -> e7e6c80 -> 7b40ec7 -> ab3eed81` exhausts the declared individual width-4 terminal irreps. P398 `5389200`/`afc619c` completes the coupled 23-dimensional accumulator and its automatic higher projected-Gram condition. The physical continuation is now also complete: branch-only [`e38fe76`](https://github.com/LightChainr/Matching-One/blob/e38fe7634354b0cb2201fa55fd9b4d37ccedeef2/notes/p398-positive-cylinder-propagation.md) computes the same AP/landing functions' neutral connected matrix under a positive Q=1 square-bond cylinder measure on 14 frontier states. It has two distinct nonzero eigenvalues `(3±sqrt(5))/64` and positive determinant at every finite separation. Child [`b35e100`](https://github.com/LightChainr/Matching-One/blob/b35e100a3903c706dceba57c4667386eb4510ac3/notes/p398-anisotropic-cylinder-spectrum.md) proves no Jordan collision anywhere in this positive anisotropic family. Compare unchanged readouts across width or microstructure; the finite propagation does not identify a site-Matching field or make the signed accumulator a stochastic transfer. The formal and physical constructions form one linked exact lineage, not independent samples.

## Rank-1 reuse and escape contract

The marked-birth program already retains most of the common field below. Its bare mean-line q2 and simple local-UV q2 continuations failed; its external Euler continuation is positive and survives direct `J_S` Gram projection. Reuse those results before producing another stream. Any continuation should retain, in the same orientation-paired replicas and delete-one batches:

```text
q and q^2;
I01, I12 and direct 0->2;
ell, iota and Re/Im chi4(ell);
line H4 and landing H4 separately;
N*S_H4, N*D_H4 and the unmarked S birth mass;
K1,K2,C,W and their line/landing-conditioned curve bins;
two-root field products when an independent observer is used.
```

The zero-production Draft analyses now provide both a translation/zero-area shape split and a boundary-resolved F3 flux split. The external stored-data residual and two-observer production are complete; the current matrix leaves rank one alive. Useful new acquisition must therefore add a section-aware completion mark, a projective-leg field, a cross-boundary/winding source, path order or another typed coordinate. More `qJ`, `q^2J`, mean-line q2, reversible source rotations or nonlinear q-only connected rows are either rejected parameterizations, exact contact transforms or repeated coordinates.

## Parallel programs

### Ordinary versus charged sectors

The ordinary P205 prism selects H4, while the branch-only N325 deck-charged likelihood weights H8/H12/H4 at approximately `71/21/8`. The charged H8 row is a likelihood leader, not a discovered spin-8 field; H12 and H4 are not both rejected at strict 5%. This is sector tension, not contradictory replications.

This is the typed charge-spatial tomography lane. Its target is not another pooled H4/H8/H12 vote but the rank of `charge type x calibrated spatial phase` after normalization. The ordinary opposite-alias branch has now completed 600M/design: H4-only and zero both survive, while A12 is unresolved. The design worked as a de-aliasing coordinate but did not identify either H4 or H12; a stronger source is more informative than its third alias.

The two primitive Z5 cubic channels have a 1M/100-batch result, but their compact vector is compatible with zero. The normalized-separation continuation closes the R1-R4 local-annulus family. The root-marked projective-leg observer then advances through fresh pair and cross-scale production: pair support is robust, the d2 cubic is not, every one-component N505 transfer shape fails, and the T row has a minimal rank-two recurrence. Exact direction tomography shows that A is not closed even by free amplitudes at those roots. Draft PR #267 establishes that common rank two fails while common rank three closes the old axis T/A window.

A statistically independent 80k/400-batch bivariate block at `4c1f5d8`, dependency group `p250-projective-leg-N505-bivariate-80k`, preserves signed C4 (`p=.379`) but rejects common commuting diagonal ranks one through three. Frozen post-result reuse at `ded78bb` rejects the canonical rank-five Z5 Weyl models and favorable free commuting rank four/five controls. The model-free child `a770ac9` sharpens the boundary without assuming diagonalizability: plus and minus two-charge blocks reject rank at most four (`p=2.80490e-11`, `.00235376`) while rank at most five survives the frozen `alpha=.01` (`p=.0542623`, `.0655065`); the raw four-channel stack rejects rank at most five (`p=2.84020e-5`). The last statement is conditional on one common raw state/basis and is not a physical dimension-six identification.

The child annihilator result `a46ed63` compares minimum-singular directions and never jointly imposes `H_plus q=0` and `H_minus A(q)=0`. The independently preregistered radius-five branch `11130ae` supports both hand extensions, rejects identity and keeps only R2 in the frozen family (`p=.013379`; R3 `.009686`). Draft result `99d23a7` then implements the missing joint radius-four null. Its identity-linear replay exactly reproduces `a770ac9`; it rejects R2 (`p=.005824`) and keeps R3 (`p=.077486`). Thus the radius-four joint and radius-five direction survivor sets are disjoint. Because the published radius-five map score reuses the old 80k moments, the two gates are not independent evidence and neither map is identified.

Draft PR #267 now completes the augmented old-plus-degree-five gate with separate old/fresh influence covariance. All five primary and eleven secondary maps reject, and the plus-hand `20 x 6` rank-at-most-five chart rejects at `p=6.01e-7`. This closes the current fixed-bridge election and the generic `5+5` fallback, but it does not prove rank six, noncommutation or a field. The live comparison is a candidate-independent rank ladder versus ordered `T_xT_y/T_yT_x` or path-enriched memory on the committed blocks.

Branch-only `33c557b` remains a valid finite-window result: in the declared endpoint-Hankel observer, both hands eliminate `rank<=5,6,7`; `rank<=8` is first compatible, and `93eaab1` rejects the full rank-eight R2/conjugation plane. PR #416 changes the interpretation rather than those statistics. The complete uniformly anchor-averaged N505 autocorrelation has at least 100 strictly positive nonzero spatial-frequency weights, and production branch `dbeb29c` finds all four archived hand-charge channels compatible with the nonnegative 101-frequency cone (`p=.163--.378`). Thus endpoint Hankel rank measures resolved spatial spectrum; it is not a hidden-state, transfer-rank or field count.

The next state test cannot be another endpoint word table. Exact `af7dd01` proves the endpoint API factors through word abelianization; `3128e3e` proves fixed-site delete/add is degenerate. Exact `6fbbe5e` constructs the covariant state-dependent cut/connector, `0061e4e` completes N505 at 200k/400, and Draft `eb29446` completes its zero-sample use. Pre-outcome state lowers held-out MSE `.70173 -> .48438`; both hands pass, yet complete endpoint periodograms are absent and the enriched residuals share `k=36`. The live P250 mechanism is therefore a future high-frequency/full-spectrum discriminator, not another fit of the same archive.

Open PR #385 supplies a useful identifiability boundary for that comparison. Ordinary distinct-eigenvalue modes can converge to a Jordan semigroup on every finite word set, so unrestricted ordinary and Jordan classes have zero separation unless a physical spectral gap, conditioning/positivity constraint or exact symmetry removes the near-collision. Its length-five C4-character obstruction is exact only for a closed flat Laurent quotient with a cyclic invariant source and internal rotation. The theorem is a premise-gated discriminator, not evidence that P250 realizes its nonreduced control; its R2-specific application is superseded by the augmented all-map rejection, and the radius-six lower bound gives no statistical support to a five-state P250 quotient.

### Low-dimensional state and the targeted context rectangle

Norm-4 rejects the frozen analytic q2 and common-generator forms. The post-reveal Jordan-plus-one-even-mode recurrence then passes its frozen N520/N680 generation-four target at `lambda=1/2`, but `lambda=0`, `1/2` and `1` are nearly indistinguishable. The small-state family remains usable, while neither a nonzero extra mode nor its transfer eigenvalue is identified. In the PR #247 multiradius block, the ordinary/local plus shell is compatible with a common amplitude (`p=.3564`), the matching-odd minus shell rejects that scalar law (`p=.00382`), and matched-cutoff equality is marginally tense (`p=.04544`); all three are correlated views of one raw block. A distinct two-state recurrence trained on N325/N425 passes held-out N365 (`p=.965`). The newer sector score rejects rank one only for `A_minus` and does not resolve shared-J2 versus gap-one.

A same-semantics Gaussian/annulus rectangle is complete: shared generator `3.93393/6`, separate pairs improve `.21622`, p `.40024`. The static P255 gain-free determinant `68.959+/-.495` and H1-orthogonal residual `-34.496+/-.247` reject a one-gain endpoint explanation without recording order. Subsequently branch-only `4daa50c` replays the old N650 stream and measures `Cact=I(h25=2)[I(h2=2)-I(h5=2)]` at `43.1466/3`, with exact rank three. Thus intermediate filtration is no longer absent, but this proxy is not physical Gaussian/annulus AU-UA.

For a common generator, `A=exp(-t_A G)`, `U=exp(-t_U G)` imply `[A,U]=0` and commensurate-clock equality. A physical ordered test still needs the annular boundary partition/lift and one shared source/readout contract for AU and UA. Define its separating prediction using the completed static and intermediate controls; do not request generic h0/h2/h5/h25 or re-score Cact as a new experiment.

### Potts-Q tangent and the P263 variance gate

The exact boundary tangent supplies a function-valued positive control, but the lattice estimator still needs an efficiency gate. P263 constructs an exactly unbiased stopped-transcript covariance estimator and proves cancellation of irrelevant spectator noise. Its frozen 20k pilot reveals only about 20%--23% of edges, yet the comparison to the earlier global-score smoke used different random streams and shows no demonstrated variance advantage.

The next P263 output is therefore paired: on each outer transcript record both the stopped estimator and its matched global score, vary completion count `K` and scale, and report their covariance and variance ratio. This is an estimator-mechanism gate, not a rescore of the boundary shape.

The continuum-family velocities of the four-leg and thermal-Q4 candidates are already exact on `main`; the live task is not to recompute them but to expose the lattice measure score, finite confluent projector and explicit field derivative in one typed interface. Before any higher Q derivative is called a new state, subtract the topology-forced Pascal jet

```text
D^n H|Q=1 = sum_(k<n) binom(n,k) D^k P|Q=1.
```

Only a typed residual after this subtraction can be assigned to an explicit field derivative, singular projector residue or finite-lattice failure of the continuum identity.

Main-integrated PR #288 also prevents a false closure: the four parity-allowed rows have structural rank four, so `f_I1/f_I0` and `f_T1/f_T0` require dynamics or frozen calibration. Q=4 seam ratios test colour sector, separately from spatial harmonics. PR #291's single-C4-orbit alias warning survives; calibrated axis/diagonal phases separate scalar from a `4 mod 8` angular class, not H4 uniquely from H12. Further phase diversity or a typed internal kernel is needed for that finer claim.

### Cross-microscopic logarithmic control

The triangular log-pair cross-cutoff result is positive but its `kappa_proxy` is gauge dependent. The next statistics are a top field with two two-point-normalized spin insertions and the two-modulus weight-12 H4/E6 double ratio. These close the declared gauge before comparison across triangular, square-bond and square-site systems.

### Relative source and threshold origin

The scalar relative source closes on `1,q,q^2`, its three-sector algebra is semisimple, and `HH^2=0`. Higher powers and unsubtracted Pascal Q-jets do not add new representation content. A newer branch-only classification explains why the missing state is typed rather than another scalar variable. Under the natural product-local edge-subset contract, one site requires a four-terminal one-block junction `J4` absent from ordinary ribbon edges and transition pairings, so the matching defect is not a direct Tutte/Krushkal-family specialization. The positive exact object is a typed-site rank-image quotient,

```text
M = (z d_z - 1) R_site|_(z=1) = Q d_Q Z_rel|_(Q=1),
```

and, on honest carriers, the side-genus derivative of a site-Krushkal quotient. Occupation alone fails already on two order-2 quotients; one topology source is necessary and sufficient for the terminal defect, while the transfer frontier still needs typed partitions. The theory frontier is therefore the recurrence/coefficient algebra of this quotient and its connectivity/defect radical—not another named edge-subset substitution.

P333 now makes that radical target concrete. For the raw partition-connectivity Gram form at `Q=1+epsilon`, there is one unit pivot and `Bell(n)-1` nonzero epsilon pivots; the first-jet pairing on the endpoint radical is nondegenerate. This exact extension is universal rather than selective. Join-only dynamics are simultaneously diagonalizable, and no single deterministic join/detach word through four marks is both defective and first-jet-Gram self-adjoint. The latest exact child `6c60b0e` supplies the missing algebraic positive control at three marks:

```text
K = D + J - DJ - JD = (D-J)^2,
rank(K)=1,  K^2=0,  HK=K^T H.
```

Its Jordan bottom is exactly H-isotropic. The signs encode a connected-history subtraction rather than a positive stochastic transfer, so this still does not identify a VJS pair or logarithmic coupling. Scalar detach and endpoint marks fail; the width-3 charge-one doublet repairs the Gram gate, but width-4 charge one fails. The final width-4 charge-two alternating character also fails at `ab3eed81`. P398 `5389200`/`afc619c` completes the formal accumulator chain, while `e38fe76`/`b35e100` separately establishes positive-measure AP/landing propagation with two ordinary modes. Width/microstructure transport of those same readouts is the next comparison; full-Q realization and dimension velocity remain separate.

The numerical origin of square-site p_c remains a separate program. Main has the bounded four-terminal corpus (27 signatures, 353 nonzero polynomials), abstract planarity (26/27), specified disk embeddings (18/27), gluing and Mobius inversion. These primitives alone are not a tiling or comparison theorem. Open PR #438 / `bd2561a` has since supplied the missing concrete geometry: 192 labelled/41 D4-enriched states, explicit checkerboard torus gluing and a primal/dual edge bijection.

The P14 continuation distinguishes spherical W5 duality from the true disk-relative cycle-with-leaves dual; joint boundary labels alone add no state. W5's balance root `.297930519` is far from square-site p_c. Open PR #438's actual periodic primal and dual have `(V,E)=(3,8)` and `(5,8)`, hence are not self-dual. The remaining exact object is a law comparison on these already constructed graphs, not a first wiring diagram or another application of gluing. Main's independent 400/373 block-event plan remains execution-ready, not a bound.

The median/IQR/standardized-quantile shape contract is main-integrated, and branch-only production reuse now rejects exact finite-size shape equality while finding an almost rank-one residual tail direction across norm2, norm5 and an independent P43 comparison (direction cosines about `.9915-.999995`). This is a promising deformation fiber, not universal collapse: the effective amplitude ratios disagree and only one lies near `5/8`. Freeze the direction, test amplitude on a fresh lineage, and connect it to the thermal jet before naming an exponent. Correlated-hyperedge closures and symbolic obstructions can run in parallel, and success or failure there neither identifies nor refutes the continuum carrier.

The exact microscopic H4 stencil gate also removes one uninformative search family: any nonzero nonnegative horizontal/vertical-only fourth-moment proxy is strictly positive. In the declared axis-plus-integer-diagonal normalization, cancellation occurs only at weight ratio `4:1`; more generally a nonnegative candidate needs an oblique orbit with negative `cos(4 theta)`. This guides improved-action geometry design but is not a statement that the renormalized H4 coupling vanishes.

## Lifecycle boundaries

The mechanism chain deliberately mixes lifecycle states:

`integration_status` says where an artifact lives, not whether a mechanism is accepted. Readiness (`exact_gate`, `protocol_frozen`, `implementation_ready`), acquisition (`no_data`, `smoke_only`, `production_acquired`) and result stage (`no_score`, `engineering_score`, `claim_score`) are recorded separately.

| Layer | Current status | Scientific use |
|---|---|---|
| Digital Alexander theorem, filtration and cached quotient frontier | `main_integrated` through index 13, including PR #330 / `c4a9955` | exact finite-state backbone and reusable subset/line/`iota` regression oracle; finite exhaustion does not establish the unrestricted theorems |
| unrestricted rational, integral, projective-birth and root-consistency claims | `branch_only` `73d4960`, child `c1a72e5`, stacked oracle `6f54935`, P334 chain through corrected reservoir `6a7b042` and `23fd682` | review the all-period rational/integral parents; current timing and Gram geometry are separated, BA+TM is sufficient for ULC, and translation regularity closes all proper Hall cuts. Mark/base-only injection is refuted; prove arbitrary-HNF saturation of the base-exchange plus transverse-mark reservoir and BA, reuse the N130/N170 current subset, and sharpen the near-critical window |
| `C/W`, `I01/I12`, `S/D`, tiny `Cov(A_top,J_D4)` | `branch_only` | exact coordinates and finite contact controls; the later all-order theorem excludes q-only connected rows as independent field matrices |
| P334/P337 cooperative state and line response | scalar collision `branch_only` `6147e22`, graph/capacity `1b5a9de`, full-production overlap `85af498`; direction/capacity reuse `open_pr` #267 / `2b0844a`/`2e32fd0`; F5 `branch_only` `2d2a9ab` | Graph replay, Ferrers/triples and support/side allocation are done. Explain physical two-sided cuts; distinguish unresolved P334 direction from resolved F5 W_line/JS response and unresolved P439 M loading |
| marked-birth q/local closures and external Euler bridge | `branch_only` `936cff2`/`eea85c4`, `48a8818`/`26cecb4`; occupancy `open_pr` `c34dd98`; later F5 `branch_only` `2d2a9ab` | Earlier q/local carriers fail and the Euler basis leaves rank one alive; F5 subsequently resolves source-response rank. Fixed-K/Palm/product-noise semantics remain distinct; next use the existing typed rows for transport/M loading, not another first winding source |
| P275 global-line and Cartan contact map | `branch_only` `4d7ecd5` and `7faa317` | rejects declared Q4/Jordan/H4 maps and reclassifies `Gamma` as contact/projective polarization |
| P205 total prism | `branch_only` | frozen ordinary character selection at small N |
| ten-size K1/K2 root, full-curve, transport/shape and F3 activation-flux reuse | `open_pr` #267 | retrospective component, area, stable-node, translation/deformation and signed birth/exit decomposition with shared covariance; P337 adds the N85/170/340/680 birth-state current split, while F3 remains one N65 dependency block |
| P205 K1/K2 prism reuse | `open_pr` #267 | retrospective component-character decomposition with branch-only source data |
| Q=1 spin-sector velocity oracle | `main_integrated` PR #264 | exact continuum-family discriminator; lattice overlaps and field-definition terms remain missing |
| four-channel operator-mixing audit | `main_integrated` PR #288 | exact rank four; two external Taylor ratios remain missing |
| P321 projector, modulus shape and closure extension | `branch_only` P321 through `d0fca79`; formal P398 through `afc619c`; physical AP/landing propagation through `e38fe76`/`b35e100` | conditional E4 is not rejected. The finite positive width-four response is complete; compare width/microstructure transport with fixed readouts while keeping the P321 overlap and full-Q lift separate |
| generic-Q lift and connectivity-radical transport | `branch_only` siblings `502719c`/`c34c838`, Gram/source `96df7c8`, radical/history chain `6c60b0e`, formal/physical P398 continuations above | raw Q jets are lift-dependent and signed `K=D+J-DJ-JD` remains a formal control. Existing AP/landing functions now have ordinary positive-measure propagation at width four; changing width or microstructure tests its transport, not an automatic connection to that signed Jordan control |
| spin-4 alias, Q=4 seam, annulus and mixed-context gates | angular alias `main_integrated` PR #291; rectangle `branch_only` `5873de8`, static residual `6de148b`, intermediate proxy `4daa50c` | Calibrated angular design remains necessary. Static shared generator survives; the N650 proxy is measured with exact rank three. Physical annular AU-UA still lacks its boundary partition/lift |
| anisotropy stencil and threshold-confidence chains | `main_integrated` PRs #272/#278-#281 | exact microscopic feasibility/protocol infrastructure, not renormalized H4 or a new threshold bound |
| threshold shape contract, production tail fiber and exact-route map | contract/map `main_integrated` PRs #289/#290 plus exact profile certificates through PR #383 / `0dc4430`; production `branch_only` `bc19bcb` | exact collapse fails, nearly rank-one tail deformation survives. The synthetic rows add exact standardized moments, reflection, quantiles, distances and a rank-one control Gram only for declared fixtures, not a production or universal invariant. Freeze the production tail direction and hold out amplitude; the separate height-100 degree-one certificate excludes only low-height rational threshold roots |
| primitive C3 and Z5 transfer frontier | C3 `branch_only` `63d81fb`/`2402a33`; P250 N505 `0061e4e`, state result `open_pr` #267 / `eb29446` | Fresh C3 E_top and primitive-H4 rays differ; next compare their transport. P250 retained state adds 30.97% beyond the declared residue model; full spectrum/common-k36 remain its sharper missing coordinate |
| production-first model elimination | Issue #370 fixtures/framework at component tip `cc3c204`, available in `main@5ac456d`; `open_pr` #267 E_top results; completed P250 and P429 production frontiers | ten cross-geometry and six P154 frozen blocks have Phase-D eliminations. The next method object may consume an actual P250/P429 covariance survivor when it changes the scientific decision, not more toys or compilers |
| exact design filters | `main_integrated` PRs #421--#423 | adjacent annihilator gives only the conditional map `w=4+q`; Gaussian fingerprints separate the declared channels algebraically; N1105 has no fifth primitive D4 direction. Apply the first two to real data and stop the N1105 fifth-direction search; N32045 is not a production recommendation |
| observer/geometry/scale/acquisition design filters | exact filters `main_integrated` through PR #437; P437 production `branch_only` `1751e1c`/`386db0a`/`888af29` | Product-Walsh, fixed-K Johnson and Palm semantics are distinct. The old full estimator is expensive, but finite support is measured at 14.97 SE and 99.8485% degree>=6; neither means a field or makes P267's absent coupled-noise rows scoreable |
| ordinary H4/H12 opposite-alias lane | design `main_integrated` PR #325; completed production `branch_only` `86e77db` | 600M/design leaves H4 and zero compatible and A12 unresolved. Preserve the de-aliasing coordinate, but strengthen the source before another alias row |
| typed-site polynomial and terminal-state infrastructure | classification `branch_only` `5d8343d`; main canonicalizers/corpus/disk/gluing; enriched periodic pair `open_pr` #438 / `bd2561a` | The disk-relative dual, enriched states and explicit periodic gluing are now constructed. A stochastic comparison and threshold bound remain absent; another first geometry setup is not next |
| real-gadget comparison and W5 duality | main finite algebra; `branch_only` `2cc9466`/`5f5afd3`; actual periodic pair `open_pr` #438 / `bd2561a` | Spherical and disk duality differ. The explicit checkerboard primal/dual pair has unequal vertex counts. Compare those laws; do not infer self-duality or new state dimension from boundary labels |
| Issue-27 rational-width prereveal | `main_integrated` PR #340 / `1c7f54b` | width-1..21 knowledge and width-22..24 forecasts are frozen while the primary target is absent; score once on arrival before any refit, with no current model winner or threshold claim |
| scorer semantic transport | historical inventory snapshot `main@5ac456d` and earlier Draft PR #267 snapshot; incorporated main snapshot `e300609` | recorded counts were 59 = 28 direct + 27 covered + 3 frontier/migration + 1 utility and 65 = 28 + 27 + 9 + 1 respectively; these are not current totals after new scientific scorers. Typed adoption improves later reuse and does not postpone analysis |
| odd-jet coordinate invariants | `main_integrated` PRs #339/#349 | exact linear metric removal and synthetic order-five nonlinear elimination; next value is typed real-data adaptation, common observable normalization, root uncertainty and delete-one/full joint nonlinear covariance, not a standalone production run |
| C4 tangent and Issue43 typed wrappers | `main_integrated` PR #341 / `3aebee0` and PR #344 / `4a77650` | exact identity transport for frozen scores only; no new data or score, and the science still needs a different observer/geometry rather than repeated production |
| GPU rental gate | `main_integrated` PR #342 / `b6c81d0` | exact fivefold end-to-end resource threshold plus output/determinism requirements, but no GPU measurement or scientific result; resource attention stays separate from science priority |
| Gaussian norm-10 commuting-square power | design `main_integrated` PR #328; source-only power gate `branch_only` `2cfde0b` | projected 27.21B/55.18B per lineage and negligible covering-CRN gain defer N650/N850 production; use only a target variance/runtime pilot because no target data exist |
| Q4/Jordan overlap and fingerprint | `hypothesis` plus branch-only exact predictions | source rank, modulus shape and typed charge-spatial rows are three orthogonal axes; no single passing row identifies the field |

Use `docs/STATUS.md` and `analysis/research_ledger.yaml` for exact commit/path pointers. Citation here never promotes an unmerged result to `main`.

## Update rule

Integrate useful science into the map before forcing issue taxonomy to catch up. Record what is supported, what is in tension, and exactly which parameterization was excluded. Change attention when a sharper observable appears; do not lock, close or rhetorically erase a distinct attempt.
