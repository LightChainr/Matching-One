# Completed multi-line handoff, no duplicate execution request

This package adds completed work. It does not alter historical freezes, main, STATUS,
or the submitted scope of original-U. No Monte Carlo, new-width ladder, or GPU job
is commissioned.

## Existing #733 / #636 channel

Record the all-spatial-source 4x4 Hessian, its 9+/6-/1zero inertia, the exact conditional
structure-factor identity and the logit-versus-probability contact term. The earlier
two-row dipole examples are retained as special cases, not relabelled as new data.
The full polynomial/covariance oracle is now supplied; do not commission the same
width-four source response again.

## Existing #337 / #275 theory channel

The two positive invisible spatial marks preserve the whole (rank,K) law for every
uniform p, including every unmarked rank-source/thermal jet, while two addressed
spatial sources distinguish them. This is a finite dictionary-sufficiency control,
not candidate identification. It says what a new physical-source forward map must
supply. It does not authorize replacing original-U with a better behaved source.

The rank-source zero-free strip is exact and general. Do not spend a numerical zero
search looking for a finite-real-s pinch that bounded support rules out. An extensive
s=m*sigma bias is a different ensemble and is already characterized at fixed width.

## Averaging/estimation channel

The annealed curve versus quenched root example is complete. It is NOT a reanalysis
of any prior production and NOT the same as finite-sample quantile-estimator bias.
Use it as a unit-level control whenever an analysis averages environment roots.

## Conditioning/algorithm channel

Geometric-onset-anchored conditional-odds integration is a ready finite reference.
The supplied optimal allocation formula includes the cost per independent conditional
sample, but not quadrature/anchor/selection bias. Those costs and errors would need
measuring before a larger sampler is proposed. No such larger sampler is ordered.

## Existing #734 / retrieval channel

The continuum mesh limit and fixed microscopic width limit must remain distinct.
The critical homology observable has established prior art; the correction does not
revive a new-observable claim. AET2022's reach is a paper-specific historical result,
not a verified current record. The rank/direction/spiral dictionary already exists.
New primary retrieval should compare identical source, ensemble, boundary closure,
normalization and limit order, rather than only matching names or exponent values.

## Dependencies and validation

Apply to a tree carrying the two unchanged input files from #708/#710. New code imports
only the new modules, not previous mutable analysis scripts. The standalone physical
Hessian needs no input file. The full test set and two independent validators use the
pinned certificate/definition. Full repository CI has not been run; 27 focused tests,
clean minimal-tree patch application, same-parameter regenerations and higher-precision
numerical checks are documented by the bundle's external validation records.
