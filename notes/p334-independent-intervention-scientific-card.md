# P334: independent intervention passes the frozen positive-response decision

One million **fresh prefixes**, not additional suffixes on the old40k,
have now tested the discovery prediction under a directly sampled finite
intervention. The fixed primary result is

**T = (3.0852 +/-0.3919) x10^-8**, new-batch SE.
The predeclared3SE interval is **[1.9096,4.2608] x10^-8**; its lower end
exceeds the frozen practical-zero margin **1 x10^-8**. The exact decision
string is `stop_complete_two_score_label_closure`. The frozen archive
forecast **3.6565 x10^-8** lies in that interval; no amplitude was refitted.

The four predeclared own-center responses, N325 first/second thenN425
first/second, are `(3.2314,2.8101,3.3661,2.9332) x10^-8`; all have the
predicted sign. A_ref signs are negative as forecast, but remain correlated
secondary outcomes. No best coordinate was selected after collection.

- **Mechanism removed:** the complete conditional label-mean model
  `c_a(Z)+b_f(Z)s_f+b_s(Z)s_s`, with common slopes across safe classes,
  predicts exactly zero and now fails the frozen independent-block rule.
- **Intervention:** exact rational q+/-=(1+/-phi/B)/d retains all safe-class
  masses, instantaneous joint rank/Euler laws and both original score means.
  Weighted B(Fplus-Fminus)/2 is exactly the predeclared normal response,
  without a small-step approximation.
- **Not proved:** origin of the unperturbed global anomaly, a field count,
  or failure of the more limited first-Jacobian approximation J=B G.
- **New source/dependency:** N325/N425 each20x25000 fresh prefixes,8 paired
  draws/own-axis, separate size domains, same frozen seed and protocol.
  All old discovery batches remain outside new inference. All40 batches
  completed; no extra sample after scoring.
- **Delivery:** [immutable freeze bc0a18c2](https://github.com/LightChainr/Matching-One/blob/bc0a18c207e3b09f49ea6b6af6601471114d654a/notes/p334-independent-intervention-freeze.md),
  dispatch7fc6fcbb, producer513552c7, raw blockf1b36436 committed before
  [frozen score1164ba91](https://github.com/LightChainr/Matching-One/blob/1164ba91/results/p334-independent-normal-intervention/score.json).
  NePnUn and551oUR generated the two blocks in12.578/14.739s including
  compilation, excluding provisioning/transfer. All artifacts retrieved.

The fixed block stops here. Next work should address transmission to a
named global observer; no extra contact-descriptor search or fourth fitted
model is attached to this result. Branch-delivered, not merged; general
#334 remains open. The3SE rule is a fixed diagnostic, not an anytime or
mathematical confidence certificate.
