# P255 ordered-context action audit and archive proxy

The existing static Gaussian-by-annulus rectangle is not an ordered-action
experiment.  Its common source/readout is valid, but radius is stored only as
an observer coordinate.  It does not retain the annular boundary state needed
to compose a Gaussian isogeny before or after radial propagation.

The frozen exact-maximin row `N85 -> N425` makes the obstruction concrete.
`R7/R8` are injective on N425 but not on N85, so the `UA` word cannot use the
same P253 landing registry.  Both orders exist through `R4`, but that disk lies
below the N85 systole: ambient homology, quotient-fibre incidence and the
connected topology residual are then identically uninformative.  A static
four-cell difference must not be relabelled `AU-UA`.

The immediate replacement is deliberately narrower and uses no new random
block.  Replay the committed N650 20k stream and retain the intermediate
degree-two and degree-five join corners.  The two Gaussian joins have the same
endpoint but different filtrations, so

```text
Cact = 1{h25=2} (1{h2=2} - 1{h5=2})
```

is a genuine order-sensitive proxy.  Its four typed projections `ES/ED/OS/OD`
are scored jointly.  Every legacy batch sum must match the committed archive
exactly before the proxy is revealed.  This tests the proposed measurement
shape, not the missing annulus action.

The smallest real `AU/UA` implementation must add one state object to the
P253 runner: the typed boundary-connectivity partition at the intermediate
radius, with its lift/pushforward map under the chosen Gaussian cover.  No
amount of extra scalar radius data supplies that state.
