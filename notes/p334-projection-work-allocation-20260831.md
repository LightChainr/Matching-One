# P334 prefix-response readout: bounded parallel work

This execution team is consuming existing data locally. No machine, source,
prefix, suffix, determinant experiment or finite-policy job is requested.
Coordination is repository-first; one completed receipt per bounded task.

**Completed receipt:** original8 projection `90226598`, exact descriptors
`1cfa4ae8`, projection theory `a940e65d`, original-block covariance join
`172fbeb1`, and standalone00 new64 reuse `323de7d5` are pushed. No owned
compute remains. The original8 Issue334 card5477178424 is updated in place
with the final result and links, followed by one consolidated PR267 pointer.

Root result `90226598` fixes the original8 low-dimensional analysis. Four
contact descriptors capture about 80–99% of the own-source, within-rank-cell
signed covariance loading; source energy alone captures about half. These
are signed loadings, not variance fractions or a closure theorem.

- **Covariance coordinator (`p158_power_gate`)**: append this readout to
  `ce20158a` using the supplied 20-batch LOO rows, without raw replay. Report
  source-strength/contact shares and residuals, incremental projection
  variance after baseline clocks, and the uncertainty of partial contact
  cross-moments. Keep all response/receiver/source directions together.
  Do not label plug-in projection variances as confidence lower bounds.
- **New64 data reuse (`p321_shape_ratio`)**: consume only committed
  `8ad30617` prefix-level `results-extension/prefix_statistics_N{N}.npz`,
  not fork gzips or determinant recomputation. Extract the `new64` mean
  integral-A/E responses into physical-source `H_C/H_W` (divide by -2/-1),
  retaining original00 counters/batches. Compare exact-feature loadings
  with original8 and a cross-stream clock-response covariance using old8
  clocks and new64 tangents. The target is the original00 population,
  zero padded to the original20000-prefix denominator. This is conditional
  stream reuse on shared prefixes, not independent population replication.
  Keep a standalone result, code and concise interpretation; do not merge
  streams into the frozen original8 projection automatically.
- **Root**: interpret what contact structure adds beyond clock means,
  integrate the existing local-two-dimensional result and publish one
  consolidated scientific card to Issue334 and one PR267 pointer.

All result branches retain the original source identities and dependency
block. Leave other teams' worktrees and cloud state untouched.

Repository update read at18:40: the overview analysis team has claimed
held-out prediction (`J=B G` and fixed contact features) and cell00
conditional-shape tests in Issue334 comment5477163171. Those remain their
tasks. This team's projections are descriptive within-cell moment loadings;
the new64 reuse item is only cross-stream descriptive moments, not a
crossfit/prediction campaign or conditional-shape computation. HZ/TV now
belong to that other team's new jobs; the earlier all-Ready receipt is not
the current allocation.
