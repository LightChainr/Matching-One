# Issue #154 handoff archive — frozen 2026-08-31

This note is historical provenance for Issue #154 after its live body was compressed. It is **not** a current execution queue and does not upgrade any claim. Full old Issue text and comments remain in GitHub history; this file keeps the durable result/freeze pointers that explain why the current question is specifically source-to-global transmission.

Current authority: `docs/STATUS.md`. Current priority: `docs/ROADMAP.md`.

## Frozen finite-size result chain

| Asset | SHA / path | Result / boundary |
|---|---|---|
| Frozen norm-4 q2/Jordan production | PR #273 head `8b26a30a785bc142a9d17bfed99a8d0e98ddc4dc` | Declared scalar q2 law rejected; Jordan remains a borderline survivor, not uniquely identified. |
| Generation-4 target | PR #277 head `3e855ced4fd98d8979c0b712636b45c2fa54f969`, `results/server-20260829/P154-norm4-generation4-pilot` | N520/N680 100M each; lambda 0, 1/2 and 1 all compatible at similar scores; secondary eigenvalue not identified. |
| Ordinary integrated bulk-energy alias | `54b3e80822fa4c407470cd669912c959b9ea4591` | Reconstructed integrated `J_bulk` is exactly the existing topology-even `P4[E_top']/2`; it is not a second independent field coordinate. |
| Fixed-p even/source response | `56a6267d6a6826a165f93ed3a64a670ca7088180`, `results/p40-even-given-odd/REPORT.md` | Strong source response exists; raw H4 direction remains unresolved and does not identify an energy/operator field. |
| Source thermal analysis | `29f339faa71064436b4350027f258dcccc43603d` | Common source/root response and thermal-clock decomposition on reused historical streams; not a fresh validation block. |
| Source precision / two-stage analysis | `b77f3e2a56df2b033561e6fe9164d09ab4698949` | Endpoint precision improves; candidate source-transfer stories remain unresolved; no independent evidence vote from nested reuse. |
| Conditional winding / fixed-K localization | `d2a3f445abcb99f9a21bc7b5cd70b5a22e6a77c7` | Strong rank-one internal spatial response survives fixed-K controls; this does not imply global `U` transport. |
| Current lagged-source analysis | `dd48177340f169c18cd1fc9217101b54090e1e3a`, `results/norm4-lagged-source/REPORT.md` | Reused 2.4M permutations show strong prior-step source effects on later entry/exit and moving-root rank-one population; original global `U` H4 source response remains unresolved. |
| Large discovery archive | PR #509 head `0820b8d203e2dc534bb883d6fdb4d6d1e0acb11f` | Additional source/contact/lag explorations; not an independent prospective block. |
| Research/context archive | PR #267 head `dd48177340f169c18cd1fc9217101b54090e1e3a` | Context recovery + current lag result; branch-local NEXT/decision pages are historical after cleanup. |

## Historical conclusions that remain boundaries

- A source can have a large local or rank-population response while the original pooled-root/slope-normalized `U` response remains unresolved.
- q2/Jordan scale behavior alone does not identify a continuum module; free exponent fitting is retired as an identification strategy.
- The exact integrated `J_bulk` alias removes one supposed second direction instead of creating a new one.
- Replaying the same historical permutations with more source coordinates can improve mechanism discovery but cannot become independent validation.
- The primitive square-bond `x≈4` sector remains distinct from the square-site thermal-Q4 `x=21/4` candidate.

## Current question produced by the history

The next useful object is not another source coordinate. It is one explicit, prospective transmission prediction:

```text
microscopic source
  -> ingress / egress
  -> J_grad = j_in - j_out
  -> d_p E_top
  -> original pooled-root / slope-normalized U
```

The model vocabulary is capped at M0 lifecycle/current-only, M1 contact-regulated current and a possible M2 extra transfer coordinate **only when M2 is frozen before target reveal**.

## Required next freeze

Before any fresh target block, publish a machine-readable prediction that contains:

- the exact source/readout semantics and orientation order;
- numerical M0/M1(/predeclared M2) predictions for the same global `U` target;
- full source/model and measurement covariance treatment;
- an equivalence/separation band showing the models are actually distinguishable at the chosen budget;
- the failure action for each model.

If those ingredients cannot be fixed before new data, do not launch the target block.

## Stop rule carried forward

A validation failure eliminates or demotes the frozen model. Do not use the same target block to add a feature, fit a new exponent or define a new source/readout rescue. A failed transmission test leaves the existing C3 finite-size phenomenology intact and ends the current operator-identification attempt for this observable until a genuinely independent design is proposed.
