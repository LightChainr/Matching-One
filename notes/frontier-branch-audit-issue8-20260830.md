# Frontier branch audit: Issue 8 and adjacent exact/reuse heads

Date: 2026-08-30. Refreshed base: `origin/main@c64bfab`. Lifecycle: navigation only. Issue-8's formerly branch-only patch is now `main_integrated` via PR #397; adjacent results retain the explicit states below.

Several branches descend from older side histories, so the raw `main...head` diff contains inherited files. The ranges below isolate the branch-local scientific delta.

Ancestry check: Issue-8 branches directly from `6f711e5` (1 commit, 4 files). The current-geometry, P333 doublet, P334 two-time and P337 gluing heads have merge bases `9d21f36`, `c1a353a`, `4d70c17` and `ca866f0`; their raw relative-main diffs contain respectively 96, 131, 37 and 10 files, whereas the isolated branch-local ranges below contain 5, 8, 10 and 4 files. The larger counts must not be attributed to the new heads.

| Remote head at audit | Branch-local range and files | Result and boundary | Effect on current target | Absorb? |
|---|---|---|---|---|
| `main@44def18` / PR #397 (lineage `2818f24`) | contract, note, Fraction oracle and tests; main and old branch patches are byte-equivalent | Angular and radial rows commute on the declared tensor fixture. Ratios are synthetic; no production separability, exponent, covariance projector or Issue-8 closure follows | No priority change. Useful exact regression only | Register once as `main_integrated`; do not count the old branch as separate evidence |
| `analysis/p334-current-k0-geometry-pilot-20260830@769d7ae`; bottleneck result `f97b906` | raw locks and frozen scorers retained on their source branches | Fresh cheap geometry and compact bottleneck/2-core/articulation pilots are complete. Neither commonly absorbs the production age signal; bottleneck retention is `75.6%/67.0%` at N325 and `30.4%/27.2%` at N425 | Eliminates two proxy families without proving intrinsic memory; main PR #409/open PR #415 point to cooperative continuation | Run checkpoint-clustered `b2`/survival sketches with two future orders; do not repeat one-pass shape geometry |
| `theory/p333-source-landing-doublet-20260830@7b40ec7`; charge-two freeze `a572058` | width-3 success plus width-4 charge-one result | Width 3 is repaired, but width-4 charge one fails exactly at Gram self-adjointness. The minimum untested irrep is one-dimensional C4 charge two | Localizes the next type without yet requiring rooted history | Run charge two before a larger full-Q/rooted module |
| `analysis/p334-two-time-birth-kernel-20260830@7641a08` | existing-data kernel plus post-reveal eigenspace map | Rank one rejects; the leading-three subspace agrees across sizes within `.44` degrees, without exact rank-three or memory claim | Supplies the stable subspace used by the scored current-geometry result | Retain as one production dependency family, not independent mode votes |
| `theory/p337-typed-six-arm-gluing-20260830@cfb3ead` | one commit after carrier theorem `1c93bed`; note, exact fixtures, oracle and tests | Untyped six arms are insufficient. For globally typed theta-six and figure-eight-eight events, fixed local surgery gives finite-energy costs `p^6(1-p)^18` and `p^8(1-p)^16`, each at least `eta^24`. The remaining reverse comparison needs a uniform landing-separation probability and frequency of the required component partition/deck determinant | **Closes the bounded local-surgery part** of the `D_N` bridge, not the probabilistic gluing constant, figure-eight suppression, exponent or universality transfer | Yes: replace “prove local gluing” by measuring/proving globally typed arm frequency and separation; store component IDs plus relative deck-address vectors at annulus cuts |

## Priority consequence

1. Run the frozen width-4 P333 C4 charge-two alternating landing gate; charge one already failed.
2. Run P334 checkpoint-clustered cooperative-pair/survival sketches; both cheap geometry and the frozen bottleneck proxy have already failed common absorption. Keep `J12` risk-composition versus conditional hazard separate.
3. Redirect the P337 arm lane from local surgery to the nonlocal typed quantity: landing component partition, two relative deck vectors, separation constant and figure-eight share. Full microscopic paths are unnecessary.
4. Treat PR #397 / `44def18` as one exact utility fixture. Do not duplicate its old branch lineage.

The adjacent branches remain `branch_only`; PR #397 alone is `main_integrated`. Shared ancestry and dependency groups must not be counted as independent evidence.
