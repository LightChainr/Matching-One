# P334 active-boundary organization pilot freeze

This 20k/size pilot follows the revealed `8fd596d` localization and is therefore
explicitly mechanism-directed, not an independent discovery block.

The runner adds five aggregate current-boundary fields only: the horizontal
versus vertical Fourier imbalance of cut edges, corner-versus-opposite contact
balance, number of vacant-frontier components, largest vacant-frontier arc, and
sum of squared arc sizes.  They are obtained during one graph traversal and no
configuration or component labels are saved.

The primary comparison keeps the cross-size rank-one morphology state from
`8fd596d` fixed.  The fresh paired shift from that state to the same state plus
all five organization coordinates is added to its disjoint production-anchored
age residual.  Absorption requires both size-level joint p-values to be at
least `0.01` and all four retained magnitudes to be at most `25%` of the
`8fd596d` residual.

As a fixed secondary description, the same opposite-size rank-one score is the
baseline for temporal modes 2 and 3.  We report the incremental line-centered
R2 from the organization vector with its complete batch covariance.  No mode,
orientation, coordinate, or target is selected after reveal.
