# P334 mixed-source response: bounded reuse allocation

The previous contact-loading readout is complete (`f225d2e2`). This new
question is nonlinear response under the already fixed Euler-preserving
two-source family, not another fit of prefix means or conditional-shape test.
PR509 retains held-out `J=B G` prediction and fixed-prefix shape analysis.
No cloud, new source, prefix, suffix, determinant or finite-policy production
is requested. Coordinate through this repository record and final receipts.

For joint-safe degree class a, retain its mass pi_a=n_a/d and set

```
q_t(u|Z) = pi_a exp[pi_a(t_f L_f(u)+t_s L_s(u))]
           / sum_{v in a} exp[pi_a(t_f L_f(v)+t_s L_s(v))].
```

Outside safe classes use1/d. Both L marks are R0-only. The natural source
coordinates and their normalization are fixed by the prior one-source work.
The score is s_i=pi_a(L_i-mu_ai), and the second density derivative at zero is

```
t_ij = s_i s_j - pi_a^2 Cov_a(L_i,L_j).
```

The mixed response is H_fs F=E[t_fs F]. It is zero outside original00.
Nonzero mixed response would refute additivity in these **specified source
coordinates**, not prove path memory, noncommuting perturbations or a field
identity. The exponential source parameters commute.

## Parallel deliverables

- **Root**: reuse `8ad30617` saved finite-source20-batch vectors. The four
  prior g_plus/g_minus points at t=±1 are precisely the rectangle corners
  physical(t_f,t_s)=(±1/2,±1/2). Form
  `plus(+1)+plus(-1)-minus(+1)-minus(-1)` for every existing A/E endpoint
  and physical/S/D observer. This is an exact finite interaction contrast,
  not exactly the zero-source Hessian. Do not recompute finite weights.
- **p321**: extract the exact ff/fs/ss second-response tensors using the
  old375cd3a1 NPZ and existing new64 raw extension at8ad30617, with exact
  class counts/loop products from1cfa4ae8. Derivative score integer numerator
  is `(n L_i-S_i)(n L_j-S_j)-(n Q_ij-S_i S_j)`, denominator d².
  The paired estimate is `(t_ij(U)-t_ij(V))*(F_U-F_V)/2`, with two suffixes
  averaged per label. Report physical receivers and fixed observables
  `A(p_ref),E(p_ref),C,W`, p_ref=.59274605079; C/W normalized byN+1.
  Keep old8 and new64 separate; new64 only00 with the full1000-per-batch
  denominator. Retain original20 batches, prefix identities, all tensors,
  per-prefix quartet means and centered batch covariance. New64 first
  responses already exist in saved prefix statistics; reuse those if needed.
  No finite weights, determinants, prediction model, shape test or new tails.
- **p337**: exact theory note for this fixed-coordinate Hessian, class-mass
  invariance, integer centering, rectangle averaging of mixed derivative,
  and what additivity/nonlinearity claims are justified. No simulation or
  literature campaign; derive from the explicit finite probability family.
- **p158 (after inputs exist)**: single covariance join of root finite
  rectangle and p321 exact tensors; align original20 deleted-batch rows
  (the old finite-source factor uses the opposite sign convention). Report
  mixed-vs-diagonal and finite-vs-zero-source differences with dependence,
  without rerunning readers or creating an inverse covariance test.

Each agent uses an isolated branch, pushes its bounded result and returns
one final receipt. Root integrates one scientific card, then one PR267 pointer.

## Bounded covariance readout after the first tensor is available

Root rectangle result is `e7473233`; theory is `a6e7141a`. The exact tensor
has now shown negative own-center diagonal curvature at both geometries
and both sizes, with weak mixed components. The coordinator should keep
the complete tensor and add only the following physically named summaries:

- For each receiving geometry, own-minus-other pure curvature and own-minus-
  mixed curvature, as paired differences on original00 new64.
- Original8 finite mixed rectangle minus original8 H_fs(0), retaining its
  exact pairing; separately retain new64-minus-old8 H_fs, which shares
  prefixes but changes conditional suffix stream.
- Read the already saved new64 first-response mean matrices at8ad30617
  (`prefix_statistics_N{N}.npz`) to give own-source H_oo F / H_o F for
  F=C and F=A_ref only, whose first responses are resolved. This is local
  fractional change of response gain per unit natural-source parameter,
  not a finite extrapolation. A fixed t=1/2 quadratic-to-linear Taylor
  contribution has ratio(1/4)*H_oo F/H_o F. Do not form ratios for weak
  cross responses, E or W.

The existing shared block is `172fbeb1`; all new tensors, rectangle and
first-response rows keep the same original20 deletions. No new fit, source
weight computation, trajectory or high-dimensional covariance inverse.

## Completed receipt and transition

All bounded lanes above are complete: rectangle e7473233, Hessian
c48fa360, theory a6e7141a, shared covariance621dfbd0. The root's exact
source-normal decomposition is2c3a5ca2, interpreted at20a95bf2. It reveals
positive own-center normal response opposing the larger negative tangent
part; this is still the same original discovery block.

Following the user's new direction, this work item stops here. The next
task freezes competing response predictions and generates independent
prefixes for an actual intervention; it does not extend the contact-feature
catalogue or repeat PR509 prediction/conditional-shape work. Machine use and
the frozen protocol will be recorded separately in the repository.
