# P267 rho-child Alexander-even complex C3 production

The existing rho-child production answered the primitive rank-one polarization
question.  This acquisition asks an orthogonal topology question with the
already canonical even coordinate

`E_top = P0 + P2 = 1 - P1`.

At each actual N112 child modulus, subtract the continuum rank-even probability
obtained from the complete Pinson--Arguin primitive-line sum.  The primary is
the nontrivial complex child character

`delta_Etop_r1 = (delta_E0 + zeta^-1 delta_E1 + zeta^-2 delta_E2)/3`.

It neither contains `A_top=P2-P0` nor changes angular harmonic.  A same-stream
primitive-H4 row is retained solely to form the amplitude-free two-observer
determinant across C3 characters.  This separates an independent even
rank-redistribution ray from a shared geometry mixture without reopening the
H4/H8/H12 vote.

The production uses one common 224-bit field on all three children, 2M
replicas, 100 batches, and the fresh seed/counter domain frozen in the manifest.
The decision threshold is 0.01 and no top-up is permitted.
