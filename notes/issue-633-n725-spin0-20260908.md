# #633 N725 spin-0 reanalysis (existing histogram only)

One reanalysis of the committed N725 block. No new Monte Carlo. Draft PR, no merge, no `docs/STATUS.md`.

**Histogram (locked):** `results/server-20260907/P612-n725-fullcurve/raw/n725_100m.{hist.csv,moments.csv,metadata.json}`  
`git_commit` `8b5f9d1a`, seed `2026105011`, 100 batches × 1e6 paired = 1e8 per orientation, reps `(26,7)` and `(23,14)`. Command in the metadata file. Do not rerun `threshold_rank_integer_period_mc`.

**Engine:** `scripts/threshold_quantile_lineage.py` via `scripts/probe_invariant_shape/n725_zflow_corrected.py`.  
**Artifact:** `results/probe-invariant-shape/n725-zflow-corrected.json`.  
**Withdrawn object (kept on disk):** `results/probe-invariant-shape/n725-zflow.json`.

---

## What was wrong, and what replaced it

| | `n725_zflow.py` (#628) | this reanalysis |
|---|---|---|
| `cos_four_theta` | `(a²−b²)/N = cos 2θ` | lineage: `(a⁴−6a²b²+b⁴)/N² = cos 4θ` |
| spin0 weights | (−1.1326530612244896, 2.13265306122449), extrapolation | (0.5382777069160998, 0.4617222930839002), interpolation |
| Σ w | 1 | 1 |
| Σ w · (true cos 4θ) | **−1.7944485136741972** (spin-4 leakage) | **0** |
| combination | mix rank CDFs, then invert | invert Q per orientation, then `Σ w_i Q_i` |
| grid | nine slots, 0.4 and 0.6 replaced by 1/4, 3/4 | **11 unique**: deciles ∪ {1/4, 3/4} |
| Z | mean of 100 batchwise Z | Z of the **pooled** histogram; delete-one of the whole map for covariance |

Weights and cos 4θ agree with the committed #612 scorer (`p612-n725-score/latest.json` `label_crossing`) to machine precision. Equal weighting is a sensitivity, not a second vote.

Reproduce:

```bash
python3 scripts/probe_invariant_shape/n725_zflow_corrected.py
```

---

## Corrected pooled Q and Z (spin0 primary)

Levels in order `(0.1, 0.2, 0.25, 0.3, 0.4, 0.5, 0.6, 0.7, 0.75, 0.8, 0.9)`. Z anchors `a=1/4`, `b=3/4`. Jackknife SE from 100 aligned delete-ones.

| u | Q_pooled | Q_se | Z_pooled |
|--:|--:|--:|--:|
| 0.10 | 0.54183362 | 2.74e-6 | −0.468071 |
| 0.20 | 0.55970681 | 2.35e-6 | −0.127050 |
| 0.25 | 0.56636562 | 2.27e-6 | 0 |
| 0.30 | 0.57229601 | 2.20e-6 | 0.113152 |
| 0.40 | 0.58290959 | 2.12e-6 | 0.315659 |
| 0.50 | 0.59274645 | 2.08e-6 | 0.503347 |
| 0.60 | 0.60253412 | 2.07e-6 | 0.690096 |
| 0.70 | 0.61298542 | 2.10e-6 | 0.889507 |
| 0.75 | 0.61877643 | 2.13e-6 | 1 |
| 0.80 | 0.62523757 | 2.17e-6 | 1.123279 |
| 0.90 | 0.64236730 | 2.45e-6 | 1.450114 |

Q(1/2) sits 4.0×10⁻⁷ above the accepted p_c ≈ 0.59274605. That is a location reading, not a threshold claim.

Equal-weight Q differs from spin0 by < 2×10⁻⁷ at every listed level (both orientations already have the same N). Sensitivity, not a second result.

Old zflow batch-mean Q(0.5) was 0.59275195. The shift −5.5×10⁻⁶ is from the four bugs together (weights + mix order + grid + batch-mean vs pooled), not from new data.

---

## Retractions

1. **“Batch-noise covariance full rank ⇒ mean shape cannot be one-parameter.”** False. Counterexample in the issue body: y_N = m + f(N)g + ε with ε uniform on {±e_i}. Mean on a line, noise covariance I/d. Withdrawn.

2. **159.6° as almost-orthogonal.** `blindness-and-glift.json` affine-removed angle between consensus g and ΔZ_{L=4}−ΔZ_{L=3} is 159.6248°. Unoriented angle between lines is **20.375°**. Report that. Tiny-torus ΔZ does not prove an asymptotic mismatch.

---

## g comparison (compatible chart)

g is the frozen 9-decile consensus from the five committed transitions (725 never in the freeze), `p612-n725-score` spin0 vector.

**The issue’s comparison is g versus ΔZ in the same chart**, using

```text
DZ_Q[g](u) = (g(u)−g(a))/W − (Q(u)−Q(a))(g(b)−g(a))/W²
```

with default shared anchors **0.2 and 0.8** (both exist on g). 1/4 and 3/4 would need interpolating g; that sensitivity is not silently used.

On the **tiny torus** (existing ΔZ, not N725): unoriented **20.4°**, as above. That number is now stated correctly; it is still a lab-size angle.

On **N725** there is no second production in this ticket, so there is no N725 ΔZ. The JSON also records the angle between DZ_Q[g] and Z_725 itself in the 0.2/0.8 chart: unoriented **81.4°**. That asks whether one size’s shape is parallel to the consensus flow direction. It is **not** the issue’s g-versus-ΔZ test and is not used as an incompatibility claim.

Exact-lab ΔZ should be read from this working tree’s repaired `census-exact.json` (PR #653, bond `dual_fail=0`), not from PR #628’s broken bond Z. This ticket does not re-litigate the L=3,4 exact shape.

---

## What still stands, at this finite N

- The histogram, the #612 spin0 Q-path, and the 290→725 amplitude forecast are unchanged: this ticket does not re-score #612.
- A one-parameter mean shape is **not** ruled out by batch covariance rank.
- g is **not** almost orthogonal to the tiny-torus ΔZ (20.4° unoriented).
- Compatible N725 g-versus-ΔZ remains **undetermined** without another size. Stop.

Does not fund a larger N, a new observable, or a free exponent.
