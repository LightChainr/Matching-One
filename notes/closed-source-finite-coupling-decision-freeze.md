# Fixed finite-coupling decision for the closed source

This freeze predates the new `(K,g,q)` histogram and every finite-coupling
score. Source, geometry, observer and four multipliers are fixed in
[the contract](../analysis/p337_closed_source_finite_coupling_contract.json).

The named model is the already closed action `S*=C+F+Bv`. Write
`J=beta1+beta_null`, `g=2K-J`, `m=exp(t)` and
`h=p/((1-p)m)`. After a configuration-independent factor, its weights
are exactly `h^K m^(-g)`.

The proposed strong-coupling argument has a sharp global consequence.
On the honest N25 pair, empty and full configurations have g=0 while
proper nonempty occupied sets have g>=2; rank-one sets have g>=3.
If this cut-space argument holds, at fixed finite N the matching root
has h0->1, E->1, and U(t)->0 as t->infinity. The previously published
positive U(0) and positive U_t(0), together with the now-proven simple
positive-t root, then force a turnover. This is a new consequence of a
fixed source, not a fitted description of archive residuals.

The single new counting pass retains the full finite-source sufficient
statistic `(K,g,q)`. Old per-K first moments cannot exponentiate the source
at finite t. It uses the same N25 quotient and rollback connectivity code;
there is no new Monte Carlo stream or new geometry convention.

Evaluate only m=2,4,8,16. Each geometry is normalized separately before
forming pooled Q and normalized P4(E). Compute U and its root-complete
derivative, including root motion and slope motion, using rational
intervals. A strictly negative U_t/A enclosure resolves a downturn. All
four outcomes will be published; an unresolved fixed grid is not a reason
to extend it. No new source, extra descriptor or adaptive coupling scan
belongs to this calculation.

The infinite-coupling limit is finite-volume two-state concentration.
No thermodynamic transition, universal exponent or continuum-field
identity is claimed. This does not reopen the stopped independent F4 or
lag-one transmission blocks.
