# Unacquired Large-Scale Compute Queue — 2026-09-01

This note records computations that have **not actually acquired their target
population**. It is deliberately narrower than the open-Issue list. A route is
not launchable merely because it appears here; its activation gate must first
be satisfied. The machine-readable source is
[`analysis/large_scale_compute_queue_20260901.yaml`](../analysis/large_scale_compute_queue_20260901.yaml).

## Current decision

Issue #275 remains the only P0, but it has **no large Monte Carlo block queued**.
Its next discriminator is a same-source, same-observer, same-normalizer
restricted rank-0/rank-2 transport calculation on existing assets. Only a
proved missing coordinate may justify one fresh acquisition. The exact
Gaussian-ideal rotation symmetry closes further same-character angle scans.

## Genuinely unacquired large computations

| Route | Target acquisition | Launch gate | Resource decision |
|---|---|---|---|
| #156/#159 Pell–hexagonal character tomography | Square-bond control at `N=418/780`, followed conditionally by square-site full curves | Typed C3/H4 projector, actual-modulus continuum subtraction, exact/control pass and frozen covariance score | Best current large-production candidate after the gate; parallel Huawei CPU |
| #158 Gaussian commuting square | `N=650/850` root-character target `7/1250` | A new leverage argument must overturn the completed power gate | Target data are zero, but projected costs are 27.21B/55.18B samples; keep stopped |
| #11/#31 transfer-matrix frontier | Reproduce through width 24, then profile widths 25–28 | Measured state/memory/runtime model and independent canonicalization check | 32 GB hosts for sub-frontier work; frontier likely needs 256 GB+ CPU, to be measured |
| #112 rigorous block-event confidence | Fresh final IID trials under the 400-trial/cutoff-373 contract | Useful cell scale, power, genuine-IID provenance and domain separation | Parallel CPU after the geometry decision; currently parked P2 |

The only unacquired medium-scale experiment worth keeping visible is #227:
an N65 Boolean spectral-sample pilot with 100k–1M base configurations and a
single covariance-aware 5–7 point noise curve. It changes the measured object,
but it is not a large-size frontier calculation.

## Explicit non-queue

The following are completed or stopped and must not silently return as “large
compute still pending”:

- #537 N65 20M and N145 200M; no top-up, third size or descriptor grid;
- #154 N85 5M and N340 160M prospective transmission;
- #334 both N325/N425 independent interventions;
- P57 norm-5 500M and P50 N145→290 full curve;
- N100/N400/N900 shape/clock production;
- Gaussian-ideal C3 N65/N145 angle production;
- HNF exact enumeration through index 13.

Further brute-force HNF indices are not a default compute task; the scientific
continuation is a general theorem or a specifically motivated counterexample
search.

## Machine routing

Large union-find/percolation acquisitions default to the Huawei 16-vCPU/32-GB
CPU pool, sharded by independent counter domains. Local compute is for
contracts, smoke tests, scoring and small exact work. Current algorithms do not
justify GPU rental. Transfer-matrix widths beyond the reproduced frontier are
the exception: they need a measured high-memory CPU decision rather than more
32-GB shards.
