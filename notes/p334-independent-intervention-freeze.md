# P334 independent intervention: freeze before new production

Date2026-08-31. Owner: mathematics execution team. This is a new random
prefix population, not another conditional continuation of the old40k.
The discovery block ends at f97c227c. No result from the block below exists
when this document is committed and pushed.

## Two competing predictions, one primary scalar

M0 (complete two-score label closure): at every prefix,
`m_C(Z,u)=c_a(Z)+b_f(Z)s_f(Z,u)+b_s(Z)s_s(Z,u)` with common slopes across
safe classes. It predicts every source-normal center response exactly0.
This is stronger than the first-Jacobian approximation J=B G.

M1 (transportable source-normal response): the discovery response survives
new prefixes under exactly the same geometry and source definition.
Its frozen four means, in N325-first/second,N425-first/second order, are
`(4.116,3.233,3.300,3.977)*1e-8`. These are empirical finite-size forecasts,
not first-principles continuum amplitudes. Their simple average is
`T_forecast=3.6565e-8`.

Primary target: T, the equally weighted average of these four own-source
center responses. The practical-zero margin is fixed at `delta=1e-8`.
All four coordinates are reported, but no best orientation/size is chosen
after collection. A_ref negative responses are named secondary predictions;
E/W and separate K1/K2 are bridge diagnostics, not additional primary tests.

## Direct finite intervention, no Taylor extrapolation

At each fresh prefix use the exact existing R0 loop source and joint-safe
degree classes. Let `s=(s_f,s_s)`, `G=E[ss^T]`,
`T_oo=s_o^2-pi_a^2 Var_a(L_o)`, and
`phi_oo=T_oo-E[T_oo s]G^-1 s`.
The projection uses the complete label census, not future responses.
Every class has zero mean phi, and E[phi s]=0. Outside original00 the
target is defined0, matching the old normal-response estimand.

For each own axis set `B(Z)=max_u|phi_oo(u)|`. If B=0, contribution0.
Otherwise draw labels directly from

```
q_plus(u|Z)  = [1+phi_oo(u)/B(Z)]/d,
q_minus(u|Z) = [1-phi_oo(u)/B(Z)]/d.
```

Both arms are valid finite laws and retain every joint-safe class mass,
the outside law and instantaneous joint rank/Euler distribution. They
also have the same mean of both original source scores. The perturbed
label is followed by an ordinary uniform suffix. Common random numbers
may couple the two arms, preserving their separate marginals.

The weighted arm difference `B(Z)*(F_plus-F_minus)/2` has expectation
`E_uniform[phi_oo F|Z]` exactly. Thus this direct finite intervention tests
the already measured normal derivative, without a small-eta assumption.
It is a weighted response target, not an unweighted population mean shift.

## Fixed production budget and separation

- N325 andN425, original paired geometries; k0=193 and252.
- Independently seeded uniform fresh prefixes:20 batches of25000 perN,
  500000 prefixes perN, one million in total. Do not select only00 prefixes
  and silently change the denominator. Non00 contributes0.
- Eight paired +/- label/suffix draws for each active prefix and each own
  source. Both receiving geometries share the declared suffix coupling.
- Base seed `202608311920334`; disjoint N,batch,prefix,source,rep stream
  identifiers. Technical smoke uses a different seed and is not pooled.
- No old prefix, old continuation, old conditional mean or old response
  is used in generating or weighting this block. The fixed census algebra
  and the four frozen forecast numbers are reused.
- Host plan: NePnUn forN325; 551oUR forN425. Remote execution and actual
  CPU/memory quotas will be recorded. Other machines remain available.
- Producer SHA is appended before dispatch, with no parameter/forecast
  change after scientific output. Keep original batch sums and all named
  response coordinates; primary uncertainty uses new independent batches.

## Decision and stopping rule

Use the fresh-batch mean T and SE, combining the two independent sizes
with their within-size paired orientation covariance. The fixed diagnostic
interval is T±3SE; it is not an anytime confidence sequence or a theorem.

1. If the lower endpoint exceeds delta, stop promoting M0 as a full
   conditional-label closure. Continue only toward the source-to-global
   transmission question; do not add descriptors to rescue that closure.
2. If the upper endpoint is below delta, stop promoting the material
   positive M1 forecast as transportable at these sizes. Retain the old
   finding as discovery-block-specific pending the user's separate checks.
3. Otherwise record inconclusive at the fixed budget and stop this block.
   No significance-triggered extra batches or post-hoc fourth model.

Also report the frozen forecast discrepancy without refitting its amplitude.
Neither outcome by itself identifies the unperturbed global anomaly,
continuum field count, a complete Markov state, or an asymptotic mechanism.
M0/M1 are operational finite-size hypotheses, not an exhaustive partition
of all possible physics. The team's research choices elsewhere are not
locked, closed or reprioritized by this protocol.
