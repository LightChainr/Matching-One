# Scientific card: final TM rewrite critical pair

1. **Local theorem.** Ordered fixed-line squares have only `D=1222`, two mixed types, `Y=1112`, and `F=1111`; Alexander duality gives a complete five-pattern birth table.
2. **Finite rewrite.** The only negative product is `D x F`; canonical `R_M` uses `M x M`, then `R_Y` uses `Y x nonD`.
3. **Extreme rays.** All nine Pareto rays have minimal quotient witnesses and rules; four are lower-hull rays, three require synergy rescue, and six are mixed-only.
4. **Bounded closure.** 968/984 rows use `R_M`, 16 use both rules, four rescue signatures suffice, and zero hard tokens remain.
5. **Unique gap.** A general proof must topology-realize the `D x F` cross-switch across displacement classes; the aggregate lexicographic rewrite alone is not that injection.
