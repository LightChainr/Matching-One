Question: Is the locally-subtracted N650 mixed interaction only one ambient-H1 endpoint direction, or is another static typed direction required?

Observable: same-stream N650 `R_c=J_full,c-J_local,c`, projected in `(ES,ED,OS,OD)` against convention-labelled ambient-H1 with the complete joint 8x8 delete-one covariance.

Result: the gain-free common-plane determinant is `68.9591 +/- 0.4948` (`z=139.37`); after calibrating the shared gain on ES, the H1-orthogonal OS residual is `-34.4959 +/- 0.2474` (`z=-139.45`).

Geometry: the corresponding ED/OD residual jointly remains null (`chi2(2)=0.01976`, `p=0.99017`), so the surviving direction is common across the two static HNF embeddings.

Boundary and next gate: this eliminates exact local incidence plus one ambient-H1 endpoint direction, but does not identify chronology/memory, noncommutation, Jordan structure, or a field; the smallest identifying acquisition is one same-semantics Gaussian-by-annulus 2x2 common-random rectangle retaining typed colour, before/after ambient H1, `J_local`, connected `R`, ED/OD, and full covariance.
