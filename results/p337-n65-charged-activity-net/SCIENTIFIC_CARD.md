# Scientific card: A charged orientation response is a net-timing counterflow

- Mechanism changed: the N65 A charged score is not common traffic.  Under the
  frozen `(W,J_minus,J_plus)` split, net derivative alone carries
  `11.494/1 df`, while common activity carries `0.286/1 df`.  D has no resolved
  net or activity mode.
- Covariance result: A full score remains `12.153/3 df`; adding activity after
  `(W,net)` contributes only `0.236`.  Correlation eigenmodes independently
  localize the score to modes with substantial net-current content.
- Does not prove: independent evidence, large-N survival, or a continuum field.
  This is an invertible re-expression of the same 20k block.
- Observer/sector/source/geometry: charged F3 plateau and source/sink current;
  intra-axis A/B1 and intra-diagonal D/B2; N65 `8+i` versus `7+4i`.
- Dependency: `83207c7` and raw block `1714141`.  The `381984d` line-sorting
  coordinate is the same dependency group.  Exact `77aa3fe` N13/N17 tables are
  a separate group and are not pooled.
- #334 crosswalk: `J_plus` is common activity and `J_minus` the counterflow
  residual.  The shared morphology is a timing/composition residual of large
  boundary currents; the representation sectors remain different.
- Next discriminator: carry the unchanged activity/net triplet to a larger
  independent rank-birth block and ask whether A net timing persists while
  common activity continues to cancel.
