# P334 / P403 / P429 — marked interior-bridge mechanism

- **Mechanism changed:** the real same-(H2,b2,degree-square) clock collision is
  localized: A's five genuine triples share the single interior site198;
  B's19 are carried by six sites across two factors, led by184(6) and340(5).
  The exact new marked statistic is
  `m3(y)=|N(y)∩L||N(y)∩R|−e(N(y)∩L,N(y)∩R)`.
- **Exact scope:** fixed-node clique elimination preserves the already
  certified physical two-terminal event for every selected random-site subset.
  Every minimal triple has a unique L–I–R decomposition. No triple enumeration
  or continuum assumption is needed.
- **Not established:** this structural middle mark is not the actual final
  birth-site mark; g3 does not explain every later full-clock difference; no
  CFT identity, universal population law, or path-memory claim.
- **Observer / sector / source / geometry:** black-NN rank1→2 minimal triple
  channel; counters43042508631/43042514803, N425 second HNF, k0=252, age10,
  ell=(12,-19). Site IDs and typed ports are retained in the artifact.
- **Dependency group / lifecycle:** same old P334-cooperative-closure N425
  block; source maps at87b6ca5b → exact graph lemma → marked local adjacency
  count → 5/19 decomposed. Two configurations only,0new samples,0newDP solves.
- **Next discriminant:** use the typed interior-bridge profile as an explicit
  third-order state coordinate; keep its structural-middle meaning separate
  from the actual insertion-site distribution. This follow-up does not launch
  that production or expand the cohort.
