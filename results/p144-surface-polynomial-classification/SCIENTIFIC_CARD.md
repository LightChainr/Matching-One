# Scientific card

1. **Exact identification:** `M=(z*d_z-1)R_site|z=1`, where `R_site` is the black ambient-rank quotient of the typed site-state sum.
2. **Closest named readout:** Krushkal's subgraph/complement genus difference equals `q` on the torus, but its edge-subset state family is not the matching site-state family.
3. **No-go:** one ribbon edge or one transition smoothing cannot realize the required four-terminal `J4`; a minimum three-edge gadget has six unwanted mixed subsets unless a new block projector is declared.
4. **Minimal extension:** besides occupation, one terminal topology source `Q` is necessary and sufficient; locally the ground element must be a typed `J_edge/J_face` partition state.
5. **Oracle:** all 512 axis-L3 masks make black-rank, relative-rank and side-genus derivatives reproduce the same defect coefficients; order two is the first occupation-only counterexample.
