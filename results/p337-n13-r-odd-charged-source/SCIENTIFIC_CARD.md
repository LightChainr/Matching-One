# Scientific card: explicit R-odd F3 defect activates both missing projective blocks

- Mechanism changed: the exact unweighted A/D null is not a dead sector.  The
  minimal phase defects `omega^q_A` and `omega^q_D` activate the two independent
  quarter-turn charge-2 multiplicities, respectively D4 `B1` and `B2`.
- Exact response: `O_C(omega)=(omega-omega^2)W_C/2`; at `p_ref`,
  `W_A=0.319893954769` and `W_D=0.0201214105295`.  State coefficients and the
  birth1/exit2 identity are exact at every Bernstein degree.
- Selection rule: A source has only A response, D source has only D response;
  H and A-D cross-response remain zero.  The scalar defect partition is even,
  so its first derivative at zero is still zero.
- Does not prove: survival of either response in a continuum limit, a CFT field
  identity, or significance of the former unweighted N65 D fluctuation.
- Observer/sector/source/geometry: F3 projective-line state and birth/exit
  currents; R-odd A/B1 and D/B2; explicit `u^q` source; exact Gaussian `3+2i`
  quotient, N13.
- Dependency: exact subset/orbit-flux backend and the `770fe27` unweighted-null
  theorem.
- Next discriminator: from one existing-projective archive and one covariance
  block, publish `(W_A,J_A,birth,J_A,exit,W_D,J_D,birth,J_D,exit)`.  A nonzero
  scaling projection or a stable A:D response ratio would then distinguish a
  genuine charged/projective field from a purely finite quotient response.
