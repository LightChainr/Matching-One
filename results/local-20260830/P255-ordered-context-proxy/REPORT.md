# P255 ordered-context audit and N650 archive proxy

## Physical AU/UA decision

The existing arbitrary-period and P253 annulus runners cannot be minimally
spliced into physical `AU` and `UA` actions.  The selected exact-maximin row is
`N85 -> N425` under the norm-five cover.  P253 radius is an observer index,
not a retained boundary-connectivity state; moreover `R7/R8` is noninjective
on N85.  The common injective `R4` disk lies below the N85 systole and makes
the requested ambient-H1, local-incidence and connected-topology state
degenerate.  No static rectangle is renamed as order.

## Existing-archive proxy

The committed N650 20k stream was replayed with exactly the same seed,
counter interval and batches.  All eight legacy batch sums match the committed
archive exactly.  The replay adds `h0,h2,h5,h25`, partition ranks, `Jlocal`,
connected `R`, and the typed two-geometry `ES/ED/OS/OD` projections.

The frozen order proxy is

```text
Cact = 1{h25=2} (1{h2=2} - 1{h5=2}).
```

Its point and marginal errors are:

| channel | value | SE |
|---|---:|---:|
| ES | -0.000950 | 0.000146 |
| ED | +0.000100 | 0.000142 |
| OS | +0.000900 | 0.000149 |
| OD | -0.000150 | 0.000141 |

The complete covariance has rank 3, not the frozen assumed rank 4.  The exact
batchwise null direction is `ES-ED+OS-OD=0` on this replay.  The identifiable
subspace gives `chi2=43.1466/3`, `p=2.29e-9`, but the preregistered rank-four
gate therefore fails closed and no extension is allowed.

This is still mechanism-useful: a topology-typed intermediate filtration
contains a strong order-sensitive direction on the old stream, while one
geometry/colour projection is structurally absent.  It is not the missing
Gaussian-by-annulus commutator and is not evidence for memory or a continuum
generator.  The smallest real acquisition must retain the typed annular
boundary-connectivity partition plus its Gaussian lift/pushforward map.
