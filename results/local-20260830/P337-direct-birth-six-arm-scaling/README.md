# P337 direct-birth four-scale score

Single completed score under freeze `9e73388` and scorer `95b14ce`; no new
simulation.  `batch_direct_rates.csv` contains the paired first/second direct
counts and denominators for every size and batch and is sufficient to rebuild
all covariance matrices in `score.json`.
