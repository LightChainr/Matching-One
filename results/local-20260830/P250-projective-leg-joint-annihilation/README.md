# P250 candidate-constrained joint annihilation result

Zero-new-sample reanalysis of the pinned N505 bivariate 80k archive: 400
aligned batches, both hands, charges 1/2 and the radius-four diamond.

The identity-linear implementation replay exactly reproduces the `a770ac9`
shared rank-five score.  The post-radius-five Alexander R2 plus conjugation
candidate fails the stricter joint null (`p=0.0058241`); only Alexander R3 plus
conjugation survives the four-map primary family (`p=0.0774864`).  Because the
radius-five line-direction result retained R2 and rejected R3 at the same
threshold, no fixed parameter-free map survives both frozen gates.

See `score.json` for every candidate's full `38 x 38` covariance and the core
six-map `228 x 228` cross-candidate covariance.  These are correlated
reanalyses, not independent votes.
