# Scientific card

1. **Exact identity:** `q_(k+1)/q_k=(1-xi_k)/(1-beta_(k+1))`, where `xi` and `beta` are normalized exit and birth boundary hazards.
2. **Exact conditional lemma:** complement duality swaps birth/exit and reverses layers, so nondecreasing exit hazard on both primal and matching carriers implies ULC.
3. **Exact local mechanism:** exit pivotals nest upward and birth pivotals nest downward along every internal fixed-line edge.
4. **Precise missing term:** uniform-layer monotonicity additionally needs edge slack to dominate `Cov_k(u,h)/E[u]-Cov_(k+1)(d,h)/E[d]`; this correction is negative in 484/984 audited pairs.
5. **Bounded evidence:** all 480 primal/matching fixed-line sequences through `N=12` pass the uniform hazard criterion; a general aggregate two-step inequality remains conjectural.
