# P275 A_top field-identity selector

H4 is an input, not a model choice.  The selector measures only the complex
rank-birth response

```text
gamma_D4 = Cov(A_top,J_D4) / B,
A_top=P2-P0,
J_D4=sum chi4(ell)(I12-I01).
```

No existing archive can reconstruct it: the quotient prism lacks `ell` and
same-sample `A_top*J_D4`; the Hecke children have only square modulus; the
chiral archive is a different charged local observable.

The frozen new design uses N=50,130,170 and the genuine moduli `i`, `2i`, and
`5i/2`.  All nine period matrices have determinant N and Smith type `(1,N)`.
After exact H4 frame transport, score the 18-real-vector of
`Y=N^(13/8) gamma` with its complete synchronized covariance.

- ordinary thermal Q4: `Y=c*E4hat(tau)`;
- thermal Q4 Jordan: `Y=E4hat(tau)[c0+c1 log N+c2 e_eta(tau)]`;
- other pure H4: an unrelated modulus intercept;
- other logarithmic H4: unrelated intercept and slope at every modulus.

The Q4 numbers are parameter-free after normalization:

```text
E4hat(2i)/E4hat(i)     = 11/4,
E4hat(5i/2)/E4hat(i)   = 4.293436854374923...,
[R(2i)-R(i)]/[R(5i/2)-R(i)] = 0.665078134386613...
```

The last ratio is the eta-block cocycle.  The runner extension and 20M/geometry
acquisition are frozen but not authorized or launched.  Full machine details
are in `latest.json`; the five-line result card is `SCIENTIFIC_CARD.md`.
