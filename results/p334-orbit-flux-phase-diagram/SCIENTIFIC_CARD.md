# Scientific card

- changed mechanism space: the close N13/N17 `p_ref` share is a paired-zero counterflow phenomenon, not a geometry-independent 76/24 law;
- exact phase diagram: birth, exit, orbit net and signed-share zero/pole structure are isolated over all `0<p<1` with rational Sturm intervals;
- mechanism: large common-phase source/sink currents nearly cancel; a 1--2 percentage-point orbit-composition skew creates the H4 net;
- sharp contrast: axis-share slope changes from `-28.31` at N13 to `+55.53` at N17 while the point values remain close;
- next falsifier: a new two-orbit quotient without a close paired-zero reinforcement window rejects the counterflow classification.
