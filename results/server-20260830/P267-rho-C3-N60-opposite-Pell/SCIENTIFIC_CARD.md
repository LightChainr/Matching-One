# Scientific card

1. **Question:** does the coherent N112 r2 remainder preserve the normalization-free E4-squared completion phase on the opposite Pell side?
2. **Observation:** `c60/c112=0.63518-0.14514i`, a phase shift of only `-0.22465` rad rather than a sign flip.
3. **Primary:** the phase-preserving ray has an interior scale `.79676` and survives (`0.9584/1`, `p=.3276`).
4. **Opponent:** the flipping ray formally survives the strict `.01` gate (`p=.0332`) only at the lower scale boundary `1e-6`; N60 zero also survives (`p=.1036`).
5. **Meaning:** E4-squared phase completion remains viable and is numerically favoured, but the frozen result is unresolved; signed-Pell/Jordan-like loss is not excluded because the new covector is not individually resolved.

This does not reopen the three pure harmonic models.  It evaluates one
prospectively frozen complex relation after annihilating E4 and E6 exactly.

