# Scientific card: natural A current persists at N85 but attenuates faster than scale-neutral transfer

- Mechanism changed: the amplitude-free response
  `K_A=p(1-p)Jminus_A/W_A` remains nonzero at an independent scale:
  `Delta_K_A(N85)=0.02559 +/- 0.00793` (`3.229 sigma`).
- Frozen comparison: zero gives predictive quadratic `10.424`; scale-neutral
  transfer `4.033`; project H4 transfer `1.583`.  H4 is closest, but the
  observed attenuation is stronger than its nominal target.
- Does not prove: H4 scaling, a fitted exponent, large-N survival, or a
  continuum field.  No N85 quantity selected the normalization or targets.
- Observer/sector/source/geometry: logit derivative of the explicit F3 A/B1
  charged plateau; Gaussian N85 `9+2i` versus `7+6i`; 200k fresh aligned block.
- Dependency: targets and source-fit covariance from N65 `1714141`; independent
  N85 seed `202608337`; preregistration commit `5e601dc`.
- Exact gates: production matches all frozen fields; current continuity closes
  to `7.9e-15`; raw checksums are archived.
- Next discriminator: precommit a third scale using the unchanged natural
  coordinate and distinguish H4 from faster attenuation without fitting an
  exponent through N65/N85.
