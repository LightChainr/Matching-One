# Scientific card: the held-out N145 current bends away from fast attenuation

- Mechanism changed: the unchanged natural coordinate remains nonzero at a
  third scale, `Delta_K_A(N145)=0.02031 +/- 0.00314` (`6.476 sigma`).
- Frozen comparison: the N145/N85 ratio is `0.794`, against `0.420` for the
  project H4 target and `0.137` for the secondary N65-to-N85 descriptive
  transfer.  Predictive quadratics are zero `41.938`, scale-neutral `0.384`,
  H4 `4.376`, and secondary fast transfer `11.504`.
- Mechanism-space update: continued fast attenuation is not stable at the
  held-out scale.  The response exhibits correction curvature or state/shape
  mixing; scale-neutral propagation is the closest frozen primary target.
- Does not prove: scale-neutral asymptotics, failure of the project H4 field,
  a new state, or a fitted exponent.  The scale sequence uses distinct
  primitive equal-area orientation pairs, so geometry-dependent amplitudes
  remain part of the live mechanism space.
- Observer/sector/source/geometry: `K_A=d_eta log W_A` in the explicit F3
  A/B1 charged sector; Gaussian N145 `12+i` versus `9+8i`; 2.4M fresh aligned
  samples per shape.
- Dependency: all targets and the design were frozen in commit `3d238af`;
  N145 uses a new seed/counter block independent of N65/N85.  Source-fit
  uncertainty is included only in predictive residuals.
- Next discriminator: decompose the same natural current into a fixed
  geometry character before a fourth scale, or use a precommitted geometry
  family whose harmonic factor is identical across scales.  Do not fit an
  exponent through these three revealed points.
