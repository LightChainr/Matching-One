# Scientific card

1. **Question:** does the named non-`A_top` primitive-line response form one clean modular C3 character on the three rho children?
2. **Primary:** clean Pell-transported E4/r1 is decisively rejected (`235.2105/4`, `p=9.97e-50`); every frozen pure support is rejected.
3. **Character content:** ideal r0 and r1 are both resolved (`p=1.73e-50`, `5.59e-32`), while r2 is unresolved (`p=.1034`).
4. **Secondary:** E4+E6 is uniquely close but narrowly misses the frozen `.05` line (`6.0645/2`, `p=.04821`); the other pairs fail strongly.
5. **Meaning:** the observable is a real topology-typed multi-character response at N112, not a clean C3 eigenrow and not renewed H4/H8/H12 ordinary-channel voting.

The exact direction-character gate is passed because this observable sums the
full primitive-line family: `(1,0)` and `(0,1)` already give scalar/spin-4
character rank two in every child. It is not the forbidden single-C4-orbit
direction statistic.

