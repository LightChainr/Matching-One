# Scientific card

1. **Linewise BA:** `A sum bx-(sum b)(sum x)` is half the concordance-minus-discordance mass of marked fixed-line state pairs.
2. **Linewise TM:** `m A N_new+(m-1)X^2>=m A E_2`; `N_new` counts oriented same-line synergy squares and `E_2` ordered same-state exit pairs.
3. **Exact switches:** synergy squares have the canonical `v<->w` involution; Alexander complement swaps joint birth/exit marks and preserves BA.
4. **Injection boundary:** eight nonzero BA equalities require bijection; 74 TM rows have no synergy but positive demand, so the independent `X^2` reservoir is essential.
5. **Conditional topology theorem:** the two linewise path inequalities on both carriers imply ULC; all 984 existing rows pass, while a general homological switching remains open.
