# Exact N25 strong-coupling angular law

`U/A = -(625/1152) lambda^11 +(390625/1152) lambda^13 +O(lambda^15)`,
where `lambda=exp(-t), A=25^(13/8)/2`.

The pooled critical root starts `h0=1+lambda^2-lambda^6+O(lambda^8)`.
The full exact rational computation extends through degree32, with zero
pooled-root residual through that order. Both geometries are normalized
separately and D=Q_h and root motion are included.

Axis rank-one probability starts at g9; its normalized reciprocal shell
has zero h slope at h1. At degree11, the fixed-h slope `5025/2` loses2500
to the root shift, leaving `25/2`: cancellation of200/201. Tilted rank-one
probability begins at g13. The angular projector and D then give the
negative leading coefficient above.

Together with the previously published U(log16)>0, the sign forces a
later zero crossing, a finite negative valley, and eventual recovery toward
zero with positive U_t. No crossing location or extra coupling point was
computed. This is deterministic post-reveal derivation from the same
finite populations, not independent or prospective evidence.

See [the derivation](../../notes/closed-source-angular-strong-coupling-law.md),
[series.json](series.json), and [coefficients.csv](coefficients.csv).
