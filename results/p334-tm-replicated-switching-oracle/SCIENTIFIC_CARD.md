# Scientific card: replicated TM switching

1. **Question.** Can the frozen TM determinant be realized as a local marked-token injection rather than only an aggregate inequality?
2. **Exact reformulation.** Demand is replicated same-state exit pairs; supply is replicated synergy squares plus the independent `X^2` reservoir.
3. **Discovery.** Exact-pair locking fails on 658/984 rows, but preserving one marked site and switching the other admits a canonical integer full flow on all 984.
4. **Theorem.** This injection exists iff every induced demand family obeys the explicit capacitated Hall cut; 1,176,258 bounded cuts pass, tightest ratio `24/25`.
5. **Frontier.** The remaining general proof is precisely that digital Alexander topology forces the proper-subset Hall cuts; the aggregate all-site cut is only ordinary TM.
