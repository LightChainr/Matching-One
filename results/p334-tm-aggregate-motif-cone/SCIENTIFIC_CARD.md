# Scientific card: aggregate TM motif identity

1. **Exact expansion.** Ordered two-site faces split into coexit `D`, mixed `M`, synergy `Y`, and flat `F`, with `M^2+4Y(T-D)-4DF=4(m-1)TMmargin`.
2. **Mechanism.** TM is the curvature-corrected Rayleigh inequality `4DF<=M^2+4Y(T-D)` or `P(D)<=p^2+P(Y)`.
3. **Independence.** Ordinary Rayleigh fails 16 rows and synergy-only fails 68; the bounded cover cone has nine Pareto points and four lower-hull rays.
4. **No-go results.** Per-displacement fails 3,900/51,912, quotient-order pairing fails 220/3,330, an exact Fourier contrast is negative in 802 rows, and reflected carrier margins differ in 474/492 pairs.
5. **Frontier.** The only remaining TM statement is the aggregate four-face injection `coexit x flat -> mixed x mixed OR synergy x non-coexit`.
