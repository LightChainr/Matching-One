# Scientific card

- flux-blind selector: first honest non-quarter-turn HNF with two distinct non-real constant-character line orbits is `[[7,2],[0,1]]`, N=7;
- exact timing result: net zeros `4/7` and `0.592783237894885...` form a close pair inside the frozen N13/N17 envelope;
- frozen falsification: positive character Gram `527/625` makes the contributions cancel between the zeros, opposite to the Gaussian prediction;
- mechanism update: paired source/sink balance times and spin-4 alignment are separate layers, with alignment controlled by the character Gram sign;
- exact theorem: `Re[(chi1 J1) conjugate(chi2 J2)] = Gram(chi1,chi2) J1 J2`; paired zeros fix only the current-product sign;
- #337 bridge: finite-character geometry supplies the Gram factor, while #334 projective timing supplies `J1 J2`; the two are exactly separable;
- next falsifier: another two-orbit quotient must obey Gram-sign alignment topology or reject the two-scalar-current reduction.
