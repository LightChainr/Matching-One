# Scientific card: exact geometry resolves the N145 rebound without choosing a new exponent

- Mechanism changed: the geometry-free `N^-13/8` direction fails on N65/N85
  (`23.134/3`), while the exact H4 covector fits them (`2.575/3`) and predicts
  the held-out two-component N145 vector (`1.519/2`).
- Geometry/curvature split: exact angle rotation accounts for 22.8% of the
  central rebound from the N85-anchored radial target; 77.2% remains as a
  central curvature term, but that remainder is only 1.453 predictive SE.
- Charged descriptor: `q_A^2=(u+H_F3)/2` supplies one projective scalar.
  Adding it improves source chi-square by only 0.121 and is not required by
  N145 (`1.029/2` versus H4-only `1.519/2`).
- Does not prove: pure H4 asymptotics, absence of curvature, or an independent
  projective state.  N65 is noisy and the central values still lean toward
  curvature even though covariance does not resolve it.
- Observer/sector/source/geometry: six single-orientation values of
  `K_A=d_eta log W_A`; exact Gaussian `tau=i`, rational `chi4`; full 6x6
  covariance; N65/N85 direction selection with N145 held out.
- Dependency: reuses the existing N65, independent N85 and independent N145
  archives.  No new random block and no exponent refit.
- Next discriminator: N170 `(11+7i,13+i)`, the exact angle-flip child of N85
  under multiplier `1+i`.  H4 flips while the charged scalar stays fixed.
  The required N170 `tau1,ell,tau2` archive does not yet exist; no run was made.
