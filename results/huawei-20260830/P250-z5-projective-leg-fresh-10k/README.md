# P250 fresh projective-leg 10k

This directory preserves the raw XPk2PZ stream, exact freeze hashes, both
pre-replica failed launch logs, and the single frozen support-first score.

`score_10k.json` contains no phase statistic because d2 cubic support failed.
`raw.sha256` records the server-side raw hashes; `SHA256SUMS` covers the local
package.  The scientific interpretation is in
`notes/p250-projective-leg-fresh-production-result.md`.
