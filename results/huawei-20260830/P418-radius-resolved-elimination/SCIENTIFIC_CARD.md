# Scientific card: P418 radius-resolved elimination

- **Mechanism space changed:** the CRT-mask-times-positive cone survives at
  every radius separately, but one masked spectrum shared across radii four,
  five and six fails in all four channels at `p=1/251`.
- **Raw control:** the cross-radius sharing penalties for the unmasked cone are
  not rejected (`p=0.912`, `0.183`, `0.139`, `0.231`).
- **Masked discriminator:** sharing penalties are `527.04`, `617.17`,
  `1093.32`, and `1111.51`, all at the bootstrap floor.
- **Single-radius result:** zero of twelve masked fits reject; radius four is
  raw/masked equivalent to `2.7e-11`, radius five has only small increments,
  and radius six is saturated rather than informative.
- **Observer/sector/source:** projective-leg endpoint autocorrelation; plus and
  minus hands, charges 1/2; archived radius-four 80k, radius-five 1.2M and
  radius-six 1.2M streams with complete within-radius covariance.
- **What is not proved:** the result does not choose between radius-dependent
  observable transport, gauge flow, or a failure of the stationary common
  spectrum; it identifies no field, Jordan sector, state count, or ordered
  memory.
- **Next lift:** measure or derive an explicit cross-radius transport law before
  assigning the residual to new states.  More samples at one radius do not test
  the eliminated common-spectrum mechanism.
