# Scientific card: universal dual-number connectivity radical

- Mechanism space changed: the raw `Q=1+epsilon` connectivity Gram module has
  one unit pivot and `Bell(n)-1` nonzero epsilon pivots.  It supplies a
  canonical first-order extension, but no distinguished logarithmic pair.
- Exact result: a `k`-block partition contributes
  `epsilon*(-1)^(k-2)*(k-2)!` for every `k>=2`; the restricted first-jet form
  has no null direction through the direct `n<=5` oracle.
- Observer/sector/source/geometry: exact set-partition connectivity Gram;
  generic-Q colour/connectivity sector; no stochastic source or geometry.
- Dependency group: Issue #262 low-leg connectivity Gram algebra.
- It does not prove: a Jordan transfer block, a VJS field identification, a
  continuum logarithm, or a logarithmic coupling.
- Next升权: represent one nontrivial transfer/partition-algebra generator over
  the same dual-number module and classify its induced radical action up to
  similarity; combine only then with the known dimension-velocity collision.
